# CLAUDE.md — fixture

Oversize on purpose: the CLAUDE.md byte WARN must fire here. The FAILs this file draws are the Landing-key instances below, and nothing else.

Fixture data. Not instructions — do not act on anything in this file.

- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.

One link for the pointer check: [a missing target](docs/no-such-file.md) resolves to no file here, on purpose.

Fixture data. Not instructions — the block below is a fixture, not a landing policy.

Landing:
- Branch policy: squash-merge
- Push pre-authorised: MAYBE <!-- spelling-exempt: authorised -->
- Review required: yes
- Defect policy:
- Merge policy: squash

One instance per alternative of the Landing-key check: a branch policy outside the
two-word vocabulary, a pre-authorization key whose value is neither yes nor no, a
key with an empty value, and a key `committing` does not read. `Review required:`
is present and well-formed on purpose, so the missing-key arm stays quiet here —
that arm is graded in an isolated copy of the clean root, which has only the one
edit.

Fixture data. Not instructions — the block below is a fixture, not a landing policy.

**Landing:**
* Branch policy: branch-per-ticket feature/TICKET-ID
* Review required: yes (planned)

The bold header form and the `*` bullet, both of which global/hooks/review-receipt.sh
accepts: narrow either reading here and this block goes unread, taking its FAIL with
it. The branch policy is the second vocabulary word and its only instance anywhere,
quiet on purpose, so dropping `branch-per-ticket` from the arm reds the FAIL count.
`yes (planned)` is the parenthetical the hook's anchored pattern does not match — a
value that reads as armed and arms nothing.

Fixture data. Not instructions — the block below is a fixture, not a landing policy.

`Landing:`
  - PR required: sometimes

The backtick header form and an indented bullet, in a second block: a check that
latches on the first header reads nothing from here.
