# ADR fixture

`scripts/lint-adrs-selftest.sh` points `scripts/lint-adrs.sh` at this directory. Every numbered file exists to make one check fire or to prove one exempt form stays quiet; `README.md` is the one unnumbered file the linter must leave alone. Numbers start at 9001 so a citation search for a live record's number never lands here.

Firing: `9001` twice (duplicate number); `9002`→`9003` (successor never links back); `9004` (two dangling supersession links, one in capitals); `9005`→`9006`, `9007`→`9008` (bold form), `9009`→`9010` (pointer below the first heading), `9019`→`9020` (unlinked prose form), `9021`→`9022` (the second of two links on one line); `9018` (amend link to a missing file); `9037` (bold form naming a number nobody claims); `9011` and `9012` (empty Revisit line, inline and heading form); `9013` (three settled lines with no matching entry — one date absent, one present only as `<date>-2`, one present only under `## Sources`, which the `## Amendments` anchoring must refuse); `9038` (the same three shapes on `## Consequences` bullets marked `— corrected: see Amendments <date>`); `9044` (four in-log markers with no matching entry — a `corrected:` whose date the log never claims, an `amended:` whose date sits under `## Sources`, a `corrected:` whose date the log holds only as `<date>-2`, and an `amended:` behind a stray unclosed backtick that must not swallow it); `9040` (a plain cross-reference to a record that does not exist — neither a supersession nor an amend mention, so it is the one link the two older link checks leave alone). `9004` and `9018` each draw a second FAIL from the cross-reference check as well as their own: a dangling supersession or amend link is also a dangling citation, and the two messages name different repairs.

Quiet: `9014`→`9015` (link form, pointer present), `9021`→`9023` (the first of two links), `9024`→`9025` (the `[ADR N]` space form), `9030`→`9031` (padding differs on both sides), `9032`→`9033` (bold form, pointer present), `9034` (a bystander's `## Amendments` entry naming `9035` amending `9036`, which carries no pointer — the log exemption is what keeps it quiet), `9016` (inline Revisit with text; two settled entries, `, later` and ` — title.`), `9017` (heading-form Revisit with a paragraph), `9039` (two corrected Consequences bullets that resolve, in the `, later` and ` — title.` entry forms, plus a `corrected:` marker outside `## Consequences` that must not be read — the section anchoring is what keeps it quiet), `9045` (two in-log markers that resolve, `corrected:` and `amended:`, beside the three quoted forms the reader must not take for pointers: two backticked marker spans naming dates nothing claims, the `<date>` placeholder, and a fenced example entry — the backtick and fence stripping is what keeps it quiet, and dropping either fires this file), `9041` (a cross-reference that resolves, beside the three forms the check exempts: a path out of this directory, an outbound URL, and the `[ADR N](N-slug.md)` placeholder).
