# Plain cross-reference that resolves, beside the forms the check exempts

One line of rationale. Compare [ADR-9040](9040-xref-dangling.md), which exists.

The forms outside the check, each of which would fire if it widened to every
markdown link: a path out of this directory, [the fixture README](../README.md);
an outbound URL, [the format reference](https://example.invalid/adr-format.md);
and the template placeholder `[ADR N](N-slug.md)` the format file itself writes.
