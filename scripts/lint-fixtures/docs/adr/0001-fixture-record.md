# ADR-0001 — Fixture record

Status: accepted (2026-08-31)

This record exists so the pass-4 spelling walk has an input. Delete the loop
header and this row goes red: the walk over `docs/**` is otherwise reachable by
no fixture, and only the repo's own already-clean tree exercised it.

## Context

The run normalises the row before it lands, which is the British form the walk
must catch outside the skill tree.
