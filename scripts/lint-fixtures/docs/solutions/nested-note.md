# A nested docs note

One level deeper than `docs/adr/`, so the two-enumerated-level glob this walk
used to carry would miss it: `capturing-learnings` writes to `docs/solutions/`.

The colour of this line is the violation.
