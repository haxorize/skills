# A global rule with no Depends line (fixture)

Why not a hook or lint: this file exists to make the admission check fire.

The `Depends:` line is missing on purpose; the self-test asserts on the failure.
