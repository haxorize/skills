# A global rule whose dependant cites it by path (fixture)

Depends: `undeclared-dep`
Why not a hook or lint: this file proves the citation check stays quiet on the path form, `~/.claude/rules/path-cited.md`.
