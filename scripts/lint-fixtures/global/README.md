# Global layer (fixture)

The pass-4 walk reads this file by name. Without it the `global/README.md` half
of that loop is graded by nothing.

A run that favours one form over the other is the violation this file carries —
not `behaviour`, which the URL-exemption reject row downstream needs to itself.
