---
name: shared-trigger-scalar
description: "Fixture model-invoked skill carrying the shared phrase inside a whole-value YAML double-quoted scalar — the form the colon check exempts, so it must still be read. Use when the user says \"walk the tree\" and the description needs a bare colon: here."
---

# Shared Trigger (Quoted YAML Scalar)

A body with nothing in it. The description is the fixture.
