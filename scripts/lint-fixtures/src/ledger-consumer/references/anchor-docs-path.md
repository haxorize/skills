# Anchored by the evaluation directory path alone (fixture)

A path under docs/evaluation/<slug>/ is the only thing anchoring this file — no slug,
no spaced prose form, no bare file name. It names `marketed` and `verified` and omits
the third, so it fires; drop that one alternative from the anchor and this row goes
quiet while every other anchored fixture stays red.
