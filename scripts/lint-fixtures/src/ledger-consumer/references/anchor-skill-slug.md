# Anchored by the skill slug alone (fixture)

The `evaluation-ledger` slug is the only thing anchoring this file — no path, no
spaced prose form, no file name. It names `marketed` and `verified` and omits the
third, so it fires; drop that one alternative from the anchor and this row goes quiet
while every other anchored fixture stays red.
