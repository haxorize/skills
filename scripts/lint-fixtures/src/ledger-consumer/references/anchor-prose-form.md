# Anchored by the spaced prose form alone (fixture)

An Evaluation ledger named in prose, with no slug, no path and no file name, is the
only thing anchoring this file. It names `marketed` and `verified` and omits the
third, so it fires; drop that one alternative from the anchor and this row goes quiet.
