# One status, referenced rather than enumerated (fixture)

This file is ledger-anchored and must still stay quiet, because it names exactly one
status. A `verified` row in the ledger at docs/evaluation/<slug>/ledger.md is judged
against the evidence its own row cites. A file that names one status is not claiming to
list them all, and that is the rule this fixture grades — one status on an anchored
file, not an unanchored line, which `unrelated-vocabulary.md` next door owns instead.
