# Anchored by the file name alone (fixture)

A bare reference to ledger.md, with no directory path in front of it and no slug, is
the only thing anchoring this file. It names `marketed` and `verified` and omits the
third, so it fires; drop that one alternative from the anchor and this row goes quiet.
