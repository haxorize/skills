# Retired form, in a reference file

A `references/` file is where the convention regresses unseen, because a publisher writes
from the template rather than from the skill body. This one names `/fixture-discipline` in
the retired slash form, so the slash-on-model-invoked check must reach past `src/*/SKILL.md`
to find it.
