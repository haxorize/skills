# Unanchored, inside the legend's own skill (fixture)

This file names no ledger word, so nothing anchors it by content. It is read because it
sits inside the legend-owning skill, where every file is about the vocabulary whether or
not it repeats the word — drop that rule and this stops being checked.

The sentence you are reading is the enumeration: it names `marketed` and `verified` and
omits the third, which is what fires. There is no table here and no row below it; the
two status words above are the whole of the fixture, so trimming this prose turns the
row into a no-op rather than leaving it standing.
