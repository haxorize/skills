# A reference file carrying a load gate (fixture)

Its owner, `broken-links`, is model-invoked, so no human is watching for the line
below and the gate has nobody to report to. This file exists because the load gate
was graded only on a `SKILL.md`: a classifier that narrowed the load-gate class to
`src/*/SKILL.md` lost every reference file and no root here went red.

Call the Skill tool with `fixture-discipline`.

Launching skill: fixture-discipline
