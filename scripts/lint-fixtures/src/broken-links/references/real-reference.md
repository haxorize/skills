# A reference that exists

The `broken-links` fixture links here to prove the reference-link check stays quiet on a
target that resolves. If this file is deleted, the self-test fails with an extra failure
rather than a missing one — which is the correct direction for it to break in.
