# Bulk body (fixture)

This file cites `~/.claude/rules/early-cited.md` on its third line, so the citation check matches early no matter which file the directory walk hands over first.

Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
