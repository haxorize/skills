# Bulk body, third file (fixture)

Three files rather than one because a single reference file is capped at 200 lines, and the tail has to outrun a Linux pipe's fixed 64 KiB capacity, not just a macOS pipe's initial 16 KiB.

Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
