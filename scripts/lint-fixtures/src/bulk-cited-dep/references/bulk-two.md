# Bulk body, second file (fixture)

The citation is already behind the reader by the time this file is written, which is the point: everything from here on is tail the producer must still hand over after `grep -q` has gone.

Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
Filler prose whose only job is length: the citation check has to keep grading correctly when the reader stops at its first match and walks away while the producer still has writes queued behind it.
