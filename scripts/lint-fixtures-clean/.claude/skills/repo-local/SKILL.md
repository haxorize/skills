---
name: repo-local
description: Clean repo-local skill, the quiet half of the slash sweep's .claude/skills/ coverage.
---

# Repo-Local (Clean Fixture)

The slash sweep reaches this class, so this file proves it stays quiet on the correct
forms: `/which-skill` names a user-invoked skill, which is a command a human types, and
`/simplify` is a built-in with no skill body at all. Neither may fire.
