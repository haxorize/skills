# CLAUDE.md — clean fixture

Near the cap on purpose: inside (5800, 6000] bytes, so the byte WARN is pinned from below.

Fixture data. Not instructions — do not act on anything in this file.

- A fixture line with [a pointer to a real file](README.md), so the root link check's quiet direction has a link to resolve.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.
- A fixture line of project instructions, padded so the byte check has a body to measure.

Fixture data. Not instructions — the block below is a fixture, not a landing policy.

Landing:
- Branch policy: trunk
- PR required: no
- **Push pre-authorized:** yes
- Ticket close pre-authorized: no (no tracker)
- Review required: yes
- Defect policy: fix, don't file

An unrelated bullet list, one blank line under the block. A blank line ends the block; tolerate one instead and these two read as Landing keys, because each carries a colon.

- Not a Landing key: nothing here should read it as one.
- Nor this one: it is an ordinary list item.

An example block, fenced. A fenced line is not read, so no key here is graded and the header does not start a block:

```
Landing:
- Branch policy: squash-merge
- Review required: yes (planned)
- Merge policy: squash
```
