# A markdown file at depth one under src/ (clean fixture)

The quiet half of the depth-one arm: no ADR token, no over-long body, no unresolved
link, and no retired slash form. `/which-skill` names a user-invoked skill and must not
fire.
