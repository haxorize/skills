# A complete consumer (clean fixture)

Read docs/evaluation/<slug>/ledger.md before grading: never lift a `marketed` row to a fact, a `verified` row is a project fact already cited, and a `contradicted` row is disconfirming evidence the grade must account for.

This file is also what `scripts/lint-skills-selftest.sh` breaks in an isolated copy of
this root, to prove the vocabulary check moves the exit status on its own rather than
riding on some other check's failure.
