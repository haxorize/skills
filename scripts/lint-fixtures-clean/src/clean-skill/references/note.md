# Note

The file the link extractor and the `Depends:` citation check both read. `lint-selftest.sh` breaks this file's mode in a throwaway copy to reach the two read-error branches on a tree that would otherwise exit 0.
