# Note

The file the link extractor and the `Depends:` citation check both read. `lint-skills-selftest.sh` breaks this file's mode in a throwaway copy to reach the two read-error branches on a tree that would otherwise exit 0.

## 2. The numbered half

A step number is a position, not a name: this heading is cited from the body as `§ The numbered half`, without its number, and the section-pointer check must accept it — a renumbering would otherwise break every citation of the section.

## Sizes and shapes

The heading a body citation reaches only through the comma cut: the name it is cited by carries a comma, and the full name matches no heading here in either direction.
