---
name: clean-skill
description: A minimal well-formed skill whose only job is to make this fixture root lint clean. Use when a check needs a tree that exits 0 before something is broken in it deliberately, when the user says "do the thing", or says "do the thing" again — one description quoting a phrase twice is not a duplicate; it also shares the backticked span `pin the bound` with near-cap, which only double-quoted spans are compared, so that must stay quiet.
---

# Clean skill

Do the thing the check needs, then read [references/note.md](references/note.md) for the note it leans on.

This skill leans on `clean-rule` for the admission check's citation direction.
