# Health sweep — 2026-09-05

Measured-tree: ae6949f52fffeadfb2a1ba299c4140303c6edc9d
Head: b5ffca34e07d2104b9a67c53a98c414b89fa86f1

The stamp is `git rev-parse HEAD^{tree}` — the tree the sweep read, with the working tree clean when it started. It is deliberately not the working-tree one-liner `review-changes`' `tree-stamp.md` carries: this report is an untracked file being written into the tree it names, so that one-liner would fold the half-written report into its own hash and name a state existing for one instant that no reader could re-derive. `sweep-corpus` § 3 now fixes the rule this paragraph follows.

**This report was corrected on 2026-09-05 by the fix pass that closed its findings**, after a review of that pass found defects in the report itself. Corrections are named in place rather than silently applied; the checks and verdicts are otherwise as the sweep ran them. Scope note: the sweep session's own writes were confined to `docs/health/`, but the fix pass that follows lands as a split of six commits in the order `CLAUDE.md` § Commit order prescribes, carrying edits under `src/`, `global/`, `docs/adr/`, `scripts/`, `.claude/skills/`, and the two root documents. This file rides the last of them, the corpus commit its findings were raised against. `sweep-corpus/SKILL.md:10`'s "reports and never edits `src/`" binds the sweep, not the fix pass that follows it.

First run — `docs/health/` did not exist, so no open finding blocked the sweep. `open-findings.md` was created with its heading and legend, not empty; `sweep-corpus` step 1 said "create it empty on the first run" and now names what a first-run file holds, because a heading and a legend were what the skill's own step 3 needed to append to.

## Why each document was read

No prior health report exists, so the churn query has no `--since` date to run and **every router and README row was re-read** rather than the subset whose `description:` changed. `DOMAIN.md` and `global/README.md` were read whole for the same reason.

## 1. Mechanical gate

All 15 gates ran under `bash -c`; every one exited 0, so this section takes the table form `sweep-corpus` § 2 allows for a clean run — no gate produced failure text to carry verbatim. The parenthesised figures are quoted from each gate's own last line. No `no gate matched` line (all four globs matched) and no `FAIL <path> is named by this loop and is not there`.

| Gate | exit |
|---|---|
| `scripts/lint-skills.sh` | 0 |
| `scripts/lint-adrs.sh` | 0 (84 records) |
| `scripts/install-selftest.sh` | 0 |
| `scripts/lint-adrs-selftest.sh` | 0 |
| `scripts/lint-skills-selftest.sh` | 0 |
| `scripts/security-selftest.sh` | 0 (362 expectations, 57 shipped skills PASS) |
| `scripts/skill-usage-selftest.sh` | 0 |
| `scripts/wizard-template-selftest.sh` | 0 |
| `scripts/git-hooks/commit-msg-selftest.sh` | 0 |
| `scripts/git-hooks/post-merge-selftest.sh` | 0 |
| `scripts/git-hooks/pre-commit-selftest.sh` | 0 |
| `global/hooks/commit-bypass-selftest.sh` | 0 |
| `global/hooks/rename-safety-selftest.sh` | 0 |
| `global/hooks/review-receipt-selftest.sh` | 0 |
| `.claude/skills/mine-skills/scripts/enum-selftest.sh` | 0 |

Every gate returned 0, so no gate returned 2, 3, or 4. Corrected: this line previously read "No partials: no gate returned 2, 3, or 4", which makes *partial* the name for 2 and 3 and 4 together — `DOMAIN.md:386`'s **Exit-code taxonomy** alias column bans exactly that, since 3 is a usage error and 4 is a check that never ran, and neither is a partial result.

## 2. Doc-claim check

Corpus measured live: `ls -d src/*/ | wc -l` → **57**; 35 user-invoked (`disable-model-invocation: true`), 22 model-invoked.

### WITHDRAWN — README.md:222, the mutation-table-row count

> `security-selftest.sh`, which copies and edits the scanner **75 times** and writes a 10 MB archive and a 1.1 MB file

The sweep graded this **FAIL** on the ground that the file holds 74 mutation rows and pins itself at 74 (`scripts/security-selftest.sh:582`, green). **That grading was wrong and the verdict is withdrawn.** The sentence's subject is copies of the scanner, not rows of the table, and there are 75 — `:467` and `:568` are the two mutation helpers that copy it once per row, and `:598` copies it a 75th time to `lock-read.sh`. Measured by running it:

```
$ bash -x scripts/security-selftest.sh 2>/tmp/sst.err; echo "exit=$?"
exit=0
$ grep -aoF 'cp scripts/security.sh' /tmp/sst.err | wc -l
      75
```

Graded against a pin over a different noun, this was the `evidence.md` failure mode *a check parses the way its consumer parses* — committed by the check itself. The 74-row pin is real; it does not grade this sentence.

**The two secondary inaccuracies in the same clause were real and are fixed.** The selftest writes **two** 1.1 MB files (`:587` `padded.sh`, `:595` `package-lock.json`) sequentially, the first removed at `:594`, so 1.1 MB at a time on disk; and it writes **two** archives, whose members unpack to 10.5 MB (`:666` `huge.skill`) and 20 MB (`:678` `sizelie.zip`) — both `ZIP_DEFLATED` runs of a single byte, so kilobytes on disk, and an unpacked figure quoted among disk figures is the wrong unit for a paragraph disclosing what `git pull` costs your disk. The old text named one archive at the smaller of the two.

### FAIL — global/README.md:13, the dependants recipe

> The five rules (each file's `Depends:` line names the skills lint checks cite back … `recommend-and-proceed.md` names 5 while more do, **three of them overriding its bin 1 outright**. … for the dependants of a rule, run `grep -rl <filename> src/ .claude/skills/`)

Run the command the sentence hands the reader and the three do not appear:

```
$ grep -rl 'recommend-and-proceed\.md' src/ .claude/skills/
src/address-findings/SKILL.md
src/committing/SKILL.md
src/grilling/SKILL.md
src/implement/SKILL.md
src/receiving-review/SKILL.md
src/write-skill/references/skill-security-review.md
```

Six files — the 5 on the `Depends:` line plus `write-skill`'s reference, which does not override bin 1. The three overriders cite the rule by bare name, never by filename:

```
$ grep -rn 'recommend-and-proceed' src/offboard-engineer/SKILL.md src/onboard-me/SKILL.md src/rebuild-contract/SKILL.md
src/offboard-engineer/SKILL.md:21:… The global recommend-and-proceed rule puts "run it and find out" in bin 1 — **override that here** …
src/onboard-me/SKILL.md:26:… (same sentence) …
src/rebuild-contract/SKILL.md:23:… (same sentence) …
```

The substantive claim ("more do", "three of them") holds; the recipe does not reproduce it. This is the `evidence.md` failure mode "a check parses the way its consumer parses": the sentence exists precisely so the reader re-derives the census instead of trusting a number in prose, and the grep as spelled returns a census missing the three the sentence counts.

Corrected scope: the sweep named three bare-name citers and there were **five**. `src/audit-skills/SKILL.md:69` and `src/handoff/SKILL.md:54` both lean on the rule and were missed, because the check went looking for the three the sentence counted rather than for every file that cites the rule at all.

### PASS (checked, no line owed elsewhere)

- Router line 9's model-invoked roster (19 Discipline + 3 Domain) is exactly the 22 skills without `disable-model-invocation: true`. README's "Model-invoked skills" section lists the same 22; its user-invoked sections list the same 35.
- Router line 53: "Eight upkeep sweeps" — eight named, all present.
- README line **163**: "`.claude/skills/` holds two skills" — `mine-skills`, `sweep-corpus`. (Corrected: the sweep cited line 149.)
- `global/README.md:13` "`evidence.md` names 8 … while more files cite it" — `Depends:` names 8; `grep -rl 'evidence\.md' src/ .claude/skills/` → 13 files.
- `global/README.md:29` "a hook is the script whose header carries an `# Install note:` line" — `grep -ln 'Install note:' global/hooks/*.sh` returns exactly the three hooks, not the two libs or `selftest-lib.sh`.
- README line **212**'s `pre-commit` gate map matches `scripts/git-hooks/pre-commit:11-12` **in substance**; the named `scripts/git-hooks/*-selftest.sh` roster matches the three on disk. (Corrected twice: the sweep cited line 217, which is blank, and called the match "word for word" — README reads "`lint-skills.sh` for a path under `src/`, `global/`, `.claude/skills/`, or `scripts/`" against the hook's "when a staged path is under src/, global/, .claude/skills/ or scripts/". Substantively equal, verbally not.)
- `DOMAIN.md:325` names three lint functions (`check_evaluation_ledger_authority`, `_rule_agreement`, `_consumers`) — all three defined and called in `scripts/lint-skills.sh` (1846, 1884, 1921; called 2110–2112).
- `DOMAIN.md:395` "the five defined byte-identically in the three `references/evidence-tags.md` files" — identical md5 `86fd39e4…` across `offboard-engineer`, `rebuild-contract`, `onboard-me`.
- `DOMAIN.md:80` "ADR-0053's 2026-08-31 amendment now carries all five" — `docs/adr/0053-global-rules-layer.md:43` carries the amendment and the five counts.

No UNSUPPORTED or UNGROUNDED candidates surfaced: every claim checked resolved to a live artifact.

## 3. Cross-reference check

**Reference resolution — PASS.** Every `references/`, `scripts/`, `docs/`, and `global/` path named across the 242 scoped documents resolves, including the cross-skill prose pointers (`address-findings` → `../review-changes/references/finding-discipline.md`, `evidence.md` → `~/.claude/skills/review-changes/references/tree-stamp.md`, `DOMAIN.md` → the three `evidence-tags.md` siblings, and 30 more). The `docs/product-description/*` paths in `product-description` are paths in the *consuming* repo, not this one, and `mine-skills`' `scripts/enum.sh` resolves to `.claude/skills/mine-skills/scripts/enum.sh`. Positive control: the same extractor found `references/tree-stamp.md` in `src/review-changes/SKILL.md` (3 hits).

The 242-document scope is the one `sweep-corpus` § 2's cross-reference bullet fixes, re-derived here so the controls below can be:

```
$ git ls-tree -r --name-only HEAD | grep -E '^(src/[^/]+/SKILL\.md|src/[^/]+/references/.*\.md|\.claude/skills/[^/]+/SKILL\.md|DOMAIN\.md|README\.md|CLAUDE\.md|global/README\.md|global/rules/.*\.md)$' | wc -l
     242
```

**Alias misuse — one FAIL.** Scope run: the 32 alias cells `DOMAIN.md` marks as a former, retired, rejected, deprecated, or upstream name, where a hit is **terminology drift** by construction, plus the "bare" cells the sweep names. (Corrected: the sweep wrote "any hit is drift by construction" unqualified, which § Flagged ambiguities forbids of *drift* — the sweep's own output violating a rule the sweep enforces.) The "32 aliases" figure was a hand count of those cells. A marker sweep re-derives 33 rows / 34 alias names (`awk -F'|' '/^\| \*\*/ {print $4}' DOMAIN.md | grep -icE 'retired|rejected|former|deprecated|upstream'`), and hand-adjudicated the class is 36 names — so 32 was the zero-hit subset, not the class total, and the arithmetic below is stated against 36.

Positive control on the same pipeline, at the swept tree:

```
$ git grep -cIi 'handover' HEAD -- $(git ls-tree -r --name-only HEAD | grep -E '^(src/[^/]+/SKILL\.md|src/[^/]+/references/.*\.md|\.claude/skills/[^/]+/SKILL\.md|DOMAIN\.md|README\.md|CLAUDE\.md|global/README\.md|global/rules/.*\.md)$') | awk -F: '{s+=$NF} END{print s}'
11
```

Eleven lines across 8 files, so an absent term would have been found. **Corrected: the sweep reported 9 and wrote no command.** A control whose own number is wrong is not a control, which is why the command is now beside it.

- 32 of the 36 retired names: zero hits — the four with hits are below. (Class enumerated by hand from the alias column; see the caveat at the head of this section.)
- `Irreversible action` — one hit, `src/wizard/SKILL.md:23`, "the irreversible actions between them" describing cutover steps. Plain English, not a name for an **Outward act**. Not a violation.
- `Display name`, `Handover`, `VSTS` — hits are the ADO field display name, a `SKILL.md` H1's display name, `docs/handover/`'s named-as-retired path, the quoted phrase "Please write a handover doc" the skill exists to reject, and `Microsoft.VSTS.*` field reference names. Each is the carved-out sense. Not violations.
- `` the `X` behavior `` (bare, as a skill kind): zero hits. Positive control on the parallel form `` the `X` discipline ``, re-derived at the swept tree — `git grep -cI 'the `[a-z/-]*` discipline' HEAD -- $(git ls-tree -r --name-only HEAD | grep -E '^(src/[^/]+/SKILL\.md|src/[^/]+/references/.*\.md|\.claude/skills/[^/]+/SKILL\.md|DOMAIN\.md|README\.md|CLAUDE\.md|global/README\.md|global/rules/.*\.md)$')` summed → **24**, so the grep shape lands. (The review of this fix pass put this figure at 22 and was wrong; 24 is what the report said and what re-derives.)
- Bare `Unverified`, `the five families`, bare `Healthy`: zero hits.
- Bare `smell`: hits are "smell catalog", "smell baseline", "corpus smell" and anaphora inside `smell-baseline.md` itself. Not violations.
- Bare `the map`: hits are the **Story map** inside `to-feature`/`to-story` files that name it first, `derived-documents.md`'s source map named a line above, and two metaphors. Not violations.

### FAIL — bare "ledger" in `.claude/skills/mine-skills/SKILL.md`

(Corrected: this heading read `### FINDING`, an ALL-CAPS marker registered in neither `DOMAIN.md:393` nor `sweep-corpus`, sitting in the same `###` slot as `FAIL` and `PASS` so it read as a family member. The cross-reference arm now declares `PASS`/`FAIL` as its only two verdicts.)

`DOMAIN.md` § Flagged ambiguities: *Bare **"ledger"** is the most overloaded word in this glossary … the enumeration is a sample, not a closed set, so a sense missing from it is not evidence the word was unambiguous. Always qualify which one is meant.* The **Evaluation ledger** row's alias column carries `bare "ledger"` into the normative column.

`mine-skills` uses the bare word for the mining-round ledger, a sense the enumeration did not carry, at **`:3`, `:9`, `:31`, `:39`** — four sites. `:39` carries a second occurrence, "re-recorded in this round's ledger", which is qualified and is not one. Corrected: the sweep listed `:3`, `:15`, `:31`, `:39` — `:15` is `` `ledger.md` ``, a filename rather than the bare word, and `:9` is a bare use the sweep missed. `:19`'s two uses are each qualified ("the memory ledger `skill-mining-sources`", "ADR-0034's amendment ledger") and are not instances.

`DOMAIN.md` grants an anaphora carve-out to `chart-course` (bare "ticket") and `onboard-me` (bare "map") and writes each one down; none existed for `mine-skills`. The choice — write the carve-out, or qualify the uses — is a judgment the sweep does not settle. It was settled by the user on 2026-09-05 and the resolution is recorded against H-003 in `open-findings.md`.

## 4. Router honesty — PASS

Every one of the 57 `src/` directories is named in both `src/which-skill/SKILL.md` (or a `which-skill/references/*.md`) and `README.md`; zero misses either way. Blurbs were read against the descriptions extracted live from each `SKILL.md` frontmatter — no blurb contradicts its skill's description. `.claude/skills/`'s two skills are named in README's "Repo-local skills" section and are correctly absent from the router, per that section's own statement.

## Scope this sweep did not cover

The alias check ran the 32 retired/rejected-name cells and the named bare-form cells. The remainder of `DOMAIN.md`'s alias column is common English words whose cells qualify them ("Task", "Update", "Comment", "Status", "Clean") and which a blanket grep cannot separate from legitimate use.

**The "551 distinct candidates" figure is struck.** No command was written down and the prose named a method instead; four spellings of that method give 564, 581, 595 and 662 at the swept tree, so the sentence named a spread rather than a count. The sweep wrote "**551** distinct candidates (`awk` over the table's fourth column, parentheticals stripped, deduped)"; run as one plausible spelling of that method it gives 564, and the review of this fix pass got 565 from another. Three readings of one sentence is the finding. Lifting the marker means writing the command down and re-running it; the figure is not load-bearing for any check, and the deferral it describes — one judged search per remaining cell, taken from the cell's own wording — stands whatever the count is.

That deferral is registered in `~/code/lib/_rounds/2026-09-04/reconcile.md` § Deferrals register, so it survives this file.
