# The 2026-09-04 round's batches 6 and 7 admit material from ten outside sources, all of it as local prose

## Context

Batches 6 and 7 of the 2026-09-04 round landed on 2026-09-04 as two commits — `c7470b2` (6) and `d72bbf3` (7) — folding rows from `~/code/lib/_rounds/2026-09-04/ledger.md` into two new references (`src/work-item-shape/references/afk-scoping.md`, `src/ship/references/pr-body.md`) and clauses across `src/work-item-shape/SKILL.md`, its `done-conditions.md`, the five byte-identical `work-item-tags.md` copies, `src/to-tasks/references/task-body.md`, `src/ship/SKILL.md`, `src/ship/references/pr-path.md`, `src/implement/SKILL.md`, `src/writing-for-agents/SKILL.md`, and `DOMAIN.md` — 16 paths (`git diff --name-only c7470b2~1 d72bbf3 | wc -l` → 16). Batch 6 is grill decision 8; batch 7 carries grill decisions 7 and 13's `D1.9`, which the reconcile had assigned to no batch and to batch 4k respectively, and which the batch-6 close found unlanded.

This record is written before the family's review, inside the same unpushed range, as ADR-0082 and ADR-0083 required of their families: the admissions are recorded beside the commits they describe, and the review reads the record with the diff.

## Decision

The admissions of batches 6 and 7 are recorded here, one bullet per outside source; the round's closing ADR points here rather than restating them. Every source fed local prose — no directory was ported, and nothing here enters `docs/lineage.md`'s first table — so **no swept point is recorded and the diff-before-editing trigger does not fire**. Clone paths are under `~/code/lib/`; each tip is `git rev-parse --short HEAD` read this session with its commit date.

**Batch 6, `c7470b2`.**

- **addyosmani/agent-skills** (MIT; clone `addyosmani-agent-skills`, tip `1c760d6` of 2026-09-04) — ledger `H09` (`skills/observability-and-instrumentation/SKILL.md`, on disk since 2026-06-09 and never rowed before this round), and the 2026-08-29 park `X1.6` promoted as `P-X1.6`. Taken for `H09`: the criterion that says how production will show the change working — the on-call's question and the one signal that answers it. The Discipline the row proposed (`observable-code`) was refused by grill decision 8; the criterion is the fold that lands regardless, and the register names it as the measure for that Discipline. Taken for `P-X1.6`: a numeric threshold carries the value measured today beside it — a ratchet, not an aspiration; a bar the codebase fails at filing is a plan step. Local: `src/work-item-shape/SKILL.md` § Acceptance criteria, two clauses. Local wording.
- **swyxio/skills** (MIT; clone `swyxio-skills`, tip `79df950` of 2026-09-03) — ledger `H10` (`observability-hardening/SKILL.md`), riding `H09`'s criterion as its counterweight; the 2026-08-27 veto `A46.D2` lifted on the host change. Taken: no signal without a named consumer and the decision it drives, and an audit may find the existing signals suffice. Local: the same clause.
- **rampstackco/claude-skills** (MIT; clone `rampstackco-claude-skills`, tip `a67dd34` of 2026-08-28) — ledger `H06` (`skills/feature-flagging/SKILL.md`) and `H12` (`skills/monitoring-and-alerting/SKILL.md`). Taken for `H06`: a flag is born with a type, an owner, and the item that removes it, or it has no end of life; "a flag with no removal item" joins the scope-reduction vocabulary. The `<type>_<owner>_<name>_<date>` naming convention was not taken (a project's, not the suite's); the age-and-use sweep is `delete-dead-code`'s from batch 4c (`H07`). Taken for `H12`: no threshold without a named baseline, one clause inside `H09`'s criterion; the four-layer taxonomy was not taken. Local: `src/work-item-shape/SKILL.md` § The goal and § Sizing (`H06`), § Acceptance criteria (`H12`).
- **gnurio/nurijanian-skills** (MIT; clone `gnurio-nurijanian-skills`, tip `43a0566` of 2026-07-14) — the 2026-08-29 parks `P10` and `P12` promoted as `P-P10` and `P-P12`, both on Team-fit with their unpark conditions UNVERIFIABLE from this machine (no tracker). Taken for `P-P10`: at a parent tier, a detail whose answer comes from decomposition is a decision the children settle, not an Ambiguity block; the test is whether a human could answer it now, at this tier. Taken for `P-P12`, a fragment of a leaf/branch/trunk classification whose whole-repo report stays out: a hidden trunk — code many callers depend on, or global behavior registered with no importer to name — denies AFK however clear the item; the readiness gate is now five predicates, re-counted in `DOMAIN.md`'s AFK and Readiness-gate rows and `to-tasks/references/task-body.md`. `P12`'s source credits Erik Schluntz's *Vibe coding in prod* for the trunk idea. Local: `src/work-item-shape/SKILL.md` § Surfacing ambiguity and § Readiness.
- **cursor/plugins, the `pstack` directory** (MIT, `pstack/LICENSE`, Lauren Tan; clone `cursor-plugins`, tip `93b00b8` of 2026-09-04) — ledger `V13` (`skills/poteto-mode/playbooks/shipping.md`). Taken: a threshold relative to trunk holds only where trunk runs the same scenario; where it cannot, an absolute budget for the added work and the user-visible end state, and the item says which. Local: `src/work-item-shape/references/done-conditions.md`, the Performance row.
- **Local, no source** — ledger `H-G15`, a Half-1 gap fold: a `debt` tag so the tracker can be filtered for it, in the five `work-item-tags.md` sibling copies. Recorded here so the row's home is named; nothing to attribute.

Two relocations left `work-item-shape`'s body at 14,987 B: the `:70` stop-condition paragraph the grill named, and the `:72` reversibility paragraph added on bytes when the five clauses drafted at ~1,700 B against a 900-B estimate — both now `references/afk-scoping.md` behind one pointer. Local prose moved, nothing admitted.

**Batch 7, `d72bbf3`.**

- **kunchenguid/no-mistakes** (MIT; clone `kunchenguid-no-mistakes`, tip `911452b` of 2026-09-04) — ledger `K9` (`VISION.md`). Taken: the PR body is written for a reviewer who was not there, in four parts — what changed, what was checked and how, the risks, what had to be fixed along the way. The proxy and its pipeline were rejected by class. Local: `src/ship/references/pr-body.md` § Four parts.
- **Joannis/claude-skills** (no license file in the clone, NONE-STATED; clone `Joannis-claude-skills`, tip `0d0e412` of 2026-09-03) — ledger `D4.16` (`pull-request/SKILL.md`), **ideas only**. Taken: a durable-boundary delta gets its own section — before and after, its class, affected consumers — and an empty "no boundary changes" line is a claim nobody checked. The source's four compatibility classes were not taken; the section classes against `codebase-design`'s published-interfaces table (ADR-0082, `H14`) so the suite holds one classification. Local: `pr-body.md` § The boundary delta.
- **github/awesome-copilot** (MIT; clone `github-awesome-copilot`, tip `7b1ebe6` of 2026-09-04) — ledger `G12` (`skills/pr-screenshots`). Taken: a change to what renders carries a before and an after capture at one viewport and crop, the before taken before the first edit. The `<details>` markup and the upload mechanics were not taken (a project's convention skill's). Local: `pr-body.md` § Before and after; `src/implement/SKILL.md` step 3's capture clause.
- **coldteadotai/pr-lens** (MIT; clone `coldteadotai-pr-lens`, tip `d3a72e5` of 2026-09-03) — ledger `C19`, generalizing the 2026-08-29 `X4.9` from removals to any change. Taken: the unchanged neighbors a change leans on are named, because blast radius reads off them. Local: `pr-body.md` § What did not change, pointing at `delete-dead-code` § Report for a removal.
- **posthog/ai-plugin** (no license file in the clone, NONE-STATED; clone `posthog-ai-plugin`, tip `19e4737` of 2026-09-03; already a second-table upstream through ADR-0083's `hard-cases.md` row) — ledger `D1.9` (`writing-simplified-technical-english`), **ideas only**: the wording is the miner's, and the ASD-STE100 pack itself was vetoed in earlier rounds (`B21`, `B22`, `H3`, `H5`). Taken: one idea per sentence, one reading per word, within one document. Local: `src/writing-for-agents/SKILL.md` § Writing style, one bullet — and, per grill decision 13, seven of that file's own sentences split at their joined clauses with no rule changed.

## Considered Options

- **Leave the record to the family's review fix pass**, as ADR-0081 through ADR-0083 did. Rejected: each of those records opened by naming the interval its commits sat unrecorded; writing this one before the review closes that interval at zero and gives the review a record to read against the diff.
- **One record per batch.** Rejected as before: addyosmani/agent-skills feeds batch 6 through two rows, and posthog/ai-plugin sits in batch 7 beside its ADR-0083 row; the source is the unit that matches the lineage table.
- **Take Joannis's four compatibility classes as written.** Rejected: `H14`'s table landed in batch 4e as the suite's one classification, and a second vocabulary for the same distinction in the PR body would be the two live answers `writing-for-agents:38` forbids.

## Consequences

- `docs/lineage.md`'s second table gains rows whose `Record` cell is this record, and its header sentence names this record beside ADR-0082 and ADR-0083: `work-item-shape` now holds a first-table row (ADR-0044) and second-table rows here.
- The round's closing ADR points here for batches 6 and 7.
- The ledger's four PR-body rows (`K9`, `D4.16`, `G12`, `C19`) read ADAPT-landed as of this record; their unpark condition, "`ship` gains a PR-body reference", was met by `d72bbf3`.
- `work-item-shape`'s body sits 13 B under its cap; the next fold into it relocates first, which the register already says.
