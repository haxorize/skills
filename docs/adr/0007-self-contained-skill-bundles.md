# Self-contained skill bundles with duplicated format references

> **Amended by [ADR-0016](0016-behavior-skills-declared-deps-relaxed-atomicity.md):** the portable unit is now "skill + declared deps" — a skill installs with the model-invoked disciplines its `requires:` line names — while the format-doc duplication this record decided is unchanged.

## Context

Several skills reference shared format docs — `domain-format.md` is needed by both `grill-and-record` (writing DOMAIN.md inline during grilling) and `harden-domain` (writing DOMAIN.md from a sweep); `adr-format.md` is needed by `grill-and-record`, `backfill-adrs`, and the standalone `adr` skill. The same need recurred for the **naming-drift queue**: its definition (lifecycle, storage, entry format) is shared by the four `to-X` publishing skills (`to-bug`, `to-feature`, `to-story`, `to-tasks`). It recurred again for **tracker resolution**: the three-mode flow (Declared / Bootstrap-on-ask / No-repo CLI-only) plus required-fields rule is byte-identical across those same four skills. (`from-work-item` resolves a tracker too, but as a reader — different wording, no publish, no title prefix — so it carries its own inline version and is not part of the group.) Skills are symlinked individually into `~/.claude/skills/` from this repo; users routinely install one or two skills without cloning the whole tree.

## Decision

Each skill's `references/` folder is self-contained. Format docs are duplicated across the skills that need them — `domain-format.md` exists in `grill-and-record/references/` and `harden-domain/references/`; `adr-format.md` exists in `grill-and-record/references/` and `backfill-adrs/references/` (the standalone `adr` skill carries the format inline in its SKILL.md); `naming-drift-queue.md` and `tracker-resolution.md` each exist in `to-bug/`, `to-feature/`, `to-story/`, and `to-tasks/` `references/`. There is no shared source. Drift is caught by `scripts/lint-skills.sh`, which fails if any sibling group's copies are not byte-identical — turning the original "grep and update in lockstep" discipline into a mechanism.

## Considered Options

- **Shared `references/` at the repo root** — rejected. Breaks symlink-install portability; symlinking just `src/grill-and-record/` into `~/.claude/skills/` doesn't carry the format doc.
- **Symlinks within the repo** (e.g., `harden-domain/references/domain-format.md → ../grill-and-record/references/domain-format.md`) — rejected. Symlink chains across the install symlinks are fragile, editors handle them inconsistently, and the second hop breaks if a user installs the second skill but not the first.
- **Generated copies via a build step** — rejected. Adds tooling to a repo whose entire premise is "skills are markdown, no build."
- **Auto-discovered sibling groups in the linter** (group every `references/*.md` by basename, fail any multi-member group that isn't byte-identical, with a denylist for coincidental name clashes) instead of the hand-maintained `sibling_groups` allowlist — rejected. It would convert a silent failure (forgot to register an extracted copy) into a loud one, but only for the rare cross-author case where someone copies a doc into a new skill without knowing the linter has groups; for the person doing an extraction, registration is a same-commit step. It would not have caught the case that motivated it (`improve-design`'s tracker-resolution drift was *inline prose*, not an extracted reference file, so basename grouping never sees it). Against that thin benefit: the explicit array is a readable declaration of what is guarded and why, and the group list is short and changes rarely. Keep the allowlist.

## Consequences

- Each skill is a portable atomic unit — symlink one directory, get everything it needs.
- Drift is caught mechanically: `scripts/lint-skills.sh` asserts every sibling group is byte-identical, so a fix that misses a copy fails lint rather than silently diverging. (The original consequence — "editorial discipline, not a mechanism" — was superseded once the linter landed; see Amendments.)
- Format docs stay short and stable by design — high-churn templates wouldn't survive this trade-off, so the duplication tax stays bounded.
- Thirteen copies in total today (two of `domain-format.md`, two of `adr-format.md`, four of `naming-drift-queue.md`, four of `tracker-resolution.md`, plus the inline ADR format in `adr/SKILL.md`). Adding a new skill that writes to a shared format adds a copy and a `sibling_groups` entry in the linter. — corrected: see Amendments 2026-06-29

## Amendments

- **2026-06-19** — Extended the pattern to `naming-drift-queue.md` (four copies across the `to-X` skills), consolidating a definition that had drifted into four divergent inline phrasings. Reconciled the Context/Decision/Consequences with `scripts/lint-skills.sh`, which now enforces byte-identity for all sibling groups; this enforcement is what makes a four-copy group safe to maintain.
- **2026-06-19** — Extended the pattern to `tracker-resolution.md` (four copies across the `to-X` skills). Unlike the queue, the inline copies had *not* drifted — they were already byte-identical — but the duplication was unprotected, since the linter can only diff whole reference files, not inline `SKILL.md` sections. Extraction is what brings the block under the byte-identity check. The per-skill title-prefix line stays inline (it genuinely varies); `from-work-item`'s reader-side resolution stays inline (it genuinely differs).
- **2026-06-20 (see ADR-0016)** — The portable atomic unit relaxes from "a lone skill" to **"a skill plus its declared behavior dependencies."** ADR-0016 makes reusable *behaviors* (the grilling loop, domain-modeling, the deep-module vocabulary, the mechanical finalization pass) into their own model-invoked skills that orchestrators reach via prose invocation; an orchestrator names the behaviors it needs in a frontmatter `requires:` line, and `scripts/install.sh` resolves and links those deps when it links the orchestrator. This does **not** change the format-doc duplication above — inert format/data docs (`domain-format.md`, `adr-format.md`, `tracker-resolution.md`, `naming-drift-queue.md`) stay sibling reference files, because there is nothing to "invoke." Two re-pairings followed. `adr-format.md` gains a third copy in `adr/references/` (the inline copy in `adr/SKILL.md` is pulled into a real reference file so the linter can guard it). `domain-format.md` moves from `{grill-and-record, harden-domain}` to `{grill-and-record, domain-modeling}`: `harden-domain` now delegates to the `domain-modeling` behavior and drops its own copy, while `grill-and-record` keeps an inline copy to protect grill rhythm (the same reason it already declines to delegate to `adr`).
- **2026-06-29** — Correction to the 2026-06-20 amendment above: `domain-format.md` is **single-source in `domain-modeling`**, not a sibling group. Both `grill-and-record` and `harden-domain` delegate domain capture to the `domain-modeling` behavior (each `requires:` it), so neither carries a `domain-format.md` copy — which is why `scripts/lint-skills.sh` registers no `domain-format` group. The earlier amendment's claim that `grill-and-record` keeps an inline `domain-format` copy was wrong: `grill-and-record`'s only inline override is the ADR *write* (it declines to delegate to the standalone `adr` skill, to protect grill rhythm) — it does not inline the domain format. The guarded groups today are `adr-format.md` (3), `naming-drift-queue.md` (4), `tracker-resolution.md` (5), and `finding-discipline.md` (2); the "thirteen copies" tally on line 23 is the original-authoring snapshot and predates these.
- **2026-06-20** — Added `improve-design` to the `tracker-resolution.md` sibling group (fifth copy). `improve-design` is a tracker *writer* (it searches, creates, updates, and comments on refactor work items), so it runs the same three-mode writer-side flow as the four `to-X` publishers — unlike `from-work-item`, which stays carved out because it resolves a tracker as a *reader* (no publish, no title prefix). The skill (authored 2026-06-20 in PR4, one day after this group was extracted) had instead carried a lossy inline paraphrase of the three modes — incidental scaffolding, not a deliberate variant — which had already silently dropped the "save to memory keyed by tracker context" guidance. Extraction brings it under the byte-identity check. As with the `to-X` skills, the genuinely-varying parts stay inline: `improve-design`'s `Hierarchy.` paragraph (refactor work belongs under a parent Feature) is its analogue of the per-skill title-prefix line.
- **2026-07-06** — Correction to the 2026-06-20 amendment above: `improve-design` no longer creates or updates work items and no longer carries a `Hierarchy.` paragraph or its own work-item template — filing and body rewrites now go through `/to-story`, which owns the single story template, tracker dispatch, and hierarchy handling. `improve-design` stays in the `tracker-resolution.md` sibling group because it still runs the three-mode resolution for its remaining writer-side dispatch: the step-1 search and step-7 comments.
- **2026-08-08** — Extended the pattern to `work-item-tags.md` (five copies: the `to-X` skills plus `chart-course`, which joined later the same day per ADR-0039's amendment): the ADO `System.Tags` derivation introduced by ADR-0039, registered as a seventh `sibling_groups` entry the same commit it shipped. The guarded groups today: `adr-format.md` (3), `naming-drift-queue.md` (4), `tracker-resolution.md` (6), `finding-discipline.md` (2), `cold-reader-pass.md` (4), `github-sub-issues.md` (4), `work-item-tags.md` (5).
- **2026-08-08** — The `cold-reader-pass.md` group (4) dissolves: the pass is a genuine *procedure* (spawn a fresh-context subagent, fold gaps back), not an inert format doc, so per the ADR-0016 boundary it belongs in a behavior, not a sibling group. It now lives whole in the `work-item-shape` behavior (ADR-0044), which all four `to-X` publishers declare via `requires:`; their SKILL.md steps cite the pass by prose invocation instead of a file link. The guarded groups today: `adr-format.md` (3), `naming-drift-queue.md` (4), `tracker-resolution.md` (6), `finding-discipline.md` (2), `github-sub-issues.md` (4), `work-item-tags.md` (5).
- **2026-08-09 (see [ADR-0048](0048-naming-drift-queue-trimmed-to-check-only.md))** — The `naming-drift-queue.md` group (4) dissolves for a different reason than `cold-reader-pass.md`: not a boundary reclassification but a removal. A usage audit found the durable queue was never written on either machine while the publish-time naming-drift check fired constantly, so ADR-0048 keeps the check and deletes the queue — the 4 reference copies, the read/write steps, and the linter's sibling-group entry go together. The guarded groups today: `adr-format.md` (3), `tracker-resolution.md` (6), `finding-discipline.md` (2), `github-sub-issues.md` (4), `work-item-tags.md` (5).
- **2026-08-22** — Three re-pairings from Batch 4a. (1) `tracker-resolution.md` gains three reader-side hosts — `from-ticket`, `ship`, `backfill-adrs` — dissolving the `from-work-item` carve-out above: the readers carried lossy inline paraphrases of the same three modes, and the genuinely differing part (a reader resolves in Declared mode only and never bootstraps) is one inline sentence per host, not a different document. (2) The create-side procedure the builder had appended to that file — Publish gate, label precheck, blocked-write shape, transport safety — splits out as `publishing.md`, carried only by the five skills that create (`to-*`, `chart-course`), so readers stop carrying procedure they never run; the split keeps the ADR-0016 boundary honest without promoting a one-host-kind procedure to a behavior. (3) A new `subagent-brief.md` group carries the rules a fan-out prompt must quote, in the six skills that author a brief (`review-changes`, `improve-design`, `chart-course`, `handoff`, `work-item-shape`, `adoption-verdict`). The guarded groups today: `adr-format.md` (3), `tracker-resolution.md` (9), `publishing.md` (5), `subagent-brief.md` (6), `finding-discipline.md` (2), `github-sub-issues.md` (4), `work-item-tags.md` (5), `completion-audit.md` (3).

- **2026-08-27** — `subagent-brief.md` gains a seventh host, `product-description`, whose document fan-out authors a brief ([ADR-0065](0065-product-description-model-invoked-not-onboard-me-phase-0.md)); the `sibling_groups` entry in `scripts/lint-skills.sh` is updated and all seven copies are byte-identical. The guarded groups today: `adr-format.md` (3), `tracker-resolution.md` (9), `publishing.md` (5), `subagent-brief.md` (7), `finding-discipline.md` (2), `github-sub-issues.md` (4), `work-item-tags.md` (5), `completion-audit.md` (3). That host's brief carries one caller-stated override — its subagents write documents rather than return findings — which is stated in the calling skill, never in the shared file, so byte-identity holds.

- **2026-08-31** — Four groups reach the tally that had gone unrecorded, two of them added by the tightening round's C-batches. `ado-html-transport.md` (5) carries the shared ADO transport procedure — Markdown → HTML conversion, shell safety on the create call, the once-per-project field-shape check — for every skill that runs `az boards work-item create` — the four `to-*` publishers plus `chart-course`, which the fix pass added on review, having found it running the create call with no conversion command anywhere in its bundle; it is one-host-kind publisher procedure, the precedent the 2026-08-22 amendment set for `publishing.md`, not a behavior. `ac-ids.md` (2) carries the append-only AC-ID convention for `to-feature` and `to-story`, the two tiers that own an AC field. Both are registered in `sibling_groups` and byte-identical. Recorded at the same time, having drifted in before this round: `evidence-tags.md` (3) and `tree-stamp.md` (2). The guarded groups today: `evidence-tags.md` (3), `adr-format.md` (3), `tracker-resolution.md` (9), `publishing.md` (5), `subagent-brief.md` (7), `finding-discipline.md` (2), `tree-stamp.md` (2), `github-sub-issues.md` (4), `work-item-tags.md` (5), `completion-audit.md` (3), `ado-html-transport.md` (5), `ac-ids.md` (2) — twelve.
