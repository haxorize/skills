# Mine unmerged branches only for direct lineage or dormant main

The 2026-07-19 branch-coverage audit of the mined repos under `~/code/lib` found sweep coverage complete on every main tip but untested on unmerged branches — where real material was hiding (wayfinder's research-inline redesign, adopted in part into `chart-course`). Standing policy for future sweeps: read a repo's unmerged branches only when (a) the repo is the direct upstream of a ported skill (wayfinder → `chart-course`, ce-pov → `adoption-verdict`), or (b) main is dormant and branches carry the only delta (how superpowers' `tdd-writing-good-tests` material was found); otherwise sweeps are main-only. Exhaustive branch mining was rejected as disproportionate — unreleased WIP hasn't survived its own author's merge bar, and compound-engineering-plugin alone carries 499 unmerged branches — and pure main-only was rejected because it demonstrably missed the wayfinder material.

## Consequences

CLAUDE.md carries the revisit trigger: materially editing a ported skill starts with an upstream diff — main and branches — since the last-swept point. As of this ADR: wayfinder at main `9603c1c` (branches audited 2026-07-19), ce-pov at main `4927d7a1` (superseded; the newest dated entry under Amendments is the live ledger). — corrected: see Amendments 2026-08-30 (the trigger now resolves through `docs/lineage.md`)

## Amendments

**2026-07-20** — `chart-course` edited under [ADR-0035](0035-ticket-names-the-assignable-tier.md) (terminology only: its tickets defined as the Charting sub-type of the new **Ticket** term). Upstream diff waived: wayfinder was swept the day before and the edit carries no behavioral delta.

**2026-07-20** — The CLAUDE.md revisit trigger now enumerates the full ported-skill lineage rather than the two examples this ADR named: seventeen skills from mattpocock/skills (main `9603c1c`, branches audited 2026-07-19 — wayfinder's repo), `adoption-verdict` and `capturing-learnings` from compound-engineering-plugin (main `4927d7a1`), `receiving-review` from obra/superpowers (main `d884ae0`, dormant since 2026-07-02; unmerged branches swept 2026-07-19), and `diverging` + `verify-docs` from oaustegard/claude-skills (main `7dea9c8`; 14 unmerged branches triaged into standing reject classes 2026-07-19). The policy itself is unchanged — those two were always examples, not the scope.

**2026-08-09** — Swept points refreshed; the 2026-07-20 ledger above is superseded by this one, which covers all five upstreams. The points are the tips of the mined checkouts under `~/code/lib` at the 2026-08-07/08 rounds:

- mattpocock/skills at `84fdeff` (2026-08-06; the 2026-08-07 spot-check found a flatten refactor only, no content changes)
- compound-engineering-plugin at `0a295785` (2026-08-06)
- obra/superpowers at `44c9b2d` (2026-07-27)
- oaustegard/claude-skills at `61a85b8` (2026-08-08) — now also the upstream of `audit-tests`, ported from `gating`'s audit half in the 2026-08-08 round
- openclaw/agent-skills at `2a409d3` (2026-08-02) — upstream of `black-box-check`, and absent from every earlier entry in this ledger

**2026-08-22** — Swept points refreshed at the 2026-08-21 round (ledger Part B; every SHA is the tip of the checkout under `~/code/lib` and resolves with `git cat-file -t`); this ledger supersedes the 2026-08-09 one:

- mattpocock/skills at `5b15a47` (2026-08-21; 34 commits since `84fdeff`, nine unmerged branches read, nothing portable) — `grill-and-record` is now a one-window stub of `grill-me`, so the grill-with-docs lineage ends there
- compound-engineering-plugin at `66ccf57` (2026-08-20; 118 commits, branches carried nothing beyond main)
- obra/superpowers at `b36e082` (2026-08-12; one commit, still dormant)
- oaustegard/claude-skills at `66ec85b` (2026-08-21; 37 commits, the three ported skills byte-unchanged)
- openclaw/agent-skills at `128a4ea` (2026-08-21; 25 commits) — upstream of `validate-behavior`, the skill this ledger's 2026-08-09 entry knew as `black-box-check`
- dmmulroy/dotfiles at `a7beb72` (2026-08-20; 12 commits) — upstream of `discoverable-code`, absent from every earlier entry; modem-dev/skills is an md5-identical mirror of its `write-discoverable-code` and is never diffed again
- jakubkrehel/skills at `6c43b20` (2026-08-20; range `d01493b..6c43b20`, 41 commits) — not a lineage upstream; recorded because the `after` SHA the round's plan wrote down did not resolve, and this is the `main` tip actually mined

**2026-08-22, correction** — Two lines above, read as written at `e913c39`: `grill-and-record` is a deprecation stub of `grill-me` ([ADR-0058](0058-grill-me-absorbs-grill-and-record.md)) whose revisit trigger is waived for the stub's window, so "the lineage ends there" means the port, not the upstream; and dmmulroy/dotfiles' twelve commits are counted since `aae3dd3`, the tip at the 2026-08-14 port.

**2026-08-26** — The stub's window closed and `src/grill-and-record/` is deleted ([ADR-0058](0058-grill-me-absorbs-grill-and-record.md)'s amendment of this date), so the correction above has outlived its subject and the waiver it granted is spent. The grill-with-docs lineage attaches to `grill-me` from here: `grill-me` diffs both upstream directories, and the mattpocock/skills line's "the lineage ends there" no longer names a live skill. The swept point is unchanged at `5b15a47` — this reassigns ownership of a directory, not the range.

**2026-08-27** — Swept points refreshed at the 2026-08-27 round (ledger Part B; copied verbatim from `~/code/lib/_rounds/2026-08-27/mine-2026-08-27-delta-lineage-a.md` § Swept points and `~/code/lib/_rounds/2026-08-27/mine-2026-08-27-delta-ce.md` § Swept point); this ledger supersedes the 2026-08-22 one:

- mattpocock/skills at `6654f6b` (2026-08-24; 3 commits since `5b15a47`, all the `retro` stub graduating from `stub/retro-skill` into `skills/in-progress/`; 101 remote branches, `git log --since=2026-08-21` empty on every unmerged branch except the bot `changeset-release/main`; `setup-skill-and-vague-prose` — 50 unmerged, tip `70653e1` of 2026-04-28, absent from the 2026-08-21 nine — predates every local port and was not read; no ported directory changed)
- obra/superpowers at `b36e082` (unchanged since 2026-08-12; 112 branches fetched at depth 20, none with a commit since 2026-08-21)
- oaustegard/claude-skills at `04bfd5b` (2026-08-25; 22 commits; `generative-thinking` and `verifying-claims` byte-unchanged, `gating` +26 lines — description and a "When NOT" table, nothing folded; the new `declaring-invariants` is the source of the two ADAPT rows into `tdd` and `audit-tests`)
- openclaw/agent-skills at `52b1a0a` (2026-08-26; one test-only commit in `autoreview`; `behavior-validator` unchanged)
- dmmulroy/dotfiles at `a6d5117` (2026-08-24; 2 commits; `write-discoverable-code` unchanged; the ten changed skills are Pocock forks re-punctuated, nothing folded)
- compound-engineering-plugin at `5985d82` (2026-08-26; 34 commits since `66ccf57`; **the ported `ce-pov` directory did change in range — its hunks were read and consciously REJECTED in the round's delta report, so the next editor of `adoption-verdict` diffs from `5985d82` knowing the intervening change was seen, not missed**; the `learnings` loop unchanged; branches: read — 10 post-08-20 branches touching ce-pov / compound / ce-skill-work checked, all pre-squash forms of commits inside the range; nothing beyond main)

Not re-swept, and carried forward from the superseded ledger rather than dropped: jakubkrehel/skills at `6c43b20` (2026-08-20) — not a lineage upstream, recorded because the `after` SHA the 2026-08-21 plan wrote down did not resolve and this is the `main` tip actually mined. It is not in the six above because it has no ported directory to diff; the SHA is kept so the next miner does not repeat the unresolved-SHA hunt.

Also for the lineage record: `modem-dev-skills` unchanged at `edcdedb`, byte-identical to dmmulroy's `write-discoverable-code` (A22.1 "record as mirror; never diff again" stands).

**2026-08-29** — A lineage entry for a script rather than a skill: `scripts/security.sh` gained eight rule classes in the 2026-08-27 round's batch 9 (hex blob, drop site, download-then-exec, minified line, `.npmrc`, install-time manifest hook, compiled binary, remote instructions), rewritten from the pattern list in the sweep skill of **openhonest/honest-skills** (`~/code/lib/openhonest-honest-skills`, tip `2397865`, 2026-08-25, Apache-2.0 — the scanner's header said MIT until the 2026-08-29 review's fix pass; the ledger of that round had it right). The swept point is recorded here and the source named in `CLAUDE.md` § *Ported-skill upstreams* so the diff-before-editing trigger can fire on the rules; nothing was ported as prose, so no skill body carries the lineage. — corrected: see the 2026-09-04 correction entry below (the upstream is `zcaceres/skills`, not openhonest/honest-skills)

**2026-08-29** — `nitfolio-nirvajna-skills` enters this ledger, which it had never been in. It has fed three skills since 2026-08-27 — `onboard-me` ([ADR-0064](0064-onboard-me-read-only-kt-session.md)), `rebuild-contract` ([ADR-0069](0069-rebuild-contract-pre-rewrite-contract-in-repo.md)), and `offboard-me` ([ADR-0070](0070-offboard-me-handover-in-repo-human-as-evidence.md)) — with its swept point recorded only in `CLAUDE.md` prose and in those records' *Sources* sections, so a sweep enumerating this ledger omitted it silently. Swept at **`c842141`** (**MIT**, `~/code/lib/_rounds/2026-08-27/licences.tsv:52`; the 2026-08-27 batch plan's NONE-STATED attribution was wrong and binds the `steveruizok` gists instead, as [ADR-0065](0065-product-description-model-invoked-not-onboard-me-phase-0.md) records). Cloned at `~/code/lib/nitfolio-nirvajna-skills`. Branches were not audited at the sweep; the next editor of any of the three diffs from `c842141` on main and audits branches then. `evaluation-ledger` ([ADR-0071](0071-evaluation-ledger-multi-week-evaluation-in-repo.md)) is genuinely local and takes no trigger.

**2026-08-29** — Two more script-lineage entries, for the seven rules `scripts/security.sh` gained in the 2026-08-29 round's batch 1 — `sh-imds`, `sh-agentcfg`, `sh-persist`, `sh-noverify`, `sh-envdump`, `uni-confusable`, `bin-bytecode` (metadata endpoint, agent config, persistence, TLS off, environment dump, homoglyph, bytecode) — rewritten from the pattern classes of **nvidia/skillspector** (`~/code/lib/nvidia-skillspector`, tip `1b87593`, 2026-08-28, Apache-2.0) and **DataDog/guarddog** (`~/code/lib/DataDog-guarddog`, tip `12d036e`, 2026-08-19, Apache-2.0); `~/code/lib/_rounds/2026-08-29/licences.tsv:27,30` records both licenses, and each clone's `LICENSE` is the Apache 2.0 text. The homoglyph table is the scanner's own, not either source's ([ADR-0075](0075-transcript-sourced-mining-round.md)'s amendments say why). Both are named in `CLAUDE.md` § *Ported-skill upstreams* so the diff-before-editing trigger fires on those rules; until this entry the pair was named only in the scanner's header comment, the same silent omission the `nitfolio-nirvajna-skills` entry above records. This entry records no branch audit; the next editor of those rules diffs each repo's pattern set from these tips and audits branches then.

- **2026-08-30** — The diff-before-editing trigger and the local-name → upstream-name mapping leave `CLAUDE.md` § *Ported-skill upstreams*, which no longer exists, for `docs/lineage.md` ([ADR-0076](0076-claude-md-admits-triggers-and-contracts-only.md)); `CLAUDE.md` keeps one line naming the table and fires it on every skill edit. This ledger stays the swept-point authority, joined to the table by the upstream name, and the two 2026-08-29 entries above that say a source is "named in `CLAUDE.md`" now read as named in the table. The swept points are unchanged.

**2026-08-30** — Swept points refreshed at the 2026-08-30 tightening round's phase 0 ([ADR-0077](0077-tightening-round-one-house-style.md) § Scope and order; the sweep report is `~/code/lib/_rounds/2026-08-30/upstream-sweep.md`), which diffs every upstream before the round's batches edit the ported bodies. Each row below supersedes that upstream's earlier entry, per the per-upstream rule `DOMAIN.md`'s **Swept point** row and `docs/lineage.md` both state; the two 2026-08-29 script entries are folded in. The 2026-08-27 entry's two non-table records are not superseded and carry forward unchanged: jakubkrehel/skills at `6c43b20` and `modem-dev-skills` at `edcdedb`, neither of which has a ported directory to diff. Ten clones cover the table's rows, each re-fetched this date; **nothing was folded, because no ported material moved anywhere upstream**:

- **oaustegard/claude-skills** at `fc22960` (12 commits since `04bfd5b`, all `atprotoing` / `browsing-bluesky` / `declauding`; the 3 ported directories byte-unchanged on main — `git diff --name-status 04bfd5b..origin/main -- '*generative-thinking*' '*verifying-claims*' '*gating*'` empty; 4 new refs, all tags. Ten branches hit those globs and every hit is an **ancestor state**: `gating/SKILL.md` on 9 declauding-family branches is the swept blob minus 24 lines, lines that entered main at `4fcc13f` of 2026-08-25 against branch tips of 2026-08-16 — the same "+26 lines" the 2026-08-27 sweep read and declined — and `creating-video-skill`, tip 2026-07-18, is behind the plugins restructure)
- **openclaw/agent-skills** at `6cd3366` (13 commits since `52b1a0a` in `agent-transcript`, `autoreview`, `beam`, `crabbox`, CI; `behavior-validator` untouched on main **and on every remote branch**, checked by a loop running `git diff --name-only 52b1a0a..<ref> -- '*behavior-validator*'` over all refs, which printed nothing for any)
- **compound-engineering-plugin** at `0e758b60` (8 commits, 99 files since `5985d82`, most of them one `skills/guides/ → docs/guides/` move; `ce-pov` **moved without changing** — `R100`, and `git diff --stat -M -- '*ce-pov*'` reports 0 insertions and 0 deletions. Its blob is `6bf420c` at the swept point, on main, and on every remote branch. The learnings loop behind `capturing-learnings` is unchanged; what moved under `docs/solutions/` is two learning instances, not the convention. `docs/lineage.md`'s row names the upstream skill without a path, so it needs no edit; the new path is recorded here so the next diff does not hunt for `skills/guides/`)
- **Dormant, re-affirmed at their existing points** — each re-fetched 2026-08-30 with 0 commits in range and 0 new refs: mattpocock/skills `6654f6b`, obra/superpowers `b36e082`, dmmulroy/dotfiles `a6d5117`, nitfolio-nirvajna-skills `c842141`, openhonest/honest-skills `2397865`, nvidia/skillspector `1b87593`, DataDog/guarddog `12d036e` (default branch `v3`)

**The nitfolio branch audit owed by the 2026-08-29 entry is discharged.** That entry said branches were not audited and the next editor of the three ported bodies would audit them; this round's Batch B renamed one of them to `offboard-engineer` and edited `onboard-me` and `rebuild-contract`, so the trigger fired and the audit ran here (clusters C7 and C8 edit them further and inherit nothing). The clone has exactly two remote refs — `origin` and `origin/main`, both dated 2026-08-11 — and `git rev-list --count --all ^c842141` returns 0. There is no unmerged branch to audit, and the swept point stands at `c842141`.

- **2026-09-04, correction** — The 2026-08-29 entry above that gives `scripts/security.sh`'s eight rule classes to **openhonest/honest-skills** is wrong about the upstream. The eight (hex blob, drop site, download-then-exec, minified line, `.npmrc`, install-time manifest hook, compiled binary, remote instructions) were read from **zcaceres/skills** `skills/investigate-repo/SKILL.md` (`~/code/lib/_rounds/2026-08-27/mine-2026-08-27-new-zcaceres.md:46,58`, row N3a.30, "MIT regexes"; ledger `M2`), and honest-skills at `2397865` holds none of that vocabulary — `git grep -iE 'postinstall|pastebin|minif|\.npmrc' 2397865` returns nothing, and `git grep -ciE 'curl|chmod|base64|webhook|ngrok|pastebin' 2397865` returns one file, `.gitignore`. The misattribution entered at the 2026-08-27 round's batch-10 fix pass, the same pass that changed the header's license from MIT to Apache-2.0: the ledger's MIT was zcaceres's, and it was right. The Apache-2.0 attribution to honest-skills is not owed for these rules; nothing else in this repo derives from that repository (`git grep` at `2397865` found no other candidate text), so honest-skills leaves `docs/lineage.md` and its 2026-08-30 "dormant, re-affirmed" line above no longer names a lineage upstream. ADR-0073's source table and § Lineage carry the same correction, dated. Found by the 2026-09-04 lineage sweep (`~/code/lib/_rounds/2026-09-04/reports/mine-2026-09-04-delta-lineage.md`, `L-rec.1`).

- **2026-09-04** — Swept points refreshed at the 2026-09-04 mining round's lineage sweep (the `L` report above; the table is copied verbatim from `~/code/lib/_rounds/2026-09-04/ledger.md` Appendix A, which copied it from the report's § Swept points, and every `new` SHA resolved with `git -C ~/code/lib/<dir> cat-file -t` → `commit` in the miner's session). Each row supersedes that upstream's 2026-08-30 entry; the `old` column is that entry's point. **zcaceres/skills enters this ledger for the first time**, as the actual upstream of the eight `security.sh` rules the correction above reassigns — its `old` is the 2026-08-27 tip the rules were read at, and its branches were not audited this round. addyosmani/agent-skills is attribution only (`docs/lineage.md`'s second table) and takes no diff trigger. **Nothing was folded from any ported path**: the one ported directory that changed, compound-engineering-plugin's `ce-pov`, changed in its references and scripts with `SKILL.md` itself byte-unchanged, and the `L` report read those hunks. The 2026-08-27 entry's two non-table records still carry forward unchanged: jakubkrehel/skills at `6c43b20` and `modem-dev-skills` at `edcdedb`.

| upstream | old (swept) | new (tip) | commits | ported paths changed | unmerged-branch check |
|---|---|---|---|---|---|
| mattpocock/skills (`mattpocock-skills`) | `6654f6b` | `3cca18b368ae95cdbdebbff572ccafa662551015` (2026-09-04) | 2 | none of the 20 ported directories (`--name-status` empty) | 102 refs; `changeset-release/main` (2026-09-04) empty against the tip under `skills/`; the 59 non-ancestor refs are all dated 2026-07-01..07-13 and were read by the 08-22 and 08-27 sweeps; UNVERIFIABLE on the shallow clone whether a July ref hides a commit — `git fetch --unshallow` lifts it |
| compound-engineering-plugin (`everyinc-compound-engineering-plugin`) | `0e758b60` | `415181d3c56a6ef63415bbe9e328c9a26cad04b8` (2026-09-03) | 29 | `ce-pov`: `references/cross-model-panel.md`, `scripts/cross-model-pov.sh`, `scripts/peer-job-runner.py`, `docs/guides/ce-pov.md`; **`skills/ce-pov/SKILL.md` unchanged** (blob `23d2456e`). The learnings loop: `ce-compound/SKILL.md` (durable bar), 3 references, `ce-compound-refresh/references/worth-audit.md` (new), 90 `docs/solutions/*` instances (27 deleted) | 762 refs; 46 dated ≥ 2026-08-24 blob-checked on the ported globs: 419 ancestor-state, 36 absent-on-branch, 4 UNIQUE on `feat/ce-packs-v0` (2026-08-26; read, L1/L9); 716 older refs not re-checked (covered to their dates by the 08-27 and 08-30 entries) |
| oaustegard/claude-skills (`oaustegard-claude-skills`) | `fc22960` | `7af23bed2c09a2f540ffc1b66650492d42fb1d12` (2026-09-04) | 12 | none (`generative-thinking`, `verifying-claims`, `gating` byte-unchanged on main) | 13 refs; 10 differ on the ported globs, every one an ancestor state by content — `claude/skill-quality-fixes-from-agent-skills-paper` (2026-08-25) is `gating` 0.2.0 without the tip's 26-line "When NOT to use" block; the nine declauding-family refs and `creating-video-skill` as the 08-30 entry classified them |
| openclaw/agent-skills (`openclaw-agent-skills`) | `6cd3366` | `3f392d7531673127ebfa9ed87148e64c26c8153f` (2026-09-03) | 13 | none (`behavior-validator` untouched) | 31 refs; `git diff --name-only 6cd3366..<ref> -- '*behavior-validator*'` printed nothing for any |
| dmmulroy/dotfiles (`dmmulroy-dotfiles`) | `a6d5117` | `fd84f529229f3ed41f7e72e784164da5fd1d6a41` (2026-09-02) | 4 | none (`write-discoverable-code` unchanged) | 2 refs (`origin`, `origin/main`), both at the tip |
| nvidia/skillspector (`nvidia-skillspector`) | `1b87593` | `7805bb94843d91cb9937f57264ca52642164499b` (2026-08-31) | 23 | pattern classes: `static_patterns_prompt_injection.py` (+35/−15), `static_patterns_tool_misuse.py` (+1925/−32), new `security_reconstruction.py` (1543); `security.sh` lacks the concealment normalization (L22) and the destructive-`rm` parser (L23) — corrected later on 2026-09-04: the concealment normalization landed as `inj-obfuscated` at `22743cb` in this round's batch 2; the destructive-`rm` parser (L23) is the gap still open | 2 refs, both at the tip |
| DataDog/guarddog (`DataDog-guarddog`, default `v3`) | `12d036e` | `1f4a66c064fb2087c224750507f3b65c7633deeb` (2026-09-02) | 2 | none (one test-fixture `package.json` version bump) | 2 refs, both at the tip |
| openhonest/honest-skills (`openhonest-honest-skills`) | `2397865` | `113ce82d2fb767214e09d12a7105d7243d78b3aa` (2026-08-30) | 23 | none attributable — the eight `security.sh` rules the lineage row names are not in this repository at either end (L-rec.1); 5 skill bodies changed, 1 new (`writing`), 3 new hooks/tools | 2 refs, both at the tip |
| addyosmani/agent-skills (`addyosmani-agent-skills`; attribution only) | `d2c37ef` | `1c760d643497e9da289300e5eb2f5aca861503f7` (2026-09-04) | 13 | none (`doubt-driven-development/SKILL.md` unchanged) | 16 refs; 10 hold an older blob of that file, all ancestor-state by the blob-set check |
| zcaceres/skills (`zcaceres-skills`; **the actual upstream of `security.sh`'s eight rules**, not in `swept-points.txt`) | `b7213839` (08-27 tip) | `f6ac657774f42c73382e2f95dcba299d1bc95122` (2026-09-01) | 2 | none (`skills/investigate-repo` unchanged: `diff --stat` empty) | not audited this round (the repo is in `delta-grp` for another miner); ADR-0034 gains its first entry for this upstream when L-rec.1 lands — corrected later on 2026-09-04: L-rec.1 is the correction entry above, so this row is that entry |

One license note the round record owes (`~/code/lib/_rounds/2026-09-04/ledger.md` row `REC-2`), recorded here because this is the round's swept-point record: `Dicklesworthstone-eidetic_engine_cli`, read this round and not a lineage upstream, is MIT **with an OpenAI/Anthropic Rider** naming Anthropic, PBC a Restricted Party — ideas only for this reader, and `~/code/lib/_rounds/2026-09-04/licences.tsv` says so; the round's `briefing.md` `§ Licences` ideas-only list does not name it, and this line is the record that does.
