# The 2026-09-12 round's batch 4 admits material from three outside sources into the three capped bodies, all of it as local prose

## Context

Batch 4 of the 2026-09-12 round landed on 2026-09-13 as one commit, `57d482a`, folding the rows `~/code/lib/_rounds/2026-09-12/reconcile.md` § Batch 4 held for the three bodies at the 15,000 B cap — `writing-for-humans`, `work-item-shape`, `which-skill` — 6 paths (`git show --stat --format= 57d482a | tail -1` → `6 files changed, 34 insertions(+), 8 deletions(-)`). Batch 0 (`6a56210`) had relocated one block out of each body to make the room; the reconcile's § 4 said the folds size to the measured room, not the estimate. Measured before the folds (`wc -c`, 2026-09-13): 14,643 · 14,777 · 14,555 — headroom 357 / 223 / 445 B. The rows for `which-skill` from Batches 5, 7, and 8 (the `aeo`, `audit-aeo`, `discovery`, `retro` entries) land with those batches, not here. This record is written before the batch family's review, inside the same unpushed range, as [ADR-0084](0084-batch-family-6-7-admissions-2026-09-04.md) required and [ADR-0086](0086-batch-3-admissions-2026-09-12.md) followed; like ADR-0086 it is one record per batch, the unit this round's reconcile fixes, so `gnurio/pm-os` is a bullet in both.

## Decision

The admissions of batch 4 are recorded here, one bullet per outside source; the round's closing ADR points here rather than restating them. Every source fed local prose — no directory was ported — so **no diff-before-editing obligation attaches to any source here**. The trigger fired on one first-table host the commit edited, `which-skill` (mattpocock/skills), and was discharged by [ADR-0034](0034-branch-mining-lineage-or-dormant-main.md)'s 2026-09-13 sweep: zero commits at mattpocock since the swept point. `writing-for-humans` and `work-item-shape` are second-table only.

- **gnurio/pm-os** (no license, NONE-STATED, a sold product; clone `gnurio-pm-os`, tip `3aeba89` of 2026-09-12) — ledger `PM-B8` (`status-update`), `PM-B9` (`requirements-reconcile`), `PM-A10` (`bug-triage`), `PM-B12` (`p0-p1-p2`, the one ADAPT residue of a REJECT-covered cluster), all **ideas only**. Taken for `PM-B8`: a status line whose color matches reality, then wins, next, risks, and asks, each ask with an owner and a by-when, the status and any ask in the first three lines; the audience sizing, the voice guides, and the save path were not taken. Local: `src/writing-for-humans/references/register-by-artifact.md` § Weekly status note, and the body's row now points there (its "every next step dated or marked undated" clause moved into the reference's **next** section). Taken for `PM-B9`: a Conflicting block names the goal behind each side's wording before anything is asked — compatible goals dissolve the block, a real conflict is decided on user evidence, then strategy, then reversibility, never averaged; the five-step unified list was not taken. Local: `src/work-item-shape/SKILL.md` § Surfacing ambiguity, one sentence. Taken for `PM-A10`: the two questions — more important than the item in progress, than the next planned one — each answered with its reasoning first, two noes defer, either yes escalates to classification and never to an interrupt. Taken for `PM-B12`: a claimed must-have is tested by which core task becomes impossible without it, and a demotion is recorded with claimant, evidence, workaround, and the evidence that would reverse it; the P1/P2 split and capacity check were not taken. Local for both: `src/work-item-shape/references/sizing.md` § The three tests, the second and third.
- **riekelt-technical-writer** (MIT, `LICENSE` in `licences.tsv`; clone `riekelt-technical-writer`, tip `42fa11b` of 2026-09-11) — ledger `CR-4.31` (`writing-runbooks`), **unparked**: the reader parked it on the cap alone and Batch 0 lifted the cap. Taken: a runbook is written from a real run, with an untaken step or branch marked draft and a named first runner; the skeleton — scope and non-scope, happy path then numbered single-action steps, paste-ready commands, a risk legend applied per command, ordering consequences on the step, a rollback per state-changing step or a plain "none", a closing verify section; symptom-first troubleshooting entries with the error text verbatim and the cheapest diagnostic first; a migration guide's mapping table, dated deprecation, and completion condition. Not taken: the operator-facing-strings section (the Error message row's), the in-block command variants, the "(recommended)" marker, the tooling-shrink note. Local: `register-by-artifact.md` § Runbook, release or migration procedure, and a new body row whose typist is the engineer owning the procedure, per change — admitted under the row rule's named-typist clause, with the typist a role this repo's teams hold rather than a sighting count; the round's reviewer grades that admission.
- **riekelt-principal-engineer** (no license, NONE-STATED per `licences.tsv`, no `license` key in `package.json`; clone `riekelt-principal-engineer`, tip `e67b7af` of 2026-09-11) — ledger `CR-5.21` (`scoping-changes`), **ideas only**. Taken: an item is sized to its trigger — the defect or need, never the sentence about it — so the same defect on a second path is in scope and an improvement found because the file was open is not; the borderline addition ships on its own merits or not at all; the owner can add the second item and cannot merge the two. Not taken: decompose-never-descope (already § Sizing's "deliver fully or propose a split" and the scope-reduction vocabulary), drive-to-completion (the harness's own standing instruction), scope-in-review (`receiving-review`'s and `review-changes`' already), the unrequested-structure and ceiling-comment bullets (`codebase-design`'s ground). Local: `src/work-item-shape/SKILL.md` § Sizing, one sentence, and `references/sizing.md` § The three tests, the first.
- **Local, no source** — grill decision 5 (`CR-5.15`, CONV-C): the operating-safely disambiguation line in `src/which-skill/SKILL.md`'s rules-layer paragraph. The grill named the three homes as the harness reminder and `committing` for a destructive op, `phi-safe-code` and `security.sh` for secrets, `committing` for the shared tree. The line as landed names `committing`'s rotate-first paragraph and `scripts/security.sh` for secrets and **not `phi-safe-code`**: `grep -n -i 'credential\|token\|api key\|secret' src/phi-safe-code/SKILL.md` finds only the surrogate-key bullet — that skill holds no secrets-hygiene text, and a router line pointing at a home that does not say it is the defect `which-skill` exists to prevent. `CR-5.15` closes on the line; the flip to a global rule waits for the transcript the grill named.

**Not landed.** `PM-A9` (gnurio/pm-os `gherkin-stories`): the reader's residue was one sentence naming Given/When/Then as an optional rendering, "worth a single sentence only if the repo's projects actually use Gherkin-style BDD"; no project under this suite does by any evidence in the round, so the row reads REJECT-covered from this record. `CR-5.40`: the reconcile's Batch 4 line still lists it, but its verdict cell is REJECT (re-bucketed in Batch 1, `ledger.md:671`) — the changelog range rule it re-derives landed 2026-09-04.

**Two relocations, one new reference.** `work-item-shape`'s checklist-items paragraph (the Cold-reader pass's second paragraph) moved whole to a new `references/checklist-items.md` behind a one-line pointer — the first attempt appended it to `references/subagent-brief.md`, which `lint-skills.sh` refused because that file is byte-identical with `review-changes`' copy under ADR-0007. § Sizing's scope-reduction paragraph was condensed where `sizing.md` already repeats its second sentence verbatim. Local prose moved, nothing admitted. `writing-for-humans` needed no relocation beyond Batch 0's: the two rows point at the reference for their fat parts.

## Considered Options

- **Land `PM-A10` and `PM-B12` in the body's § Sizing, as the ledger proposed.** Rejected on bytes and on shape: three tests at a paragraph each are the reference's kind of content, and the body keeps the one-clause naming of each with the open condition.
- **Name `phi-safe-code` as the secrets home, as the grill's wording did.** Rejected by the read above; the ADR records the divergence so the reviewer can overrule it.
- **Land `PM-A9` on the strength of the grill's "fold the ADAPT residue" deferral row.** Rejected: the deferral row said the residue is folded by Batches 3–4, and the reader's residue carried its own precondition, unmet.

## Consequences

- `docs/lineage.md`'s second table gains rows whose `Record` cell is this record, and its header sentence names this record beside ADR-0082, ADR-0083, ADR-0084, and ADR-0086 (ADR-0085 is not an admissions record and is correctly absent).
- The round's closing ADR points here for batch 4.
- The three bodies stand at 14,985 · 14,991 · 14,866 B (`wc -c`, after the commit's last edit to `which-skill`); Batches 5, 7, and 8 each add a `which-skill` line and have 134 B between them before that body relocates again.
- `PM-A3`'s residue (the resync-before-critique ordering rule, host `codebase-design`) is the one row the reconcile's "Batches 3–4 fold the ADAPT residue" deferral still leaves unfolded; it is not a capped-body row and goes back to the reconcile.

## Amendments

- **2026-09-13 — the review of batches 0–4 and its fix pass.** The review (`skills-2026-09-13-unpushed-batches-0-4.review.md` in the landing zone) reversed one relocation this record narrates: `references/checklist-items.md` is deleted and the Cold-reader paragraph is back inline, condensed, in `work-item-shape` (review F57 — a near-always open condition keeps its content inline). The same pass condensed five register rows in `writing-for-humans` and four lines in `which-skill` to land the fixes under the cap, then pruned those three bodies and `diagnosing-bugs` under `writing-for-agents`' Deletion grounds (review F55) and re-inlined the why-chain rule in `diagnosing-bugs` Phase 6 (review F56); the four stand at 14,554 · 14,218 · 14,401 · 14,789 B (`wc -c`), so the 134 B figure above is superseded. One departure from grill decision 5 this record did not name: `which-skill`'s "three homes" line now spells the three cases (a destructive command, a shared working tree, a leaked secret) rather than folding the shared tree into a relative clause (review F62).
