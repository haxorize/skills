# A tightening round brings every skill under one house style, one size cap, and one intake gate

Status: accepted (2026-08-30)

The suite reached 57 skills through 6 mining rounds that ported from 8 upstreams, and it reads like it: 37 bodies address "you", 16 address "the agent", 4 use the bare imperative; 20 bodies have a `## Workflow`, 6 use numbered step headings, 31 have neither; skills are named 4 different ways (453 backticked names, 215 slash forms, 41 `Call the Skill tool` clauses, 11 "the X skill"); 180 British spellings sit beside American ones; labels come in 3 families that nobody registered; 9 bodies and 2 references exceed the 15,000-byte re-attach bound that `write-skill` states and `scripts/lint-skills.sh` only warns on; and the 5 always-loaded rules total 15,863 bytes, of which about 2,000 are rationale lines arguing why each rule is not a hook. Every figure was measured on 2026-08-30 against tree `a5de2f9` plus the open round's working changes.

We decided to run one **tightening round** — a mining-shaped round pointed inward — that settles the house style once, cuts what does not earn its load, merges or splits where seams blur, and then moves every mechanical convention into lint and every judgment convention into `write-skill`'s review checklist, so later mining rounds land ideas into a structure that pushes back. This record holds the grill's 31 answers, given by Nick on 2026-08-30; the plan and ledger live in `~/code/lib/_rounds/2026-08-30/`.

The record clears the ADR gate on all three criteria. **Hard to reverse:** the American-spelling flip makes every later upstream diff of a British-sourced port noisier for good, the lint FAILs bind every later fold, and a merge or rename cannot be undone without a deprecation stub and a window. **Surprising without context:** a reader who finds British spelling corrected inside a byte-diffed port, a lint that fails on a heading's case, or a deleted rule sentence that reads sensible will ask why. **A real trade-off:** each decision below names the alternative that lost, and a rational team could have picked most of them.

This amends [ADR-0073](0073-team-fit-mining-round.md) (its 15,000-byte WARN becomes a FAIL and extends to reference files), [ADR-0053](0053-global-rules-layer.md) (the rules layer gains a directory budget and an admission test), [ADR-0076](0076-claude-md-admits-triggers-and-contracts-only.md) (its "before it knows to look" test now governs `global/rules/` too), and [ADR-0057](0057-rename-table-and-discipline-skill-term.md) (the rename table takes any rename this round makes).

## Scope and order

The round has 5 subsystems and a fixed order. **Phase 0** lands the open mining round first (batches 10–20, 61 uncommitted paths on `a5de2f9`, one review cycle owed) and diffs all 30 ported skills against their upstreams from the swept points in ADR-0034, re-recording the swept point there, so every later edit starts from a clean `HEAD` and a known upstream. **Phase 1** is the finder: the `review-changes` lenses run over the whole repo, not a diff, and write the round's ledger. The batch families then run in this order: **B shape** (merge, split, retire, rename) → **C+D prune and relocate**, per skill cluster, one batch per cluster → **A mechanical** (spelling, case, naming, artifact names, sizes), last, locked by the new lint checks in the same batch. Shape runs before prune so nothing is polished and then folded; mechanical runs last so the lint that locks it never has to be re-run by hand. The alternative — spelling first, so every later diff is clean — lost because it makes every later `git blame` and upstream diff noisier than the flip already does.

In scope: `src/`, `.claude/skills/` (the 2 repo-local skills load in every mining session, and a convention with a carve-out reads as optional), `global/rules/`, `DOMAIN.md`, `README.md`, `docs/lineage.md`. ADR bodies get the spelling flip and nothing else; a record is amended, never rewritten. `global/README.md` and `docs/pitch.md` get spelling and casing only — they load nowhere — amended: see Amendments 2026-08-31 (Batch C9). Descriptions are frozen except for spelling and the 2 under-reaches the 2026-08-30 trigger test registered (`phi-safe-code`, `health-literacy`); any other description edit re-runs that skill's trigger test in the same batch, because a description is the one part of a model-invoked skill with a measured behavior.

## Sizes

The re-attach cut is 5,000 **tokens**, which is 15,000 bytes at the 3 bytes per token measured on this repo's largest bodies; a body under the cap is never cut, and the 25,000-token shared budget drops the oldest skill whole. So there is no "first N bytes" rule. The enforceable form is `write-skill`'s existing one: the gate and the hard stops sit on the first screen of every body, checked per body by the instruction-file lens. `check_reattach_bytes` in `scripts/lint-skills.sh` becomes a FAIL and extends to `references/*.md`, once every file is under the cap; a cap that warns drifts (`tell-catalog.md` grew 1,191 bytes in the last fold with nothing watching). No numeric target sits on `src/` totals — a percentage is a number to hit, and the cheapest way to hit it is to cut the rationalization rebuttals this record forbids cutting — but the before and after totals are reported under Consequences.

Every relocation from a body to a reference carries, on its ledger row, the condition under which the reference is opened ("only when the repo has a tracker", "only on the update path"). A relocation whose condition is "always" is refused: that content stays inline or is cut, never disclosed, because a required-at-load reference costs what the inline text cost plus the pointer.

## Pruning

Two tiers. A deletion on a **mechanical ground** from `writing-for-agents`' list — duplicate, cache of the environment, scaffolding a lint already enforces, persuasion detritus, unobserved pitfall — lands on inspection. A deletion on the **no-op ground alone** keeps `DOMAIN.md`'s bar: review nominates, and only a **line-control run** confirms — the pressure scenario re-run on the body minus that line, which is `DOMAIN.md`'s **No-op rule** and not the micro-test's own control, since that one drops the whole skill and cannot answer the question. Batched at the round's end so it gates nothing else. The alternative of relaxing the bar for the round lost to the prune limit — a rebuttal to a named rationalization measurably degrades compliance when cut, and that is exactly the sentence an aggressive pass reads as fluff. — amended: see Amendments 2026-09-01

## Shape

The overlap lens nominates merges and splits with evidence; Nick decides per nomination in the shape batch. A merge is admitted on **trigger competition** — a realistic prompt would load both skills, and neither is a discipline with its own second consumer — never on shared prose, which is a duplication finding. A merge that would land over 15,000 bytes is a split in disguise and is refused on that ground alone. No rename is forced on a name that merely breaks the convention (`adr`, `wizard`, `handoff`, `ship`, `tdd`); a merge is the one moment a new name is free, and the naming lens may propose a better name for any skill, recorded in ADR-0057's table if taken. The convention for new names: a discipline skill is a gerund, a user-invoked skill is an imperative verb phrase, a domain skill is the domain noun.

## House style

- **Voice:** second-person imperative. "You" is allowed; "the agent", "Claude", and "the model" are never the addressee, because the reader is the agent and third person reads as description. A body may say "the agent" for a subagent it briefs. Finding, not lint.
- **Skill references:** a user-invoked skill is written `/name`; a model-invoked skill is `` `name` ``; an actual invocation is always the `` Call the Skill tool with `name` `` clause; "the X skill" is never used. Lint gains the mirror of its slash-on-model-invoked check: a bare backticked name of a user-invoked skill fails. — amended: see Amendments 2026-08-30
- **Headings:** H1 is the skill's display name in title case; H2s are sentence case. Lint.
- **Skeleton by kind:** a user-invoked or orchestrator skill runs `[gate]` → `## Workflow` (numbered steps) → `## Notes`; a discipline or domain skill runs principle sections headed by their leading word and closes on `## Boundary` (what this skill is not, and which sibling owns it). `## References` leaves `write-skill`'s template in Batch A — 0 bodies use it as a real section today, and links sit where the branch fires. Finding, not lint: a lint that knows a discipline from an orchestrator encodes the invocation axis twice. — amended: see Amendments 2026-08-30
- **Labels, 3 families, registered once in `DOMAIN.md` and reused:** a marker a grep, hook, or script matches is ALL-CAPS (`PASS`, `FAIL`, `WARN`, `UNVERIFIED:`, `UNVERIFIABLE`, `Measured-tree:`); a verdict on a human scale is a title-case word (`Keep`, `Retire`, `Adopt`); an inline evidence tag is lowercase in brackets (`[fact]`, `[inference]`). No skill coins a fourth family or a synonym for `FAIL`. — amended: see Amendments 2026-08-30
- **Filenames a skill writes:** lowercase kebab, shortened where the name carries dead weight — `teach-me`'s workspace set becomes `mission.md`, `progress.md`, `resources.md`, `notes.md` (from `MISSION.md`, `PROGRESS.md`, `RESOURCES.md`, `NOTES.md`), and `onboard-me`'s `KT-MAP.md` becomes `kt.md`. Both spellings are written here on purpose: no skill reads an old name, so a workspace already on disk under one is not migrated, and this line is where a search for the old name has to land. The platform names (`README.md`, `CLAUDE.md`, `AGENTS.md`, `SKILL.md`) and `DOMAIN.md` stay; `DOMAIN.md` earned its case by being the one file every skill looks for by name. No read-old-name window: the one workspace on disk (`~/learning/aeo-geo`, clean) is renamed in place with `git mv`.
- **Landing-zone artifacts:** one shape, `<repo>-<date>-<slug>.<kind>.md`, owned by `handoff`'s "Where to write it"; the strays (`audit-skills-<date>.md`, `design-review-<timestamp>.html`, `questionnaire-<slug>.md`) are renamed. In-repo artifacts stay where their own records put them.
- **Spelling:** American, everywhere in scope, with a word-list check in `lint-skills.sh` so it cannot come back. The cost — noisier diffs against British-sourced upstreams — is accepted.

## The always-loaded rules

`global/rules/` gets a directory budget of 8,000 bytes, enforced as a FAIL in `lint-skills.sh` on the `check_claude_md_bytes` precedent, and the admission test ADR-0076 gave `CLAUDE.md`: a sentence stays in a rule only if a session needs it before it knows to look. The first pass keeps `no-unasked-commits`, `recommend-and-proceed`, and `evidence.md`'s invariant plus the bullets no skill owns; `large-write-chunking` moves into `write-skill` and `handoff`, and the dash pipeline into `writing-for-humans`' reference, each leaving a one-line invariant behind or nothing. Every `Why not a hook or lint:` line moves to an ADR — about 2,000 bytes loaded every session to justify a decision to a reader who is not there. The mechanism bullets in `evidence.md` (counts, recall, `Measured-tree:`) move into the skills whose acts they describe (`committing`, `review-changes`, `handoff`), which each rule's `Depends:` line already names. — amended: see Amendments 2026-08-31 (Batch C9)

## Must not become

Three drifts are forbidden by name, because the round's own pressure invites each. A prune must not delete the rebuttal rows of a rationalization table because they read as persuasion; the prune limit above is the reason. A spelling or casing pass must not rewrite `DOMAIN.md` definition prose in passing; a definition changes only through `domain-modeling`, with the entry replaced whole. A relocation must not produce a reference the body then requires at load; the per-row condition above is the check. A fourth drift — ported bodies rewritten so far that they no longer diff meaningfully against upstream — is a cost the spelling decision already accepts, not a forbiddance. — amended: see Amendments 2026-08-30

## The intake gate for later rounds

Drift enters at the moment an idea is written into whichever body was open, so the round ends by moving the conventions to the 3 places a fold cannot miss. Mechanical conventions — spelling, heading case, reference form, size, artifact-name shape, the rules budget — live in `scripts/lint-skills.sh`, which the pre-commit hook runs on touched paths. Judgment conventions — voice, skeleton by kind, label families, the relocation condition — live as rows in `write-skill`'s § Review checklist, the single home, and `CLAUDE.md`'s `## Review lenses` block points the instruction-file lens at that checklist, so every skill-change review reports conformance row by row as it reports the pruning test now. A separate `docs/style.md` lost as a second place to drift from; `writing-for-agents` lost because it is repo-agnostic prose discipline and these are skill-structure conventions, which `write-skill` already owns and lint already cites. Every `mine-skills` ledger row gains a mandatory `lands as` cell naming the existing skill and section that takes the idea, or the Extraction and Team-fit tests it passed to become a new skill; a row with no home is `PARK` until placed.

## Considered Options

- **One `.review.md` per lens in the landing zone, then `/address-findings`** — rejected: a whole-repo review is hundreds of findings with no batching; the round mechanism already has the ledger, the batch discipline, and the deferrals register.
- **Honor the micro-test bar for every deletion** — rejected as the sole path: most cuts fall under the mechanical grounds and need no experiment; the two-tier rule keeps the experiment where the prune limit bites.
- **Rename every outlier now** (`adr`, `wizard`, `handoff`, `ship`, `tdd`, `explain`, `prototype`, `implement`) — rejected: each rename breaks a typed habit and costs a stub window, and the outliers are short nouns people already type.
- **One heading skeleton for all 57** — rejected: a discipline skill has no workflow to number, and forcing one manufactures steps.
- **ALL-CAPS for every label** — rejected: a bracket tag sits mid-sentence a dozen times per document, and caps there is noise.
- **Bind the round's close to the GHE mirror sync** — rejected: the sync is owed from 2 rounds already and depends on the work machine; it stays the standing work-machine item.
- **A `src/` byte ratchet** — rejected: it fires on a legitimate new skill and teaches people to ignore it.

## Consequences

- The baseline, measured 2026-08-30 on `a5de2f9` plus the open round's working changes: 57 skills, 569,555 bytes of `SKILL.md`, 88 reference and template files at 392,668 bytes, 15,863 bytes in `global/rules/`, 12,123 bytes in the 2 repo-local bodies. The round's closing amendment reports the same 5 figures after.
- 9 bodies and 2 references must come under 15,000 bytes before `check_reattach_bytes` can fail: `review-changes` (24,152), `write-skill` (22,764), `diagnosing-bugs` (20,367), `phi-safe-code` (19,542), `which-skill` (18,906), `writing-for-humans` (17,464), `accessible-ui` (16,960), `health-literacy` (16,365), `rebuild-contract` (15,094); `writing-for-humans/references/tell-catalog.md` (17,463), `accessible-ui/references/patterns.md` (16,884) — corrected: see Amendments 2026-08-31
- `DOMAIN.md` gains entries for **Tightening round**, the 3 label families (**Status marker**, **Verdict scale**, **Evidence tag**), and the skeleton kinds, and its **Grounding** entry changes with the `MISSION.md` rename. They land in the round's first batch, after the open mining round commits, because `DOMAIN.md` is already modified in that round's working tree. — corrected: see Amendments 2026-08-30
- The open deferrals in `~/code/lib/_rounds/2026-08-29/reconcile.md` that fall inside this scope fold into this round's ledger: the F31 size relocations, `tell-catalog.md`'s unwatched size, D18's 3 bare script names, and the 2 description under-reaches — corrected: see Amendments 2026-08-31
- Batches run sequentially, one session each with a handoff between; each ends at its `.review.md`, and `/address-findings` runs in a fresh session.

## Deferred

- The GHE mirror sync — the standing work-machine item; settled by the next hand-sync from the work machine.
- Any rename the naming lens proposes and Nick takes — settled by an amendment to ADR-0057 in the shape batch, logged in its `## Amendments` — the table's own rows stand as landed, since amendments never rewrite. — settled: see Amendments 2026-08-30.
- The no-op batch's results — settled by an amendment here placing every nominated row in one of three states: deleted after a clean line-control run, kept because a line-control run held it, or deleted on the diagnostic with the line-control named as not run. The third state is what IF-9, IF-47, IF-174 and IF-220 landed in, and the two-state form this line first carried could never name them. — amended: see Amendments 2026-09-01

Revisit when: a later round's first fold lands a convention this record did not name — the house style is incomplete, and the missing row goes into `write-skill`'s checklist before the fold is reviewed.

## Amendments

- **2026-08-30** — Vet-grill rulings (Nick, answering the 86 NICK rows from the round ledger's vet; the per-question cases live in `~/code/lib/_rounds/2026-08-30/vet/summary.md`):
  - **Skill references, narrowed:** `write-skill` § Discipline vs orchestrator wins over § House style's flat rule — the bare backtick stays licensed for vocabulary, boundary, already-loaded-rules, and gated-offer mentions; `/name` holds at suggestion sites; "the X skill" stays banned; `check_reference_form` (the round plan's §5, Batch A) narrows to the licensed classes, and the backticked names on `global/rules/` `Depends:` lines stay as `check_global_rule` requires. `write-skill:36`'s own example is rewritten either way — it violates the shared "the X skill" ban.
  - **Labels, registered rather than rewritten:** the Verdict-scale family gains the mining scale (ADOPT/ADAPT/PARK/REJECT), whose ALL-CAPS form is a recorded exception — human-read verdicts in round-dir ledgers only, which licenses this record's own `PARK`; stored file statuses (`pass`/`drafted`/`verified`/…, lowercase backticked) become a fourth registered family; the live unregistered CAPS markers are settled per row in Batch A.
  - **A third skeleton:** the session controller — a menu- or mode-driven body that routes rather than steps (`rebuild-contract`, `evaluation-ledger`, `prototype`'s router) — joins the two shapes in § House style; forcing `## Workflow` on it manufactures steps, the same ground the one-skeleton option was rejected on. Hybrid disciplines carrying a real workflow (`tdd`, `feedback-loops`, `resolving-merge-conflicts`, `adr`, `adoption-verdict`) are noted in `write-skill`'s checklist row.
  - **The Boundary/first-screen collision, sequenced away:** size cuts land before skeleton conformance in any batch holding both; a body under the cap is never cut, so `## Boundary` closes the body while the gate one-liner stays on the first screen — both rules hold with no exception.
  - **Spelling direction for registered terms:** where the glossary's form and the bodies' disagree, the glossary respells to the bodies — each entry replaced whole through `domain-modeling`, honoring § Must not become.
  - **The `Landing:` key is renamed now:** `pre-authorised` → `pre-authorized` in the key literal, this repo's `CLAUDE.md`, `committing`, the always-loaded rules, and the selftest fixture — Nick chose the rename over the recommended lint carve-out, taking the cross-repo cost: other repos' `CLAUDE.md` keys flip when next touched, and the window is safe because the key's reader is a model, not a regex.
  - **Renames taken:** `harden-domain` → `sweep-domain`; `offboard-me` → `offboard-engineer` with its artifact `docs/handover/` → `docs/offboarding/`, deleting `DOMAIN.md`'s handoff/handover flagged-ambiguity paragraph. Each lands in Batch B with stub, router, README, `DOMAIN.md`, and the ADR-0057 table amendment the Deferred line names.
  - **Seams and scope:** `verify-docs` and `doc-claims` both stay, with a routing line in `doc-claims` naming `/verify-docs` and a flagged-ambiguity row; the four approved seam one-liners land (`domain-modeling` names its two orchestrators, `ask-for-me` gains the offboard pairing paragraph, `explain` hands multi-session topics to `/teach-me`, `wizard`'s chat fallback distinguishes itself from `/ask-for-me`); `ask-for-me`'s router entries narrow to the questionnaire the skill produces; `diagnosing-bugs` drops its vocabulary-only `requires: codebase-design`, keeping ADR-0066's declaration-set = call-set invariant; `product-description`'s description drops "re-verify" with its trigger re-run in-batch; `adr`'s "after a grill" trigger stands unchanged.
  - **Must-not-become, three added:** the round must not become a restyling round (voice edits only where a vetted ledger row names the line), a rename cascade (renames capped at the set this amendment names plus any Nick adds by name), or a second style guide (this record grows only by rulings; `write-skill` remains the style authority).
- **2026-08-30** (later the same grill) — Two more rulings: **noun names close** — `evaluation-ledger` and `rebuild-contract` keep their names, on the general ruling that a user-invoked skill may carry a noun name when the noun is its registered artifact (the ground that already protects `adr`); the naming lens stops re-nominating them. **Whole-tree lint gate per batch** — from the grill's pre-mortem: any lint check a batch adds or tightens must run clean over the entire tree before that batch ships, not only over touched paths, so a check that would over-fire on legitimate skills is found while its author is in the room rather than bypassed with `--no-verify` later.
- **2026-08-30** (closing the same grill) — The per-name pass over the Codebase-health family: `improve-design` → `review-architecture` — the old name promises an act the read-only body refuses, and "architecture" both leads its own description and differentiates from a UI-design reading, which nothing in the suite owns today; its `design-review-*.html` stray follows in the landing-zone batch. The six verb-symmetry renames (NM-21) and the scope-token renames (NM-25) are declined: `audit-tests`, `verify-docs`, `validate-behavior`, `delete-dead-code`, `backfill-adrs`, and `mine-skills` each keep names judged optimal or best-available for their jobs; the verify/validate synonymy and the `mine-skills`/`audit-skills` token overlap land as DOMAIN flagged-ambiguity rows instead. The rename cap now reads: `sweep-domain`, `offboard-engineer` (+ `docs/offboarding/`), `review-architecture`, and nothing else this round — `codebase-design` was raised and kept: "codebase" already differentiates, "design" is the discipline's accurate term of art, and a symmetry rename is the forbidden cascade.
- **2026-08-30** (Batch B landed) — The shape batch folded, closing the rename Deferred line: the three renames landed whole (directories, frontmatter, H1s, every cross-reference, `docs/lineage.md`'s two ported rows, symlinks re-installed with the dangling three pruned), with ADR-0057's amendment of this date as the table entry; the four seam one-liners, the `doc-claims` routing line, three `DOMAIN.md` flagged-ambiguity rows (verify/validate, doc-claims/verify-docs, the Audit-skills row extended over `mine-skills`), the **Handover** → **Offboarding record** entry replaced whole with the Handoff/Handover flagged paragraph deleted, the `Landing:` key literals and named files flipped to `pre-authorized` (prose in six files left to Batch A, enumerated in plan §9), the `ask-for-me` router narrowing, the `review-architecture` description's `design-quality` → `architecture` rewording carried by the rename, and the `product-description` description's "re-verify" drop with its trigger test re-run in-batch — 8/8: triggers 4/4 loaded, the re-verify prompt routed to `doc-claims` alone 2/2, the sentence-answer near-miss silent 2/2 (`~/code/lib/_rounds/2026-08-30/trigger-test/trigger-test-product-description.md`). **Two clauses of the first 2026-08-30 entry are amended by results:** no merge was adopted, and the "with stub" clause is reversed — Nick waived stubs mid-batch for all three retired names; ADR-0057's amendment carries the waiver and the typed-history counts. One B-family nomination stays OPEN: OV-4, re-batched to C6 under this record's sequencing ruling. No other remains.
- **2026-08-30** (Batch B fix pass, on review) — Rulings taken on the phase 0 + Batch B review:
  - **The rename cap holds at three.** The review proposed a fourth, `review-architecture` → `review-codebase`, on the ground that `review-changes` also reviews architecture by name, so the real axis is scope and the name needs a disambiguating sentence at `:10`. Declined: the cap is a decision, `review-architecture` was taken at the grill, and one sentence is the accepted cost. The sentence stays.
  - **A third pointer marker is registered.** `— amended: see Amendments <date>` joins `— settled:` and `— corrected:` in `adr-format.md`, scoped to body and table rows. This record's `— narrowed:`/`— extended:` coinages are replaced by it, and the normative clause that had been appended to § Must not become is withdrawn — a marker carries a pointer, never new text. ADR-0057's `Kept (42)` row takes the new marker.
  - **`domain-format.md`'s one-sentence definition rule is relaxed** to a present-tense opening sentence plus discriminating clauses, because the rule was false of nearly every row in this glossary and `onboard-repo` seeds every downstream repo from it.
  - **Six mechanical conventions this record designates "Lint" have no implementing check yet**, and four mechanical changes have now shipped ahead of that gate. All six are Batch A's per § Scope and order and the round plan's §5; the escalation is recorded rather than acted on, and the intake gate's own premise — that the lint lands in the same batch as the change it locks — is what Batch A has to make true.
- **2026-08-30** (phase 0, recorded on review) — § Consequences names only the **Grounding** entry for the `MISSION.md` → `mission.md` flip; phase 0 flipped the **Mission** entry beside it as well, each replaced whole. Two cells in one glossary naming the same file differently is the contradiction the second edit avoided, so the flip covers both. The glossary deliberately leads the bodies here: 39 occurrences across 8 files still write the old workspace filenames, and Batch A closes the window with the §6 renames and `check_artifact_names` in the same batch. The phase-0 review read this as a defect (F17) and it is not one — it is the ordering this record chose.

- **2026-08-31** — two Consequences corrected at the close of Batch A4. **The size list.** Re-measured at this batch's tree, 5 of the 11 are now under 15,000 bytes (`which-skill` 14,970, `health-literacy` 14,678, `rebuild-contract` 13,685, `tell-catalog.md` 14,758, `patterns.md` 13,908) and 6 remain over: `write-skill` 16,921, `review-changes` 16,726, `writing-for-humans` 16,538, `phi-safe-code` 16,449, `diagnosing-bugs` 15,631, `accessible-ui` 15,211 — corrected: see Amendments 2026-09-01 (the `phi-safe-code` figure is 16,448, and `work-item-shape` crossed inside this batch, making seven). Three of those 6 grew in A4 because the first-screen rule this record also binds spends bytes in the opening to save the close-out; `check_reattach_bytes` cannot flip to FAIL until the remaining 6 come under. **The description under-reaches.** Both rows landed in the 2026-08-29 round and are recorded folded at ADR-0075's batch-18 and batch-19 amendments, as are D18's 3 script names; scheduling them again here read as a live permission to edit a frozen description. It is not: § Scope and order's freeze is absolute for this round, and any description edit takes that skill's trigger test re-run in the same batch.

- **2026-08-31** (batches C1–C3 folded, on review) — The prune-and-relocate batches landed: 16 bodies pruned, 28 reference files created, 106 ledger rows folded. The review returned 34 findings; the rulings taken on them:
  - **§ Sizes' relocation condition is a scope test, not a frequency one.** Six of the review's eight blockers were one shape — a rule binding *every* run, filed behind a condition that fires only sometimes, so the run that needed it was never routed to it. The review is its own instance: the rule that stops a reviewer obeying the post-change version of the instruction files it is reviewing had moved under a handoff-only lens, and this review had no handoff. Restored to the bodies that execute them: `review-changes`' instruction-file rule, its built-in degradation rule, its not-examined requirement, and both `codebase-design`/`discoverable-code` load gates; `address-findings`' stamp command, whose gate was narrower than the stamp it compares. A condition that excludes 2 of 57 skills (`cross-skill-wiring.md`) is "always" in practice and was rescoped to a property of the edit rather than of the skill. The lesson the round carries forward: the condition is measured against *which runs need the rule*, not against how often the file is opened.
  - **The four judgment conventions become six.** § The intake gate names voice, skeleton by kind, label families, and the relocation condition; the checklist that landed carries two more — table shape (data table / record table) and the ban on a model-invoked body restating its description's trigger gate. Both were settled before they landed (the table species by Nick on 2026-08-30 to the recommended line; the trigger-gate question by the vet, which ruled `write-skill`'s checklist must settle it once before any of three ledger rows landed) but neither reached this record. A permanent convention entering the single home is a ruling, and § Must not become's "this record grows only by rulings" is what makes that the test.
  - **The fourth label family is registered**, closing the first 2026-08-30 entry's clause that stored file statuses "become a fourth registered family". `DOMAIN.md` gains **Stored status** — the lowercase-backticked family a skill writes into an artifact and reads back on a later session, where the other three are rendered into a report and read once. The distinguishing case is `pass` in a `product-description` verification row against `PASS` as a **Status marker**: same word, different family, sorted by who reads it back. `write-skill`'s two count lines now say four.
  - **The `## Boundary` checklist row states a target, not a state.** It landed while `writing-for-agents` and `writing-for-humans` both fail it, which `DOMAIN.md`'s own "2 of 22 model-invoked skills close on `## Boundary` today" already records as corpus-wide. Adding the two sections is authoring work, not a fix-pass edit, and is left to a later batch; what landed instead is `skill-template.md` gaining the Principle skeleton and Session controller beside the Workflow skeleton, so a discipline drafted from the template is no longer born failing the row beside it.
  - **One body ships over the cap, on purpose.** `review-changes` is 16,360 bytes because this pass restored always-binding rules to it; relocating further always-executed material to reach 15,000 would reproduce the exact defect six of the eight blockers named. Recorded as an exception so nobody flips `check_reattach_bytes` to FAIL believing the job is done — § Consequences' list of bodies that must come under still binds, and `review-changes` is still on it. `diagnosing-bugs` did come under (14,971) by relocating Phase 5's fix-acceptance tests, which are read once a fix exists; its headroom is 29 bytes, so its next addition needs its own relocation.
  - **Bookend sites the fold missed.** `scripts/git-hooks/commit-msg`'s non-coverage header named `tell-catalog.md` after the ten commit-and-PR tells — the register the gate deliberately does not check — had moved to `tell-catalog-shipping.md`; it now names both. `scripts/lint-skills.sh`'s pointer to `write-skill` § Frontmatter pitfalls now names `references/descriptions.md`, where that section went. And the register table's admission guard in `writing-for-humans` was cut on the stated ground that no deciding record owned the table — [ADR-0056](0056-first-off-path-skill-change-quiz.md):11 names it explicitly, so the premise was false and the guard is restored rather than the record amended.

- **2026-08-31** (batches C4 onward folded, on review) — The rest of the prune-and-relocate sequence landed: 61 bodies and references modified, 32 new reference files, three body skeletons extracted, the four-way `ado-html-transport` duplicate collapsed into one lint-pinned group. The review returned 65 findings — 16 blockers, one escalation — and the rulings taken on them:
  - **Eleven of the sixteen blockers were one of two shapes**: a pointer that no longer lands after its target moved, or content that left a body and arrived nowhere. Both are invisible to `scripts/lint-skills.sh`, which validates a link's *file* and never its `§` section, and never checks that a reference is pointed at from anywhere. That gap is the round's own lesson and Batch A's work; twenty-four `§` pointers exist repo-wide today with nothing checking them.
  - **The conditional-open header is registered.** Thirteen of the new references opened with `Open this only when …`, a convention with one prior instance in the tree and no home in the glossary, the authoring guide, or the linter. It now has a `DOMAIN.md` row and a clause on `write-skill`'s relocation-condition checklist row, which state the same thing: the header is the *second half of a bookend* and never the only statement of the condition, because the run decides whether to open a file from the body it already has. Four relocations failed that test on review (`adoption-verdict`'s adversary pass, `phi-safe-code`'s fixtures section and its four unconditioned pointers) and were fixed at the caller.
  - **Four bodies sit over the re-attach bound after the fix pass, and the overage is the fix.** `phi-safe-code` (16,075), `accessible-ui` (15,211), `writing-for-humans` (15,089, since grown to 15,395 — re-ratified at the measured figure by the C7–C9 fix pass, see the 2026-08-31 (Batch C9) entry), and `write-skill` (15,026) — Nick ratified the exception on 2026-08-31 rather than spending a relocation pass. Each grew by restoring always-executed rules the extraction had relocated or dropped — the fixtures rule the skill's own description names as a trigger, the composite's nested-stop clause, the pointer-cancellation rule, the five caller-side opening conditions. This is the ground the 2026-08-31 C1–C3 amendment used for `review-changes`: cutting elsewhere to reach a byte target reproduces the defect the restorations closed. `health-literacy` came under (14,542). § Consequences' list still binds, and all four stay on it.
  - **The one relocation the round should not have made is reversed.** `adoption-verdict`'s adversary pass returns to the body as § The adversary pass, fired by a new workflow step 6, and its 3-line reference is deleted. The body was 6,300 bytes *under* the cap, so nothing forced the extraction, and it had put two degradation clauses — "Tier 1 never offers", "a requested pass never degrades silently into a bare verdict" — inside a file the runs they govern never open. That is `writing-for-agents`' rule-placement failure, and the fix is the body, not a better pointer.
  - **The fourth tier gets its body skeleton, and the drift it was already carrying is closed.** `to-feature` was the one tier the round left with its skeleton duplicated across two templates, and the two copies had already diverged (`- Outcome 1 — what success looks like` against `- Outcome 1`; `## Open questions` in the GitHub copy only). `feature-body.md` now holds the tracker-neutral sections once, both templates assemble from it, and `## Open questions` reaches ADO for the first time.
  - **The routing policy line gets its record.** The review escalated `onboard-repo`'s cross-repo `CLAUDE.md` write rather than ranking it. [ADR-0078](0078-publisher-routing-policy-reinforces-not-enforces.md) settles the four questions it raised — the `Issue tracker:` block owns the line, `work-item-shape` cites it rather than caching its 45 words, already-onboarded repos never gain it, and the publishers name the publisher rather than refusing — on the ruling that the line **reinforces** the routing gate and is never the mechanism. That ruling is what makes writing prose into other people's repositories acceptable: deleting it costs a reminder and changes no behavior.
  - **Two glossary rows went stale in the round's own direction and were corrected, not the code.** `from-ticket` narrowing to Declared-mode-only with no file writes is the deliberate change; `DOMAIN.md`'s Cold-start-loader row still promised a `CLAUDE.md` bootstrap and now does not. The `Chart:` title marker lost its only naming site to an extraction and is restored, named from the create path as well as the format reference. The Declared-dependency row gains a third case — a `requires:` edge declared for installability behind an **Opportunistic reference**, which ADR-0066 already licensed and two skills already practiced.
- **2026-08-31 (Batch C9)** — The always-loaded cluster landed, with three corrections to § The always-loaded rules, each on evidence that section did not have:
  - **`large-write-chunking`'s host is `handoff`, not `write-skill`.** The section named `write-skill` as a receiver; it has zero citations of the rule and is not on the rule's `Depends:` line (skill bodies are the one document class the rule never governs), while `handoff` § Where to write it already carried the one-line summary. The four file-write bullets moved there; the ten other citing sites repoint to that section; `writing-for-humans` was rejected as host on size alone (15,089 B at `1aa50be`, already over the re-attach bound; 15,395 B after this batch's own pointer and the C7–C9 fix pass repointed it — re-ratified at the measured figure on 2026-08-31 rather than reclaimed, since the overage was the fix here as it was at `:115`). The chat-pause bullet cannot move — its surface is a bare-turn chat message, the reason the directory exists — so the rule survives residually with the truncation invariant and the `[PAUSED — …]` marker verbatim, not "or nothing".
  - **The `Measured-tree:` bullet stays in `evidence.md`, condensed.** The named move into `handoff` inverts two live pointers (`handoff` cites the global rule for the line; `DOMAIN.md`'s Measured-tree row binds *every* count-carrying producer, including `audit-skills` and `sweep-corpus`, which are not on the `Depends:` line), so no single dependant can own it. The inline `GIT_INDEX_FILE` recipe — a third, already-drifted copy — condensed to a pointer at `review-changes`' tree-stamp reference, and the two producers with no stamp requirement gained their one-liners. The counts and recall bullets split: counts stays (ad-hoc chat is its surface — the ADR-0076 admission test), recall moved to `committing`'s commit-style reference, which already cited it.
  - **The dash pipeline landed in `writing-for-humans`' existing `outbound-as-the-user.md`, not a new reference.** The finder's objection (a relocation whose condition is "always" is refused; a paraphrased regex is a silent pass) is answered, not absorbed: the reference opens on the outbound-draft branch, not at load — 4 draft-shaped blocks against 3,327 in the measured 30 days — and the residual rule keeps the gate plus a mandatory run-verbatim pointer naming the byte escapes a session cannot re-derive. `outbound-as-the-user.md` won over a new `dash-sweep.md` because the outbound row already opens it before drafting, so the pipeline is in context exactly when the sweep runs.
  - **The 8,000-byte budget is not reachable by the licensed cuts.** With every vetted move and condense taken and every keep-listed bullet honored, the directory lands at 11,970 bytes measured (`wc -c global/rules/*.md`, 2026-08-31, after the C7–C9 fix pass) — the finder's maximal reading (both moving files deleted whole) still measured 8,932. Against the amended 12,000-byte floor that is **30 bytes of headroom**, not the ~1,500 an earlier "roughly 10.5k" estimate in this entry implied; the figure is stated measured here because `check_rules_bytes` is still unwritten (`grep -c check_rules_bytes scripts/lint-skills.sh` → 0) and nothing else will correct it. The fix pass spent 504 of the 534 bytes that remained, restoring two bullets to `evidence.md` (below), so the directory is now effectively at its floor and Batch M's ratchet is the only slack left. The residual overage is recorded in the round plan's §9 with the choice it forces — amend the budget to the measured floor, or name cuts inside content the vet keep-listed (which the pruning rules refuse on inspection); Batch M's micro-tests (`Prose is a first draft` among them) may free more. The directory total before and after is reported in the batch's ledger rows.
  - **Ruled by Nick, 2026-08-31, same day:** the budget amends to a **12,000-byte floor** — `check_rules_bytes` lands in Batch A against 12,000 — and Batch M is the ratchet: `large-write-chunking`'s residual and `outbound-dash-sweep` are nominated for line-control runs beside IF-149, on the weakest-evidence ranking (430 writes/0 truncations; 0 organic dashed drafts in the window, the 2 counted being ADR-0073's M18 control arm — against the pre-admission Teams failures that argue keep). No rule is cut on inspection: a zero count is admission evidence, not retirement evidence, and retirement takes the line-control run. If all three nominations fall, the directory lands near 10,400 and the floor is revisited then.
- **2026-08-31 (Batches C7 + C8 + C9, fix pass)** — The batch landed as one review cycle across three clusters, 71 files; the C9 entry above describes the always-loaded seven. Ruled by Nick the same day, on the fix pass's asks:
  - **The amendment log covers C7 and C8 too.** § The always-loaded rules governs C9; the C7 (handover and learning) and C8 (docs, design, verification) clusters landed under the same cycle and are recorded in the round plan's § 9 per-batch rows. Extending the log rather than narrowing the batch is the ruling; § Must not become's "grows only by rulings" is satisfied by this entry being one.
  - **`global/README.md` was rewritten past § The always-loaded rules' "spelling and casing only" fence, and the edit stands.** The file's admission key described a `Why not a hook or lint:` line the rule files no longer carry, so the edit was forced by this batch's own move. `:17` carries `— amended: see Amendments 2026-08-31 (Batch C9)`; the scope line is amended here rather than the file reverted.
  - **`glapi-test-pass`'s de-relocation gets its ground, and a scoped exception.** Inlining `references/ado-commands.md` into the body (ledger IF-213) inverts § Sizes' default direction; the ground is that all twelve steps are one ordered shell session whose variables carry across steps, so a reader who must hop to a reference mid-procedure loses the thread the procedure *is*. The exception: the **re-run path** returns to `references/re-run.md`, because the F2 blocker fix (the re-run leaves `SUITE_ID` and `TEST_CASE_ID` unset for steps 7, 9 and 12) did not fit under the 200-line cap, and a re-run branch is genuinely conditional — a first run never opens it. The body sits at exactly 200/200.
  - **`glapi-test-pass` departs the Workflow skeleton deliberately.** It carries no `## Notes`; there is no room for one, and the four cross-cutting caveats are stated at the steps they govern. `DOMAIN.md`'s Workflow-skeleton row already reports conformance as a target rather than a lint, and the count there is now 5 of 35.
  - **Two clauses return to `evidence.md`, reversing this record's own C9 move.** ADR-0075:98 had ruled by name that *A check parses the way its consumer parses* and *an empty search does not refute a claim* stay in the always-loaded rule; the C9 batch moved them into `committing` and `receiving-review` without naming the reversal. Both are restored (the `receiving-review` instance stays as a second site), and the recall check is lifted from `commit-style.md` into `committing`'s body, where the flows that write summaries actually reach it. The cost is 504 of the directory's 534 remaining bytes — recorded in the budget entry above.
  - **Four checks stay unwritten, deliberately.** `check_section_pointers` and `check_reference_orphans` (round plan § 5 items 8 and 9), `check_reattach_bytes` in the pass-2 arms, and an `enum-selftest.sh`. Each must land with a fixture and a mutation in both directions per `scripts/README.md`, which makes them a scripts batch rather than fix-pass edits. They land before the next relocation-heavy batch; until then the `§` pointer and orphan walks are done by hand, as they were here.
- **2026-08-31 (Batch A2 — shape)** — The 93 shape rows landed: skeleton by kind, the first-screen gate, table species, and voice, plus § Scope and order's filename renames. 47 files changed. Six rulings the batch had to make, each because the shape rules read either way on the case in front of it:
  - **The Workflow skeleton gains a mode-section slot, and never opens an empty `## Notes`.** Four publishers (`to-feature`, `to-story`, `to-tasks`, `to-bug`) close on update- and maintenance-mode sections and one sibling (`from-ticket`) carries `## Notes`; four identical deviations against one conformer reads as the spine missing a slot, not as four defects. A mode section now sits between `## Workflow` and `## Notes`, and a body with nothing to note ends at its last step — an N/A section is the scaffolding `work-item-shape` already forbids. Registered in `write-skill` § Review checklist and `DOMAIN.md`'s **Workflow skeleton** entry; no publisher body changed.
  - **A principle section is headed by the rule's own leading words.** § Shape's "headed by their leading word" read two ways, and the two Domain skills answered it differently from `accessible-ui` — seven of eight `phi-safe-code` H2s and all eight of `health-literacy`'s are full clauses. Pinned: a short noun heading and a full imperative clause both conform; only a topic label fails, which is what `writing-for-agents` already says about step headings. No H2 was rewritten.
  - **A router and a one-prose-run body take no spine, and a `global/rules/` file takes none of the three.** `which-skill` is a catalog and `explain` is four paragraphs; forcing `## Workflow` on either manufactures steps a run does not take, the ground § Considered Options rejected for disciplines. The five rule files carry sentence-case propositions as H1s, which § House style's "H1 is the skill's display name" cannot reach. All three exemptions are checklist rows, not lint.
  - **`write-skill`'s reference sections moved under `## Notes`, so its checklist is now `### Review checklist`.** The user-invoked skeleton left nine post-Workflow H2s standing; they are now H3s under one `## Notes`. `CLAUDE.md` and `DOMAIN.md` switch to the level-agnostic `§ Review checklist` form; § The intake gate for later rounds still wrote `## Review checklist` for the same section — corrected: see Amendments 2026-09-01.
  - **`handoff` § Where to write it owns one filename shape for every cross-session artifact.** § Scope and order's three strays fold into `<repo>-<date>-<slug>.<kind>.<ext>`, the kind segment dropped for a handoff: `.audit.md` (`audit-skills`), `.questionnaire.md` (`ask-for-me`), `.design-review.html` (`review-architecture`). A new kind takes a new segment there rather than a shape of its own. The workspace renames landed in the bodies (`mission.md`, `progress.md`, `resources.md`, `notes.md`, `kt.md`, 40 occurrences across 7 files); the `git mv` ×4 in `~/learning/aeo-geo` ran the same day, once Nick allowed the command: four `R` renames staged on that repo's `5ed0cb8`, with a grep for the five old names across its files returning nothing.
  - **Four bodies grew past the re-attach bound to gain their close, and no licensed relocation row remained.** `diagnosing-bugs` 14,970 → 15,446 and `writing-for-humans` 15,394 → 16,079 (both `## Boundary` plus a hoisted hard stop), `write-skill` 15,026 → 15,676 (the checklist rows above), `phi-safe-code` 16,075 → 16,135 (bolding 15 table keys). The three already on the ratified over-cap list stay there; `diagnosing-bugs` joins them or takes a new relocation nomination, which is the A2 review's call.
- **2026-08-31 (Batch A3 — bookends)** — The 105 bookend rows landed: 77 amendment-bookend, 16 naming, 12 discoverability. Five rulings the batch had to make:
  - **A description edit's license splits on the invocation axis.** § Scope and order freezes descriptions except behind a trigger-test re-run "in the same batch, because a description is the one part of a model-invoked skill with a measured behavior". A **user-invoked** skill has no measured behavior to re-run — its description is a menu line — so the re-run is vacuous and the edit lands on inspection; the round's own vet said so on eight separate rows. Eight user-invoked descriptions were corrected here. The nine model-invoked ones PARK as one set behind a single run, recorded in the round plan's § 9, which is what A1 already did with HS-96.
  - **A bookend batch grows bodies, and the cap has to absorb it.** Widening an under-reaching summary adds bytes by construction, and no relocation row is licensed for the bodies A3 touches. Three bodies joined the over-cap set (`diagnosing-bugs` from A2, `work-item-shape` crossing unrecorded in A2, and this batch's own additions on `write-skill` and `phi-safe-code`), while `which-skill` was trimmed back under by cutting A3's own prose rather than the body's. The § 9 row carries the fork for the review: extend the ratified exception list to seven, or take relocation nominations.
  - **A `Depends:` line is lint's forward list, not a census of dependants.** AB-89 asked for six more names on `recommend-and-proceed`'s line; that costs about 90 bytes against a directory floor with 31 to spare, and owes the same census for four more rules. The finding landed at the sentence that actually asserted completeness — `global/README.md`'s parenthetical — which now gives the grep that finds dependants rather than a census figure nobody can re-derive from it — corrected: see Amendments 2026-09-01 (the 43 was the count of skill directories containing the bare word "evidence"; the grep the sentence prescribes returns 10). The forward direction is what `check_global_rule` enforces and it is unchanged.
  - **The head stamp gets a literal, as the tree stamp has one.** `grep -rnF 'Reviewed-head:'` returned 0 repo-wide while five bodies told a reader to read that field; `review-changes`' stamp paragraph (a bolded lead inside its numbered step, not a heading) now fixes `Reviewed-head: <short-sha>`. The tree stamp's own under-specification, carried since the C7-C9 fix pass, landed with it: both `tree-stamp.md` siblings now say the stamp carries nothing after the hash, because the `review-receipt` hook's pattern anchors at end of line.
  - **A registered term with no body spelling is an orphan, not a respelling.** The 2026-08-30 spelling-direction ruling (glossary respells to the bodies) applies only where the bodies spell the term at all; four terms did and were respelled. Nine registered terms have zero body hits, so there is nothing to respell to and each is a cut-or-give-it-a-consumer call — the call **Scope drift** and **Admission naming** took here, both landing a consumer. The nine carry to A4 with the DM rows.

- **2026-08-31 (Batch A5 — lint checks)** — **Eight of the nine lint checks of the round plan's §5 landed as FAILs; the ninth (`check_reattach_bytes`) stayed a WARN, and the mutation coverage was thinner than this sentence claimed — corrected: see Amendments 2026-09-01.** The eight: `check_rules_bytes` (the `global/rules/` total against the 12,000-byte floor this record's C9 amendment set — 11,997 B at this tree, 3 B of headroom), `check_spelling`, `check_heading_case`, `check_reference_form`, `check_artifact_names`, `check_labels`, `check_section_pointers` and `check_reference_orphans`. `check_reattach_bytes` did **not** flip: 7 bodies sit over 15,000 bytes (`write-skill` 16,921 · `review-changes` 16,726 · `writing-for-humans` 16,538 · `phi-safe-code` 16,448 · `diagnosing-bugs` 15,631 · `accessible-ui` 15,211 · `work-item-shape` 15,042 — one more than the A4 amendment above records, `work-item-shape` having crossed in A2), so the FAIL stays parked and the §5 half that could land did: the WARN now walks `references/*.md` and `.claude/skills/*/` as `check_reference_bytes`, silent at this tree because nothing there is over. `bash scripts/lint-skills.sh` and `bash scripts/lint-adrs.sh docs/adr` exit 0; `bash scripts/lint-skills-selftest.sh` exits 0 against 65 pinned fixture FAILs, and every other selftest under `scripts/`, `scripts/git-hooks/` and `global/hooks/` exits 0.
  - **The two checks §5 specified but no scan can implement as written were narrowed, in the open.** `check_reference_form`'s "a bare backticked name of a user-invoked skill FAILs" would fire on 604 sites, because the 2026-08-30 amendment above licenses the bare backtick for vocabulary, boundary, already-loaded-rules and gated-offer mentions and no pattern separates those from a suggestion. It grades the two forms banned outright instead: "the X skill" for a name that resolves to a skill, and an invocation verb followed by a bare backticked user-invoked name. Two live sites failed it and were fixed (`README:60`, `implement:66`), both suggestion sites where `/name` is what the human types. `check_labels` needs a closed vocabulary, and the tree carries 24 ALL-CAPS tokens that are acronyms, SQL and shell keywords, or literals quoted from elsewhere (`PUBLIC` from a GitHub visibility, `CUTOVER` from a confirm token, `STAGES` from a template marker); the check reads the registered set out of `DOMAIN.md` and carries those 24 as a named list in the script, exempting a token that carries a digit or an underscore by form.
  - **Six live defects the new checks found, all fixed here.** A broken `§` pointer (`prototype:33` cited `review-architecture` § Before you present, a heading that lives in that skill's `references/html-report.md`); `labelling` in `phi-safe-code:56`; `humour` in `health-literacy`'s translation reference; `flavour`/`flavours` in [ADR-0075](0075-transcript-sourced-mining-round.md); and `# Ask For Me`, the one H1 whose function word was capitalized where `Writing for Agents` sets the house form. `N+` is registered in `DOMAIN.md`'s Status-marker row, discharging the C9 note.
  - **Two defects in the checks themselves, both of the shape this round keeps finding.** A `case "$w" in [A-Z]*)` test under a UTF-8 ctype is a COLLATION range that matches every lowercase letter from `b` on, so the H1 test passed every lowercase word until it was rewritten as `[[:upper:]]`; and a `~` opening a regex written inline in `[[ =~ ]]` is tilde-expanded to the home directory, so both path arms of the section-pointer check matched nothing while the sweep reported clean. Neither is visible in a green run, which is why the mutation matrix — 24 mutations this batch, all red, two of them only after the fixture that grades them was written — is the evidence and not the run. Measured-tree: c3d6cc057e786de30dcb146266e0c2e36046d6c4 (taken before this line was added; the line is the only edit after it).

- **2026-09-01 (Batch A fix pass)** — The review of Batch A returned 144 findings; this entry records what they corrected in the round's own record, and the two rows this record had not carried.
  - **Batch A1 has no entry, and never did.** The A-numbered entries run A2, A3, A4, A5. A1 landed roughly 117 rows — the spelling flip, heading case, invocation form, label families, the core of the batch — with its only durable trace in `plan.md` outside this repo. The rows are folded and reviewed; what is missing is the record, and this line is it rather than a reconstruction nobody can check. The A5 entry's sixth defect claim, `licence` in "this record's own A1 amendment", is struck: `git show <pre-batch>:docs/adr/0077-*.md | grep -c licence` returns 0, so the string never appeared here.
  - **The A5 count is corrected to eight.** The sentence read "the nine … landed, each with its fixture instance and its selftest mutation in both directions" and then listed eight FAILs while recording that `check_reattach_bytes` had not flipped. Eight landed as FAILs. The mutation claim was also too strong: measured by mutation at the reviewed tree, three name lists were graded on one member each, two on none, the `docs/**` walk on nothing at any root, and six classifier cells could be deleted with the selftest green. All are graded now — five list-count pins, a pass-4 fixture in both roots, and one house-style instance per classifier class.
  - **The A4 byte list is corrected.** `phi-safe-code` is 16,448, not 16,449 (the lint run agrees), and `work-item-shape` crossed the bound inside Batch A at 15,042, making seven over rather than six. Two more crossed in this fix pass and were traded back down or accepted: `which-skill` was trimmed to 15,008 and stays over by 8 bytes; `write-skill` is 17,548, further over its own cap, because the checklist corrections this pass owed it are additive.
  - **The `Depends:`-census figure is struck, not restated.** `global/README.md` claimed "43 skills cite `evidence.md`" beside the grep that computes it; that grep returns 10 (43 was the count of skill directories containing the bare word "evidence"), "four … overriding its bin 1" is three, and the `recommend-and-proceed` figure of 11 was right. The sentence now gives the grep and no figure: a census in prose beside its own command is a number the reader cannot re-derive from the instruction they were just given.
  - **The `no-unasked-commits` rename is recorded here, not only in the plan.** The rule's H1 is now *No unasked outward acts*, because it binds five acts; the file stem stays `no-unasked-commits.md`, because `src/committing/SKILL.md` and `install.sh`'s roster both cite the installed path. The mismatch is deliberate and this is its record.
  - **The `Landing:` legacy-key sentences are restored.** The spelling flip fired inside three code spans whose only job was to name the PRE-rename British spelling, leaving `committing` a tautology, `README.md` asserting that legacy blocks use the current spelling, and this record's own rename line reading `pre-authorized` → `pre-authorized`. All three now read `pre-authorised` on the left. Eight `licences.tsv` citations across six records and a recorded `honours` control regex in [ADR-0075](0075-transcript-sourced-mining-round.md) were corrupted the same way and are restored. The inline exemption §5 specified and A5 did not build now exists — `<!-- spelling-exempt: WORD -->`, with a `deliberate_british` roster in `scripts/lint-skills.sh` naming every literal and the consumer that requires it — so the next deliberate British form is greppable instead of indistinguishable from a missed one.
  - **The spelling flip is repo-wide with two trees excluded, not with one word excluded.** `global/hooks/**` and `scripts/**` shell stay British throughout, because they carry machine-matched literals in every position a code span cannot mark — `hook_allow "tokeniser error"` matched by three selftests, the `neighbours.sh` fixture path four security-selftest mutation rows name by string, probe labels, expectation needles — so separating this repo's prose from a string another file must match, line by line in shell, is what the exclusion buys out of. The consequence is stated where it was not before: ordinary prose in those trees keeps its British forms too. Flipping them would mean flipping four selftests in lockstep, and a miss turns three fail-open breadcrumbs into silent failures. [ADR-0054](0054-commit-bypass-hook-always-on.md)'s prose is corrected to `tokeniser error`, the literal the hooks emit. The prose READMEs under `scripts/` are **not** excluded — they hold no literals — and pass 4 now walks `scripts/README.md`, which held the one remaining British form in the repo.
  - **The 12,000-byte rules budget stays a FAIL at 12,000.** The pruning in this pass took the layer to 11,940 B (`cat global/rules/*.md | wc -c`), 60 B of headroom against the 3 B the review measured. The check's job is to make a rule addition a deliberate act, and a WARN retires it by habit — the same failure a bumped constant would be.
  - **Heading title case is scoped, not reverted.** `tell-catalog.md`'s "sentence case, everywhere, including titles" is scoped to human-facing prose, with the `SKILL.md` H1 named as a display name that takes title case and is enforced by `check_heading_case`. The alternative was reverting a landed convention and a shipped check to satisfy one unscoped clause.
  - **The Cold-reader pass is bounded to artifacts that outlive the session.** "Anything that leaves this session" reached every publish confirmation and terminal report the skill's own description names, so it was either obeyed at the cost of a subagent per report or silently ignored — and a rule routinely ignored stops being read.
  - **`writing-for-agents` § No nuance clauses loses its own exemption clause.** "Spending the ban is a last resort and is recorded in the decision record" is unenforceable — no reader can tell what act "spending the ban" names — and it is a carve-out written into the rule that forbids carve-outs written into rules, two sentences after "never to a carve-out written into the rule".
  - **Four checklist rows get the rulings two lenses split on.** A principle heading passes when **the section under it states a rule** (`## Names are search queries` passes, `## During the work` fails) — the distinction "short noun heading" versus "topic label" never supplied one, and the two house-style lenses graded `accessible-ui` and `domain-modeling` oppositely on it. The conditional-open header's `Open this only when …` is the **shape, an example rather than a required literal**, which is why 3 of 47 references matched it and 22 more state the condition in their own words. The spine row's three no-spine cases are re-anchored **by shape** rather than by skill name, which is the anchoring row 16 requires. And naming "the agent" *descriptively* — the thing a rule is about, not the reader being instructed — conforms; only the addressee use fails.
  - **`check_reference_form` is renamed `check_invocation_form`.** `DOMAIN.md` registers **Invocation form** and its normative Aliases-to-avoid column bans *Reference form* for clashing with **Reference file** — and the clash landed: `grep check_reference` returned three checks about reference *files* plus one about invocation form. Both of its failure messages now name the convention, so a maintainer arriving from that side reaches the check.
  - **Two filenames the shape gave a token they cannot resolve.** `audit-skills` writes `skills-<date>-<slug>.audit.md`: its `<repo>` would have named whichever repo the session started in, three sentences after the same paragraph refuses a `Measured-tree:` line for that exact reason. `review-architecture`'s report carries `<HHMMSS>` in its slug, because ADR-0022 fixes it as timestamped per run and the shared shape's date alone let a second run overwrite the first. `handoff` § Where to write it now also states the disambiguator positively — a handoff is the kind with **no kind segment** — because the widened shape added two more `.md` kinds sharing the prefix, which `/review-changes` and `/from-ticket latest` would otherwise pick up as handoffs.
  - **§ Where to write it left `## Workflow`.** It is a suite-wide contract that a dozen documents cite, not a step, and `## Workflow` opening on it made every citing document enter a workflow to reach a definition.
  - **Two skills' spines and two bodies' section placement.** `sweep-corpus`' refusal gate was hoisted out of its own sequence by the renumbering, so an agent reading `## Workflow` started at the lint loop and skipped the one gate that stops a sweep while a finding is open; it is step 1 again. `grill-me`, `offboard-engineer`, `tdd` and `upgrade-deps` each had always-executed material demoted under `## Notes` or `## Boundary` and now carry it in the sequence. `diagnosing-bugs`' `## Phase N —` spine is named in the checklist's hybrid list, which had enumerated the hybrids and omitted it.
  - **The batch's scope list overstated one row and understated the delivery.** `## References` removed from `write-skill`'s template was already gone at `335ab79` (C1–C3), so §4 listed as Batch A scope something this diff does not contain; the end state is correct and complete. Against that, 29 of 31 ported bundles changed beyond spelling and case and `DOMAIN.md` carried ~16 non-spelling edits — the two review checks §4 states are **not met**, and the reason is structural: §9 pulled the shape, bookend, naming and discoverability lenses into A1–A5 without amending §4's sentence. Neither is recorded as passed.
  - **`## Review checklist` at § The intake gate for later rounds is now `§ Review checklist`.** The A2 entry named § Consequences as the stale site; the stale line was under the intake gate, and the A2 sentence is corrected above.

- **2026-09-01 (Batch M split — the no-op bar, relaxed for four rows and held for three)** — **The two-tier rule in § Pruning is amended, not withdrawn.** That paragraph rejected "relaxing the bar for the round" on the prune limit, and the prune limit still holds; what it did not have is the measurement below, which changes what the bar costs rather than what it protects.

  **The fact.** No pressure-scenario harness exists for any nominated site. `find` over this repo and over `~/code/lib/_rounds` for `*scenario*` returns nothing (2026-09-01); the only harness in the round directory is `trigger-test/`, which grades descriptions rather than body rules and whose `BASE` points at a scratchpad path from a dead session. So § Pruning's bar — corrected in this same round to read **line-control run**, since the micro-test's own control drops the whole skill and answers a different question — is not seven runs — it is **seven pressure scenarios built from nothing, per `write-skill/references/testing-skills.md` § Building pressure scenarios, and then run.** `writing-for-agents` § Pruning already carries the branch for exactly this case: "Where no scenario stands behind the document … there is no control to run: cut on the diagnostic below instead, and name the missing control in the change's record." This entry is that record.

  **Two rows were never under the bar, and are re-tiered.** Both were carried at tier `no-op` and neither is a no-op deletion. **IF-198** (`codebase-design:32`, the "inevitable in hindsight" gut-check) carries the ground *a check the agent cannot perform*, which is **Scaffolding** — one of the five that land on inspection. **AC-15** (`resolving-merge-conflicts:9`, the bolded `never --abort`) carries no deletion ground at all: it is an ADR-0024 conformance finding, and its remedy is to add the rationalization rows or step the line down to a positive recipe — a rewrite, not a cut. Re-tiered to `mech`; they land on inspection with no control owed.

  **Four are cut on the diagnostic, with the control named as not run.** IF-9 (`write-skill:14`, the "Gather requirements" step), IF-47 (`capturing-learnings/references/learning-format.md:24`, the YAML-quoting note), IF-174 (`handoff:12`, the argument-tailoring line) and IF-220 (`doc-claims:10`, "Read the prose directly for meaning"). Each is a restatement of what a neighboring step or reference already carries, and none is a rebuttal aimed at a named rationalization — which is the thing the prune limit protects and the reason the other three are held. **These four are recorded as cut without a line-control run, not as proven no-ops.**

  **Three are held until a control runs.** IF-83 (`backfill-adrs:59`, "Once the candidate list is exhausted, stop. Don't keep mining for more"), IF-113 (`to-feature:45` and `to-story:45`, "This is a pre-publication direction check, not interviewing") and IF-129 (`health-literacy:34`, the rate-comparison rule). All three are anti-drift lines aimed at a named pressure — the shape § Pruning's prune-limit sentence describes, and the shape that reads as fluff on a calm pass. They stay PARK with a line-control run as the event that reopens them — the scenario re-run on the body minus that one line, never the micro-test's whole-skill control, which would show the failure the skill exists to prevent and settle nothing about the line, alongside the three whole-file nominations in `global/rules/` that the 2026-08-31 C9 ruling put here ("No rule is cut on inspection: a zero count is admission evidence, not retirement evidence"). **Nothing in this entry touches those three files.**

  **The line-control runs use Sonnet** (Nick, 2026-09-01), for cost. Recorded with its limit: `writing-for-agents`' diagnostic asks whether *this exact agent, in this exact context* would do it anyway, so a control on one model is evidence about that model's defaults. A hold confirmed on Sonnet is the stronger direction — the rule earned its line on a cheaper model, so it earns it here — while a clean line-control on Sonnet does not by itself retire a rule for a body an Opus session loads. Any cut taken on a Sonnet control names the model on its ledger row.

  **What this does not settle.** The third line of § Deferred stays open: three no-op rows and three whole-file nominations still owe line-control runs, so the round's §8 completion condition is unmet by design rather than by oversight.

- **2026-09-01 (Batch M-core folded)** — The six dispositions the split entry sends to a fresh session landed; the three holds did not move. Two departures from the ledger's `lands as` cells, both recorded on their rows.

  **IF-9 landed narrower than its cell.** The cell reads "cut to the three steps that gate" (steps 2, 4, 5). Steps 1 and 6 were cut and the rest renumbered; **step 3 was kept**, because on inspection it is not the no-op the finder read: it carries two conditional gates — copy the template when creating from scratch and never on a revision, follow the cross-session-state rules when the process spans sessions — and it holds the only link to `references/skill-template.md` in any body, so cutting it orphans that reference against the `check_reference_orphans` the round is adding. The renumber exposed a second defect: `docs/adr/0065-…:29` and `docs/adr/0069-…:28` cited the security lens as "`write-skill` step 5", a pointer by ordinal into a list this round is editing. Both now name the step, which is what a later cut cannot break.

  **AC-15 took the step-down, not the apparatus.** Of the two remedies its cell offers, the rationalization table was refused: ADR-0024 reserves the escalated form for a rule the agent *demonstrably skips under pressure*, and no record carries pressure-skip evidence for `--abort`. No baseline failure is recorded for `--abort` at all — not a skip and not a wrong-shaped output — so ADR-0024's form-to-failure table has no row to send it to, and its default applies: judgment framing, which here is the positive recipe. The line reads as one, with neither of the two clause shapes ADR-0024 bans at every level. What would reopen this is a session that runs `--abort` under pressure with the line in force.

  **IF-198 took the propagation with it.** The `:32` gut-check is cut and `:51`'s "prefer the shape that feels inevitable in hindsight" is now "prefer the shape that lowers the concept count" — the checkable form the row names, already defined at `:28`. Cutting the bullet alone would have left the ungradeable check running the diff-relative bar.

  **What is not claimed.** IF-9, IF-47, IF-174 and IF-220 are cut without a line-control run and are not proven no-ops, exactly as the split entry records. § Deferred's third line stays open: IF-83, IF-113, IF-129 and the three whole-file `global/rules/` nominations still owe Sonnet line-control runs, and no pressure scenario has been built for any of them.

  **`bash scripts/lint-skills.sh` exits 0 and `bash scripts/lint-adrs.sh docs/adr` reports 78 records with no FAIL**, both run at this tree after the fold. Bodies touched: `write-skill` 18,141 → 18,006 B, `codebase-design` 9,872, `handoff` 13,630, `doc-claims` 7,091, `resolving-merge-conflicts` 4,753, `capturing-learnings/references/learning-format.md` 4,389. — corrected: see Amendments 2026-09-01 `write-skill` stays over its own 15,000 B cap; that is the size-and-placement batch's row, not this one's.

  **Batch M-core folded on top of an unreviewed Batch S** (Nick, 2026-09-01), against §0's "Batches A, S and M return to one batch per cycle" — the "unless ruled otherwise" clause, exercised. The review is briefed on every batch in the tree it reads, or it is not this ruling's review. — corrected: see Amendments 2026-09-01

- **2026-09-01 (Batch S folded)** — The scripts batch, recorded here rather than left as a comment inside a selftest. Every item changes what a gate reads or reports, so none of it is re-derivable from a diff alone.

  **`check_sibling_membership` moved from a basename reading to a path reading.** The old test asked only whether a name was grouped SOMEWHERE, so once `ac-ids.md` was grouped for two skills a THIRD copy under a skill the group does not list passed — covered by name, ungraded by path, and invisible to `check_sibling_identity`, which walks the group's listed paths and never sees it. A group gaining a new sharer is the commoner edit in a suite that adds a fifth and sixth publisher to a group of four, so it is the case that has to fire. Backwards-incompatible: any repo carrying an ungrouped shared reference basename that passed before now FAILs.

  **The evaluation-ledger consumer FAIL read a masked stream and a raw one.** `hits` counted masked tokens while `missing` re-grepped the raw file, so a consumer naming two statuses in prose and the third only inside a fence rendered "and not ``" — an empty pair of backticks where the missing name belongs.

  **The `docs/**` spelling walk was a pipe into `while`.** Its loop body ran in a subshell, `fail=1` died with it, and every FAIL it printed left the exit status at 0 with `OK: skill conventions clean.` beside it — so an ADR carrying a British form committed and merged green past `pre-commit` and `post-merge`, both of which read the status alone. It is a process substitution now, and the comment above it names the signal.

  **The `security.sh` file cap is charged after classification**, on the files the walk opens, closing the first of the three bypasses [ADR-0075](0075-transcript-sourced-mining-round.md)'s batch-1 entry recorded as accepted; that record carries the amendment its own line asked for.

  **`setup-hooks.sh` redefines what it names as a live git hook** — a file under `scripts/git-hooks/` whose first line is a shebang, `scripts/README.md`'s definition, in place of "every file that is not a selftest", which counted a README or a `.gitignore` beside the hooks and named it in the TRUST NOTE banner as something that now executes.

  **`selftest-lib.sh`'s `selftest_close` header was wrong about its own function** — it prints in the PARTIAL and OK cases only and is silent on FAIL, where the header implied a close line on every path. Comment lines only; the red arm's behavior did not change.

  **`skill-usage.sh` names an absent source per side** and marks that column `0+`, so a mistyped `--history` or `--projects` no longer reads as a clean zero on a retire-on-zero window; the exit-code taxonomy moved with it.

  **`review-receipt-selftest.sh` states this hook's scope and grades it** — the hook reads ONE line out of a `Landing:` block, so a block malformed in every other key still gates, and a well-formed block whose `Review required:` reads `no` does not.

  This batch folded before Batch M-core and was reviewed with it. The `77 → 83` figure the M-lint entry below records is M-lint's delta on top of this batch; `git show HEAD:scripts/lint-skills-selftest.sh` at the time pinned **74**, and without this entry the 77 was a number nobody could re-derive.

- **2026-09-01 (Batch M-lint folded)** — The six verification gaps the Batch A fix pass routed here landed, each with a fixture instance that fires and a mutation that reds its selftest. What is worth recording is where three of them did not land the way the row asked.

  **The push-gate row asked for a repo-specific check and got a general one.** §9's row reads "this repo's own push gate hanging on one unguarded `CLAUDE.md` line", and the first draft was exactly that: a `check_own_review_gate` asserting this repo's `Review required: yes`, skipped under `LINT_ROOT` the way `check_sibling_identity` is. That check is ungradeable — the selftest cannot reach a branch that only runs against this repo — so a gap closed by an unproven check is not closed. It became an arm of `check_landing_key` instead: **a `Landing:` block that exists names the key**, whatever its value. A repo that does not gate says `no`; a repo that does cannot lose the line silently. That reading is general, holds for any repo the linter gates, and is graded in an isolated copy of the clean fixture root with the line deleted.

  **It is stricter than `committing`, and the two sentences now say which is which.** `committing` reads a missing `Review required:` line as `no` — that is how to read an arbitrary repo, and it stands. The check governs what a repo this linter gates must write. Both headers carry the distinction, because a reader meeting one of them alone will read a contradiction.

  **The ordinal-pointer check was measured and declined.** The M-core session's §9 row routed one here. The narrow reading of the pattern — a backticked skill name within 20 characters of `step N` — finds one live cross-file site; the wide reading finds 24, and 17 of those are a body citing its own step on a line that happens to name a sibling. A check firing on 17 correct lines is not a gate, so the class is closed by convention: `write-skill` § Review checklist's security row names the step rather than numbering it, and says why on the row. All fourteen cross-file ordinals in the corpus were then opened against their targets and **every one resolves** — including `0066:52`, whose ordinal belongs to `harden-domain` (now `sweep-domain` § 4) and not to `domain-modeling`, correcting this session's own first reading of that line.

  **M-core's renumber had broken a third pointer, and this batch's sweep found it.** `write-skill:86` still read "Step 5 run where a security surface exists" after IF-9 moved the security step to 4. The M-core session repaired the two ADR pointers it knew of and never swept its own file. Fixed here under `Defect policy: fix, don't file`.

  **`install.sh` left the selftest-exempt list**, which is what made its two `rm` loops gradeable at all: `install-selftest.sh` runs the real installer against a redirected `HOME`, so `SKILLS_DIR` and `GLOBAL_DIR` still point at this checkout while every write lands in a throwaway tree. `setup-hooks.sh` stays exempt — it writes into `.git/hooks` and has no readable surface without a throwaway clone — so `scripts/README.md` now names one script rather than "an installer". The fresh-install row that came out of the mutation run is the load-bearing one: with `prune_stale`'s `[ -L ]` guard dropped, a first install on an empty target exits 1, because the unmatched glob reaches a `readlink` whose failure `set -e` reads.

  **`bash scripts/lint-skills.sh` and `bash scripts/lint-adrs.sh docs/adr` exit 0, and all twelve selftests exit 0**, run at this tree after the fold. The two FAIL-count pins moved with the reason on them: `lint-skills-selftest.sh` 77 → 83, `lint-adrs-selftest.sh` 23 → 27.

- **2026-09-01 (Batch S + M fix pass)** — The review of Batches S, M-core and M-lint returned 66 findings. This entry records what they corrected in the round's own record and what the fix pass changed in the gates; the ledger of dispositions is the report's.

  **The `Landing:` key check was not fit to land as written, and four of its defects were the same defect.** It read the file unmasked, so a `Landing:` block shown inside a fenced example consumed the whole check — including the deletion arm this record calls the one that matters; it latched on the first header and never resumed, so a second block went unread; it accepted only `- ` at column 0, where the hook it protects accepts `*` bullets, indentation and a `>` prefix; and it blessed `Review required: yes (planned)`, the one value that silently disarms that hook, whose own selftest already staged it. All four are fixed: the check reads the `FENCE_AWK` stream every sibling check reads, a block ends at a blank line or a non-bullet and a later header starts another, bullets are read the way `global/hooks/review-receipt.sh`'s `OPT_IN` reads them, and `Review required:` has an arm of its own taking a bare `yes` or `no`. The FAIL now quotes the author's line rather than a normalized rendering of it. Ten mutations were run against the result and nine went red; the tenth — narrowing the gate grep alone — is silent by construction and `lint-skills-selftest.sh`'s header says so.

  **A committed fixture had disarmed this repo's own push gate.** `scripts/lint-fixtures-clean/CLAUDE.md` carried a live `- Review required: no`, and `review-receipt.sh` walks upward from the push directory, so `git -C scripts/lint-fixtures-clean push` was allowed silently from a repo root that blocks. The fixture now reads `yes`; the `no` value is exercised in a throwaway copy instead, through a new `isolated_quiet_case` helper that asserts an edit the check must accept draws no FAIL and leaves the run at 0.

  **`install.sh` had left four of five exemption sites.** `scripts/git-hooks/pre-commit` still exempted it, so the selftest that made its two `rm` loops gradeable never ran at commit time. Fixed, and the exemption is now one name, `setup-hooks.sh`, stated as current truth at every site rather than as the history of the change.

  **The `slash-exempt` marker is line-scoped in the code as well as in the prose.** It was documented line-scoped in two places, implemented file-scoped, and its fixture pinned the loose reading — so one marked site blessed every later dangling name in the same body, which is the rename the check exists to catch. It now matches per line, the way its sibling `spelling-exempt` does, and the fixture pins both directions. `tmp` moved to the `slash_exempt` roster, which reverts the two markers M-lint had inserted mid-sentence into `handoff` and `review-architecture` — a lint token in prose that agent briefs must quote verbatim.

  **Five ungraded gates now have a grade.** The widened sibling-membership check's quiet arm is unreachable from any fixture root — `sibling_groups` names this repo's own paths — so `lint-skills-selftest.sh` runs the linter against the real tree for that one row. The relocated file cap in `security.sh` had no row asserting it fires; 220 scannable files now charge it. `install.sh`'s `local` fix is graded by checking every `dep X -> Y` line against X's own frontmatter, so a rename cannot turn the row into a no-op. `lint-adrs.sh`'s three awk-fronted `read_rows` arms read `$?` after a pipeline, which is the downstream command's status; they read `${PIPESTATUS[0]}` now. And `scripts/README.md`, the pass-4 spelling walk's third producer, was in neither fixture root and is now in one.

  **`— corrected:` on the A5 entry, second claim.** That entry lists `check_spelling` among the eight checks that "landed as FAILs", and the Batch A fix pass wrote "All are graded now". Both were false of the `docs/**` half: every FAIL that walk printed left the exit status at 0, because the walk was a pipe into `while` and its `fail=1` died with the subshell — so an ADR carrying a British form committed and merged green. Batch S fixed the subshell; the walk is gated only from that tree onward, not from A5.

  **`— corrected:` on the A fix pass's byte list.** Two of its six figures were wrong at the tree it named, and both are the two M-lint edited: `write-skill` was 18,123 B, not 18,006, and `handoff` 13,657, not 13,630. Re-measured at this tree after the fix pass: `write-skill` 18,454 · `codebase-design` 9,872 · `handoff` 13,877 · `doc-claims` 7,091 · `resolving-merge-conflicts` 4,771 · `learning-format.md` 4,783. Three of the six grew, because three rules the pruning test ruled restore-or-name-the-gap were restored: `handoff`'s argument line, widened to the whole document rather than the goal bullet; `learning-format.md`'s YAML-quoting rule, whose covering rule was in no file that skill loads; and `write-skill`'s show-the-draft step. `write-skill` is further over its own 15,000 B cap, which stays the size batch's row.

  **The `amended:` marker is scoped as `adr-format.md` defines it.** `lint-adrs.sh` read it anywhere outside the `## Amendments` log, including inside `## Consequences` and `## Deferred` — where it would let a bullet skip the `— corrected:` check the two-marker distinction rests on. It now excludes both sections, and `9042-amended-no-amendment.md` carries a quiet instance in each.

  **`DOMAIN.md` was the last place asserting the two-marker rule**, and it now registers the third; the **Landing key** row says the block is read per directory rather than per repo, which is the fact that would have prevented the fixture defect above; the `N+` count floor admits its second sense, an absent source; `argument-hint:` is registered as a frontmatter key; and a tightening round's deletion-tier cell joins the stored-status sets.

  **Six of the report's findings were the author's call and Nick took all six as recommended** (2026-09-01). `check_landing_key` now walks every `CLAUDE.md` in the tree rather than the root one alone, the fixture roots excluded on the rule the `docs/**` walk already states for the fixture READMEs, with a `packages/api/CLAUDE.md` below the fixture root grading the walk. `install.sh` takes `TARGET_ROOT="${TARGET_ROOT:-$HOME}"` — every path it WRITES to hangs off one declared input, so the seam its selftest's whole safety case rests on is an interface this file offers rather than a property the selftest asserts about it, and the selftest checks the redirection took before reading any row. `write-skill` § Workflow gets its show-the-draft step back: it is user-invoked, nothing else in the body or its seven references puts the draft in front of the person who triggered it, and no covering rule was ever named for that cut. Batch S has the entry above. "Control run" is qualified as **line-control run** at every site in this record and at the two bare sites in `DOMAIN.md`, so the session that reopens IF-83, IF-113 or IF-129 runs the scenario minus the line rather than the micro-test's whole-skill control, which would show the failure the skill exists to prevent and settle nothing. And § Deferred's third line admits the third state its two-state form could never name — deleted on the diagnostic with the line-control named as not run — which is where IF-9, IF-47, IF-174 and IF-220 landed.

  **Two findings are deferred to a `scripts/` shape batch and one is declined**, all three ratified and written into the round plan's §9: the two `install.sh` prune loops merged into one `prune_owned` (F12), `say_fail` made to survive a subshell so the four repeated hazard paragraphs die with it (F57), and the `is_git_hook()` extraction declined because `setup-hooks.sh` is the bootstrap and sources nothing (F13, whose false clause about `post-merge`'s roster is fixed).

  **`bash scripts/lint-skills.sh` and `bash scripts/lint-adrs.sh docs/adr` exit 0, and all twelve selftests exit 0**, run at this tree after the fix pass. The two FAIL-count pins moved with the reason on them: `lint-skills-selftest.sh` 83 → 88, `lint-adrs-selftest.sh` unchanged at 27, the two new fixture markers being quiet ones.
