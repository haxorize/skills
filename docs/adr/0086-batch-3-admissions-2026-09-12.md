# The 2026-09-12 round's batch 3 admits material from twelve outside sources, all of it as local prose

## Context

Batch 3 of the 2026-09-12 round landed on 2026-09-13 as one commit, `0c7d559`, folding fourteen rows from `~/code/lib/_rounds/2026-09-12/reconcile.md` § Batch 3 into fourteen hosts as one clause each — 16 paths (`git show --stat --format= 0c7d559 | tail -1` → `16 files changed, 36 insertions(+), 17 deletions(-)`). The grill (`grill-outcomes.md` decision 9) fixed the family: drop `PM-A6`, `PM-A7`, `A19`; the three `tdd` rows land as one clause; keep the rest. Two of the kept rows did not land, for reasons this record names, and one host the reconcile called "not near a cap" was 9 B under it. This record is written before the batch family's review, inside the same unpushed range, as [ADR-0084](0084-batch-family-6-7-admissions-2026-09-04.md) required of its batch family: the admissions are recorded beside the commit they describe, and the review reads the record with the diff. It is one record per batch, the option ADR-0084 rejected when the source was the review unit; this round's reconcile (§ 1: each batch is one review family) makes the batch the unit, so `gnurio/pm-os` is a bullet here and in [ADR-0087](0087-batch-4-admissions-2026-09-12.md) both.

## Decision

The admissions of batch 3 are recorded here, one bullet per outside source; the round's closing ADR points here rather than restating them. Every source fed local prose — no directory was ported — so **no diff-before-editing obligation attaches to any source here**. The trigger did fire on the first-table hosts the commit edited — `codebase-design`, `tdd`, `diagnosing-bugs`, `writing-for-agents`, `review-changes`, `review-architecture` (mattpocock/skills), `receiving-review` (obra/superpowers), `diverging` (oaustegard/claude-skills), `discoverable-code` (dmmulroy/dotfiles), `onboard-me` and `rebuild-contract` (nitfolio/nirvajna-skills), `capturing-learnings` (compound-engineering-plugin) — and was discharged by [ADR-0034](0034-branch-mining-lineage-or-dormant-main.md)'s 2026-09-13 sweep: zero commits at mattpocock, obra, and nitfolio; twenty-three at oaustegard with `generative-thinking` untouched; five at dmmulroy with `write-discoverable-code` unchanged, and that file byte-identical to the modem-dev/skills mirror (`diff` empty, this session). `capturing-learnings` was first listed as discharged by that upstream's deletion; the family's fix pass (review F1) found the upstream alive and ran the read — 39 commits, the learnings loop restated and extended upstream, nothing to fold — recorded in ADR-0034's corrected entry.

- **reviewstage/stage-cli** (MIT; clone `reviewstage-stage-cli`, tip `59b977b` of 2026-09-06) — ledger `CR-1.6` (`trade-off`). Taken: when a plan or diff has outgrown its task, the cuts come in order — user-facing behavior the task never asked for, then the infrastructure only that behavior needed — and stop before code-level tidying. Local: `src/codebase-design/SKILL.md` § Judging a change, one sentence.
- **duyetpt, gist `f9bcc40c`** (no license, NONE-STATED; clone `duyetpt-gist-f9bcc40c`, tip `6063252` of 2026-08-15) — ledger `N-B7` (`measure-software-simplicity.md`), **ideas only**. Taken: a design claim is graded by the evidence under it, in a fixed descending order, and the confidence tier is stated apart from the verdict. The 0–3 scoring per principle was not taken. Local: the same section, the sentence after.
- **thebushidocollective/han** (FSL-1.1-ALv2, `LICENSE` read this session — a source-available license, not MIT or Apache as the reconcile's license line assumed; clone `thebushidocollective-han`, tip `19caa51` of 2026-09-08) — ledger `CR-1.18` (`legacy-code-safety`), **ideas only**. Taken: code that arrived with no tests gets a characterization test before a slice changes it. Local: `src/tdd/SKILL.md` § Closing the cycle, the new paragraph's last sentence.
- **cursor/plugins, the `pstack` directory** (MIT, `pstack/LICENSE`, Lauren Tan; clone `cursor-plugins`, tip `889ec4b` of 2026-09-11; already a second-table upstream through ADR-0084's `V13` row) — ledger `DCB.1` (the stub-everything litmus; name-matched to `pstack/docs/guide/08-principles.md` and `pstack/skills/poteto-mode/SKILL.md` by `grep -rl 'returned undefined'`). The reconcile's license line read it NONE-STATED; the directory's own `LICENSE` is MIT. Taken: the one question — would the test still pass if every import returned nothing — and the five shapes that answer yes. Local: the same paragraph, its opening sentences, where the tautological shape points at the Philosophy sentence that already owns it.
- **Neeeophytee/finding-unknowns-skills** (MIT; clone `Neeeophytee-finding-unknowns-skills`, tip `6d7dda2` of 2026-09-09) — ledger `D3.4` (`test-blindspots`). Taken: a double standing in for the very boundary the test claims to cover is the sixth yes. Local: the same paragraph, one clause; `references/untested-at-close.md` unchanged, since the grill made the three rows one clause in the body.
- **mohitagw15856/pm-claude-skills** (MIT; clone `mohitagw15856-pm-claude-skills`, tip `f67821d` of 2026-09-04) — ledger `CR-4.50` (`ai-code-review`) and `CR-2.9` (`rollback-plan`). Taken for `CR-4.50`: three failure shapes of agent-written code — an API the author's memory supplied, logic that reads right and computes wrong, scaffolding nothing reaches; the silent security shortcut was not taken, since the verification-gap lens already grades a silenced check. Local: `src/review-changes/references/lens-briefs-code.md` § Agent-authored code. Taken for `CR-2.9`: the rollback's data-safety class — reversible, forward-only, destructive — written with the trigger, and a reverse that is not reversible rehearsed in a lower environment first; the exact-reverse-command sequencing and the comms template were not taken (trigger, baseline, and window are already `H38`–`H41`'s). Local: `src/ship/references/after-landing.md`, the rollback bullet; one clause in `src/ship/SKILL.md` step 4 naming the class where the trigger is written.
- **smallnest/goal-workflow** (MIT; clone `smallnest-goal-workflow`, tip `284333e` of 2026-09-06) — ledger `CR-6.4` (`smell`) and `CR-6.1` (`code-to-spec`, unparked by grill decision 7). Taken for `CR-6.4`: the smell catalog reads the same over a tree as over a diff, and work that grows with the data — a query in a loop, a nested walk, a sort repeated per item — is a friction to look for; the timestamped report was not taken (the HTML report already exists), and the 2026-09-04 `L6` evidence guard rides `finding-discipline.md` as before. Local: `src/review-architecture/SKILL.md` § 2, one bullet reusing `review-changes/references/smell-baseline.md`. Taken for `CR-6.1`: the inward spec as a deliverable — architecture, contracts, data model, for a reader who will open the source — on request only. Local: `src/onboard-me/SKILL.md` § Notes, a new subsection, and its read-only section's second exception (`docs/inward-spec.md`); `src/rebuild-contract/SKILL.md` § Where this skill ends, one clause naming the sibling. `onboard-me` measured 10,029 B before the fold and 10,897 B after.
- **wanshuiyin/Auto-claude-code-research-in-sleep** (MIT; clone `wanshuiyin-Auto-claude-code-research-in-sleep`, tip `f1bd907` of 2026-09-11) — ledger `D3.1` (`web-debug-search`). Taken: a web search seeds a hypothesis and is never evidence for one; the exact string first, at most one generalized variant after. Local: `src/diagnosing-bugs/SKILL.md` § Phase 3, one sentence after the Learning-doc seed.
- **gnurio/pm-os** (no license, NONE-STATED, a sold product; clone `gnurio-pm-os`, tip `3aeba89` of 2026-09-12) — ledger `PM-A11` (`metric-drop`), `PM-A12` (`feature-writeup`), `PM-B10` (`verbalized-sampling`), all **ideas only**. Taken for `PM-A11`: a metric regression runs the diagnosis loop with the control chart as the red-capable signal and segmentation as the minimizer. Local: `src/diagnosing-bugs/SKILL.md` § Phase 1, one paragraph. Taken for `PM-A12`: a results writeup is verified only on the whole population with confounders named, never on a segment chosen after the fact. Local: `src/capturing-learnings/SKILL.md` § The capture gate, criterion 1. Taken for `PM-B10`: verbalized sampling as a second technique for near-duplicate collapse, with the probability spread as its fired-if test; the four-question critique pass was not taken (`grilling`'s). Local: `src/diverging/SKILL.md` § The moves, the Random stimulus paragraph.
- **dmmulroy/dotfiles** (no license stated, first-table under [ADR-0050](0050-twelve-repo-mine-and-discoverable-code-port.md); tip `96dbc32` of 2026-09-10) — ledger `D2.5`, the folders-as-map clause the delta reader found in the mirror's diff. Taken: a directory listing is the first search in an unfamiliar repo, so folders group by feature, domain, or layer while the search-hit context stays in the symbol. Local: `src/discoverable-code/SKILL.md` § Names are search queries, the folder bullet. No new lineage row: the skill is already first-table under this upstream.
- **will-ness/ai-skills** (MIT; clone `will-ness-ai-skills`, tip `71d8909` of 2026-08-21) — ledger `D5.1` (`skills/engineering/triage/SKILL.md`). Taken: a comment or issue an agent posts under its own identity opens with a line saying it was AI-generated; the outward-act gate decides whether, this decides what it discloses. Local: `src/committing/SKILL.md` § Before any outward act, one paragraph.
- **OpenAI, "Rethinking skills and prompts for GPT-6 Astra"** (a blog post, copyright; reached through `dr_cintas`'s tweet `2096670194340901328` and two paraphrases, all in the urls report) — ledger `U9`, **ideas only**. Taken: a model upgrade is itself a re-audit trigger for instruction files. Local: `src/writing-for-agents/SKILL.md` § Pruning, the **No-ops** bullet — not the Scaffolding bullet the ledger named, because what changes at an upgrade is what the reader does unasked, which is the No-ops diagnostic. The six flag categories were not taken (each already a named failure mode).
- **Local, no source** — ledger `T6` (transcript friction; grill decision 11): at the fix pass's close, `git diff --stat` decides whether to offer a re-review — a file the review never graded, or more than half the original change's line count — and the offer is never a start. Local: `src/receiving-review/SKILL.md` § One fix pass. Nothing to attribute.

**Not landed.** `CR-4.22` (ksimback/tech-debt-skill, NONE-STATED): the "looks bad but is actually fine" section is already mandated at `src/review-architecture/SKILL.md:73` — the close "vetted and not proposed", one line per dropped friction with its drop class, by-design among them — so the fold fails the pruning test on **Single source of truth**; the row reads REJECT-covered from this record. The `chart-course` intake line (grill decision 13) points at `discovery`, which Batch 7 creates; it lands there.

**One relocation.** `diagnosing-bugs` sat at 14,991 B, 9 B under the cap the reconcile said this batch never approached. Two blocks moved to `references/hard-cases.md` behind one-line pointers: the why-chain paragraph into § Post-mortem branches, and the correct-seam definition into a new § No correct seam. The body is 14,799 B after both folds. Local prose moved, nothing admitted.

## Considered Options

- **Land `CR-4.22` as a sharpening of line 73.** Rejected: the line already says "close the list with what was vetted and not proposed" and names by-design as a drop class; a second sentence saying it again is the duplicate the pruning test exists to catch.
- **Put `U9` in the Scaffolding bullet as the ledger proposed.** Rejected: Scaffolding asks whether tooling can see the failure, which an upgrade does not change; No-ops asks what the reader does unasked, which is exactly what an upgrade changes.
- **Three `tdd` edits — the body, `untested-at-close.md`, and a characterization section.** Rejected by grill decision 9; one paragraph in § Closing the cycle carries all three, and the reference is unchanged.

## Consequences

- `docs/lineage.md`'s second table gains rows whose `Record` cell is this record, and its header sentence names this record beside ADR-0082 through ADR-0084.
- The round's closing ADR points here for batch 3.
- The reconcile's license line for Batch 3 ("the rest MIT/Apache") is corrected by this record: `thebushidocollective/han` is FSL-1.1-ALv2 and `cursor/plugins`' `pstack` is MIT, so `CR-1.18` is ideas only and `DCB.1` need not be.
- `diagnosing-bugs` has 201 B of headroom; the next fold into it relocates first. — corrected: see Amendments 2026-09-13

Revisit when: an ADR-0034 swept-point diff shows one of this batch's twelve upstreams changed the idea a row here took as local prose, which reopens that row's fold on its merit; or `thebushidocollective/han` relicenses from FSL-1.1-ALv2, which lifts `CR-1.18` from ideas only; or `diagnosing-bugs` takes a fold without relocating first; its headroom is 211 B at `8e3f363` (`wc -c src/diagnosing-bugs/SKILL.md` → 14789), not the 201 B the consequence above measured before ADR-0087's prune.

## Amendments

- **2026-09-13, review fix pass** — The headroom bullet's 201 B predates the prune ADR-0087's amendment of this date records; `wc -c src/diagnosing-bugs/SKILL.md` → 14789 at `8e3f363`, 211 B under the cap. The Revisit line, added the same day from the bullet rather than from a measurement, is restated above at the measured figure.
