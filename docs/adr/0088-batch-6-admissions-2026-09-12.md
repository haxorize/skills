# The 2026-09-12 round's batch 6 closes the 2026-09-04 park backlog and admits two one-sentence ideas, both as local prose

## Context

Batch 6 of the 2026-09-12 round is the park-sweep batch: `~/code/lib/_rounds/2026-09-12/park-sweep-2026-09-04.md` re-checked the 107 PARK rows of the 2026-09-04 ledger plus five ids the intake named (112 rows) against the tree at `0ae7f4b`, and `reconcile.md` § Batch 6 assigns each verdict a disposition — MET rows promoted, CLOSED-ALREADY rows closed, two KILL, the UNMET rows re-parked, and the PENDING-ROUND rows checked against this round's evidence and re-parked where none was found. The same pass settles the ledger rows the round's deferrals register had pointed at "the reconcile's next ledger pass" (`CR-4.22`, `PM-A9`, `CR-5.49`, `CR-5.5`, `PM-A3`, `CR-1.25`, `CR3.8`, `PM-C10`). Almost all of it is record work in the round directory; two rows change skill text, and this record exists for those two, as [ADR-0086](0086-batch-3-admissions-2026-09-12.md) and [ADR-0087](0087-batch-4-admissions-2026-09-12.md) did for batches 3 and 4, written inside the same unpushed range and before the batch's review.

## Decision

One bullet per outside source; every source fed local prose, so **no diff-before-editing obligation attaches to either source here**. One host, `codebase-design`, is a first-table row (mattpocock/skills); the trigger was discharged 2026-09-13 at write time: `git -C ~/code/lib/mattpocock-skills fetch origin && git log --oneline 3cca18b..origin/HEAD | wc -l` → `0`, so [ADR-0034](0034-branch-mining-lineage-or-dormant-main.md)'s 2026-09-13 swept point still holds.

- **affaan-m/ecc** (MIT, `LICENSE` in the clone; the 2026-09-04 round's tip `e04ea0b`, the 2026-09-12 update logged the clone `fetchfail` — it is no longer a git checkout, and the file was read from disk) — 2026-09-04 ledger `D4.11` (`skills/github-ops/references/ecc-release-checklist.md`), a PARK whose unpark condition the sweep read **MET** on its second disjunct: `ship/references/after-landing.md` landed 2026-09-04. **Ideas only.** Taken: a release is a tag switch, not a publish — publish under a staging tag, read the registry's integrity back, point `latest` at it; the rollback is re-pointing the tag, which is the reversible class, and unpublishing is the destructive class and never the plan. Not taken: the `E404`-only lookup rule, the provenance flag, the dated state block, the package-specific checklist. Local: `after-landing.md` § The data-safety class, one sentence, placed where the rollback's class is chosen because that is the decision the idea changes.
- **gnurio/pm-os** (no license, NONE-STATED, a sold product; clone `gnurio-pm-os`, tip `3aeba89` of 2026-09-12) vendoring **rjs/shaping-skills** (`breadboard-reflection`, marked `pm-os:external-skill`; NONE-STATED in `licences.tsv`) — ledger `PM-A3`, the one ADAPT residue the "Batches 3–4 fold the ADAPT residue" deferral left unfolded, **ideas only**. Taken: never judge a design against an artifact until the artifact is resynced to ground truth. Not taken: the breadboarding notation, the affordance tables, the SEE-then-REFLECT procedure. Local: `codebase-design` § Judging a change, one sentence, with the finding named as the artifact's drift rather than the design's — the local reason the ordering rule matters, which the source does not state.

**Closed without skill text** (the record is `ledger.md` § Park sweep and the rows named): `M3` (ADR-0079 names the ceiling), `P3` and `P8` (verbatim in `recap-from-transcript.md`), `A44.S3` (`explain:17`), the 08-27 `M5` (both scripts exit 2 on nothing-to-check), the six CLOSED-ALREADY rows, `G4` KILL (foreclosed by [ADR-0076](0076-claude-md-admits-triggers-and-contracts-only.md)), and `Standing 1` KILL on Team-fit — the 08-27 campaign artifacts are the mining suite's own writing task, not a product-engineering moment.

**Not landed.** `CR-4.22` (the required looks-fine-despite-appearances section) reads REJECT-covered from ADR-0086: `review-architecture:73` already mandates the by-design close. `PM-A9` reads REJECT-covered from ADR-0087. `CR-5.5`'s remainder — the order of operations and the burned-us list — is REJECT: its only proposed host was the `incident` skill the 2026-09-12 grill refused (decision 8); the triage half landed in Batch 3. `CR3.8` is REJECT as a duplicate of the still-parked `L11`; `PM-C10` is REJECT on the orchestration class, its PARK ground having been `PM-B13`'s, which the grill killed (decision 13). `CR-1.25` stays PARK with its condition made explicit: this team runs agents in CI.

**Re-parked.** Six rows were PENDING-ROUND. Five (`D4.26`, `D4.27`, `D4.28`, `D5.1`, `Standing 5`) had no evidence in this round and are re-parked: the transcript reports carry no re-run streak, no evidence-free live claim, and no dropped-figure correction (`grep -n -i 'streak\|re-run\|dropped figure\|blind' reports/mine-2026-09-12-transcripts*.md` → three hits, none of those shapes); `usage.tsv` shows `validate-behavior` typed once in the window (2026-09-05), so the second 0-typed window `Standing 5` waits for has not opened. The sixth, `D1a.1`, resolved at the grill (decision 2: not met; the friction log and `/retro` are Batch 8's). The 92 remaining UNMET rows (the sweep's 93, less `Standing 1`, whose Team-fit KILL flag the reconcile took as a KILL) are re-parked with their conditions as written, now as rows in the ledger rather than a count.

## Considered Options

- **Flip `PM-A3` to REJECT rather than land the sentence.** Rejected: `codebase-design` is 11,424 B after the fold with no cap pressure, and the ordering rule names a real reviewer failure — a design smell reported against a diagram nobody updated — that no local line covered.
- **Row `CR-5.5`'s burned-us list as a new candidate.** Rejected: a standing list of past failures is `capturing-learnings`' index by symptom, already landed; nothing is left to row.
- **Leave the UNMET rows as a count in the ledger, as the assembler did (93 at the sweep's count, before `Standing 1`'s KILL flag was taken as a KILL; 92 after).** Rejected: the reconcile's Batch 6 line says re-recorded with their conditions, and a count is not a record a future round can re-walk without opening the sweep.

## Consequences

- `docs/lineage.md`'s second table gains two rows whose `Record` cell is this record. Its header sentence does not name this record: that enumeration lists records most of whose named skills hold a row in both tables, and of this record's two hosts only `codebase-design` does — `ship` is second-table only.
- The round's closing ADR points here for batch 6.
- `after-landing.md` stands at 3,942 B and `codebase-design` at 11,424 B (`wc -c`); neither is near a cap.
- The 2026-09-04 park backlog is closed as a batch: every one of the 112 rows carries a disposition in `ledger.md` § Park sweep, and the next round's park sweep starts from that table.

Revisit when: the next park sweep re-opens a row this batch re-parked and finds its condition met by a change that landed before `0ae7f4b` — a sign the sweep's re-check, not the condition, was wrong; or the two sentences landed here are cut from `after-landing.md` or `codebase-design` for size, which reopens `D4.11` and `PM-A3`.
