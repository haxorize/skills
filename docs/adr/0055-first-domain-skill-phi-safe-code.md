# First Domain skill admitted: `phi-safe-code`, in plain `src/`

Status: accepted (2026-08-22)

> **Amended by [ADR-0060](0060-third-domain-skill-accessible-ui.md):** the third admission re-decided placement (flat in `src/`, re-opened by an observed loop cost rather than a count) and fixed which model prong (a) measures.

The suite admits its first **Domain skill**, working name `phi-safe-code`: the discipline of keeping protected health information out of logs, fixtures, error messages, URLs, analytics events, and model prompts, with secure application logging as a section of it rather than a skill of its own. Three independent mining reads on 2026-08-22 (the everything-claude-code family, the security packs, and the awesome-list inventory) scored it against the Gap-and-stakes test and all three prongs held: the model's default code leaks identifiers into those surfaces unless told otherwise, the failure is a reportable breach, and every team that touches member data would load it. No source pack supplies the skill; the packs are clinic-shaped (EMR, ICD-10, Supabase RLS) and are mined for seed rules only.

The alternative was a separate Humana-only repo holding every Domain skill, keeping this suite purely process-shaped as its `CLAUDE.md` describes it. A rational team would choose that, and it lost on one point: the Gap-and-stakes test admits only gaps that generalize past one employer (any payer, any regulated organization), and a second repo with one skill in it would not be installed. The org-specific layer, the allowed log-field list and retention figures, stays outside the suite as a project-conventions skill.

Placement is plain `src/<name>/`, flat beside the process skills, with no new frontmatter and no new tier. `install.sh` and every lint loop glob `src/*/SKILL.md`; a nested tier would cost lint, install, and selftest changes before the first body exists. The decision is provisional: a third admitted Domain skill re-opens it, with three bodies to move instead of none.

## Deferred

- The final name: B4b's rename table judges `phi-safe-code` on the stranger-at-autocomplete criterion with every other name. — settled: see Amendments 2026-08-22
- Health-literacy copy as the second Domain skill: admitted on hypothesis; the off-path-and-domain admissions session checks prong (a) against 5 member-facing samples before an authoring batch is named. — settled: see Amendments 2026-08-22
- Description triggers: the body must fire on payer vocabulary (member, subscriber, claim, eligibility, 834/837, MRN) and on the act (logging, fixtures, error text, analytics, prompts), never only on "PHI" or "HIPAA"; the B9 micro-test checks this. — settled: see Amendments 2026-08-22

## Consequences

- The suite is no longer purely repo-agnostic in subject matter; it stays repo-agnostic in mechanism. `CLAUDE.md`'s first line needs the qualifier when B9 lands.
- Two guards bind B9: no org policy numbers in a `src/` body, and no `references/` file that is a regulation digest (that is a pack, which the test rejects).

## Amendments

- **2026-08-31 — the B9 citation inventory corrected, and the NPI anti-trigger re-worded without a re-test.** `§164.312(b)` has never appeared in `src/phi-safe-code/SKILL.md`: `grep -n "164\."` returns one hit, `§164.514(b)(2)` at :8, and `git log -S "164.312"` over the file returns nothing, so the B9 amendment certified a property no version had. The inventory is also not exhaustive in the other direction — :8 additionally cites 42 CFR Part 2, CMIA, and MHMDA, and § Malformed input cites TA1/999/277CA and IK404. The guard the sentence certifies (no org policy number in the body) does hold. Separately, the anti-trigger this record fixed the NPI over-fire with now reads "Not for provider-side data (NPI, facility records) — public", against the recorded "(NPI, provider directory, facility records), which is public": `provider directory` was dropped in two uncredited steps and ADR-0075's batch-18 drop list names neither. This record's own rule for the clause is re-test, not re-word, so the shipped wording is owed a probe re-run rather than a restoration; nothing in the body changes on this amendment.

- **2026-08-22** — The second Domain skill, health-literacy copy, is admitted on the prong-(a) check this record deferred. Five member-facing samples drafted without the sources in view (a denied-claim notice, an EOB "amount you may owe" block, a prior-authorization message, a deductible-reset email, an eligibility-lookup error) all failed at least 2 substantive criteria and 4 of 5 failed at least 4; 4 of 5 read above grade 8, 2 near grade 14. The failures were the domain's own: insurer terms left undefined in every sample, the action line passive in 4 of 5, what to do next buried or absent in 3 of 5, money shown as a prose formula rather than a figure. The authoring batch is B10, after B9. Placement stays plain `src/`; the third admission re-decides it. Sources: the cms.gov guidelines page and the plainlanguage.gov word list, both public domain, are quotable; the CMS Toolkit for Making Written Material Clear and Effective is cited by name for its rules (define a term in the sentence where it occurs, show a number the reader can relate to, always say how to get help) and never quoted, because it was written by a contractor and its public-domain status is unverified. Prong (c) was not tested by the samples; the B10 description must fire on the prompts an engineer types (error messages, in-app notices, email and letter templates held in code), never only on "health literacy".
- **2026-08-22** — `phi-safe-code` is the final name, and the B10 skill is named `health-literacy`; both judged in [ADR-0057](0057-rename-table-and-discipline-skill-term.md)'s table, closing the first Deferred line.
- **2026-08-22, on landing B9** — The third Deferred line, the description triggers, is closed on the micro-test's result: four fresh-context probes given only the roster of model-invoked descriptions and one realistic user message. "Add logging to the 834 loader so we can see why enrollments are getting dropped" and "the eligibility service tests are thin — build out some seed data from the QA export" both loaded the skill, with the word PHI in neither prompt, which is the requirement this record set: payer vocabulary plus the act, never only "PHI"/"HIPAA". A billing-PDF probe outside the data domain stayed correctly quiet. The fourth, an NPI/provider-directory rename, **over-fired** with a stated rationale the description licensed, so the description gained the anti-trigger "Not for provider-side data (NPI, provider directory, facility records), which is public" — an NPI is a public NPPES identifier and this body governs member and patient data. Two further probes then ran against the **corrected** description, because the first four had tested wording that no longer existed: "the 837 intake job keeps choking on a handful of claims each night — add enough logging around the parser that we can tell which ones and why" loaded the skill on 834/837 plus the logging act, and "rename the NPI lookup endpoint to `/providers/search`, and make the not-found error message say which NPI was missing" did **not**, the probe quoting the new anti-trigger back as its reason. Six probes across two wordings, three of them fresh against the current text, is where this closes; `testing-skills.md`'s 5+ is met in count and not in kind, since the six are not six reps of one prompt, and a second over-fire report is the trigger to re-test rather than re-word. Consequences are met: `CLAUDE.md`'s first line carries the qualifier (as does `README.md`'s, which this record did not name), and both guards hold — `src/phi-safe-code/` contains only `SKILL.md`, no `references/` digest, and the body carries no org policy number, citing only Safe Harbor and §164.312(b) by section.
- **2026-08-22, on landing B10** — `health-literacy` is authored and the second Deferred line closes. Placement stays plain `src/health-literacy/`, a single `SKILL.md` with no `references/`, so both guards this record set for B9 hold for B10 as well: no org policy number in the body, and no reference file that is a regulation digest. Sources went as the first amendment required. The cms.gov *Guidelines for effective writing* page is the quoted source for sentence and paragraph length, active voice, the language-to-avoid list (including its own bar on quoting laws and regulations), one-term-per-concept, and scanning behavior. The plainlanguage.gov *Use simple words and phrases* list is cited by name rather than vendored (plainlanguage.gov now redirects to digital.gov and the list has no stable URL to cite): the general federal substitutions are not a gap in the model's default output, and the payer vocabulary that is one is carried inline instead — which is how ledger row A42.M2, the public-domain plain-English table, closes without a `references/` file. The CMS *Toolkit for Making Written Material Clear and Effective* is cited by name, in one lineage line at the foot of the body, for its money rules (a comparison in place of a decimal, a range with its reason, the running state) and for grade-level scores as ranges rather than a sole gate, and is quoted nowhere; the define-where-it-occurs and how-to-get-help rules the first amendment anticipated from it landed without a Toolkit citation, as the body's own rules. `writing-web.pdf` was fetched but not used: no PDF text extractor is installed on this machine, and installing one unasked was out of scope; the guidelines page it supplements carries the normative rules.
- **2026-08-22, prong (c) is UNVERIFIED for `health-literacy` — superseded: see the closing amendment below.** The micro-test the first amendment named as the stand-in for the untested third prong **was not run** — it needs fresh-context probes, the user declined the subagent probes in the authoring session, and no in-session substitute exists once the author's context already holds the description. The description was *written* to the requirement — it opens on the acts an engineer types (an error or validation message, an in-app notice, an email, letter, SMS, or EOB template held in code, a cost-share breakdown), carries payer vocabulary (member, subscriber, enrollee, deductible, coinsurance, prior authorization, EOB), never requires the phrase "health literacy" or "plain language" to fire, and carries the anti-trigger that sends internal-facing text to `writing-for-humans` — but that is a design claim about wording, not a measurement of behavior, and this record does not treat the two as equivalent. The first observed miss or over-fire is the trigger to run the probes; until then the trigger claim for this skill stands unverified in a way `phi-safe-code`'s does not.
- **2026-08-22 — the second Domain skill tests the call ADR-0057's review finding F57 made — that a Domain skill defines its own field's terms in its body rather than in `DOMAIN.md` — and it held.** `DOMAIN.md` says a Domain skill defines the terms of its own field in its body rather than importing them into the glossary. `health-literacy` defines *member-facing copy*, *the next move*, and *required language* in its own opening, and landing it required no new `DOMAIN.md` row — the sentence scaled to a second field without edit, which is what the alternative of per-skill glossary rows would not have done. The placement question this record left open is unchanged: two Domain skills sit flat in `src/`, and a third re-opens it.
- **2026-08-22, prong (c) for `health-literacy` closes — the UNVERIFIED amendment two above is superseded on its facts.** The user reversed the skip later the same session and the micro-test ran: five fresh-context probes, each given only the roster of model-invoked descriptions and one realistic engineer message, asked which skills they would load and why. None of the five prompts contained "health literacy", "plain language", or "readability". **Three fired, two correctly stayed quiet.** The firing three were an eligibility-lookup 404 rewritten for the member portal, a deductible-reset email template in a notifications service, and a denial-notice template rendering "You owe $0.00" on a fully covered claim — the third naming the zero-render rule as its reason, which is the description's own trigger rather than a general guess about copy. The two controls are what make this more than a positive-only test: an internal CSR admin-dashboard error carrying payer vocabulary (group number) and the error-message act **declined the skill and quoted the anti-trigger back** ("audience is an internal CSR rep in admin tooling, which its description explicitly excludes"), routing register to `writing-for-humans`; and a claims-parser module rename stayed quiet entirely. That control is the one that matters, because an over-fire on adjacent internal text with the right vocabulary is exactly the failure `phi-safe-code`'s NPI probe hit, and the anti-trigger written to prevent it demonstrably works. `testing-skills.md`'s 5+ is met in count and in kind, the five being three distinct firing shapes and two distinct non-firing shapes rather than reps of one prompt. All five ran against the **final** wording, so B9's lesson — that probes against a superseded draft prove nothing — is not repeated here. The trigger claim is now measurement, not design. What stays unmeasured is live use: a probe is a stated intention to load, not a load, so the post-B8 window keeps `health-literacy` as a fire-count confirmation rather than as the primary check, and a second over-fire report is the trigger to re-test rather than re-word.
- **2026-08-23** — The third Domain skill, `accessible-ui`, is admitted in [ADR-0060](0060-third-domain-skill-accessible-ui.md), which re-decides the placement this record left provisional: flat in `src/` stays, and the re-open trigger is the first loop that has to special-case Domain skills, not a count. The same record adds to the Gap-and-stakes test that prong (a) measures the models the invoking teams run; on the authoring model alone the probe would have rejected the skill.
- **2026-08-26 — the post-B8 fire-count confirmation ran on both machines and closes on a zero from each, with the links present on both.** This machine: `grep -rhoE '"skill":"(phi-safe-code|health-literacy|accessible-ui)"' ~/.claude/projects | sort | uniq -c` returned nothing, with all three links in `ls -l ~/.claude/skills` (dated 2026-08-22 and 2026-08-23). The work machine, pasted into `~/Downloads/install-report.md` on 2026-08-26: the same command, nothing, its three links dated 2026-08-25. The shape is a superset count, which is what a zero needs: a `Skill` tool_use stores its input as `{"skill":"<name>",…}`, checked against a known-firing skill this session, so no real load escapes the pattern and both zeros measure the descriptions and not a missing link, the precondition the B10 line above set. It does not count *only* real loads, and the next counter must not read a nonzero figure off it: a `StructuredOutput` probe answering *which skill would you load* writes the same string, as does the workflow journal echoing that answer, so `work-item-shape` returns 6 against 0 real loads. A nonzero count is the parsed tool_use, `{"skill":"X"` inside an assistant `Skill` call, which [ADR-0051](0051-usage-evidence-mining-round.md)'s amendment of this date carries beside the `Launching skill:` trap. What crossed this machine after the links is also known: the a11y-health api repo had no session at all, and the only UI-repo sessions were B7b's on 2026-08-23 (`/write-skill` for the project's convention skill, `/tdd` for its Bash gate, review, ship) — the case `accessible-ui`'s anti-trigger names, not member-facing copy, member data, or a widget. So the zero is the outcome the B10 line anticipated for niche Domain skills in this span, and the trigger claims rest where they were: `phi-safe-code` on its six probes, `health-literacy` on its five, `accessible-ui` on its twelve. The next over-fire or miss report re-opens them, not a calendar. Nothing changed. The window's other counts are in [ADR-0051](0051-usage-evidence-mining-round.md)'s amendment of the same date.
