# black-box-check ported from openclaw's behavior-validator

Nothing in the suite validated the *running* artifact — `audit-tests` audits the test suite, `review-changes` judges the diff, `feedback-loops` runs checks that already exist — so a UI that only prints success text could pass every gate. openclaw/agent-skills' behavior-validator fills that gap with a discipline that only holds as a whole skill: validation against a behavior contract written *before* testing, source-blindness as a hard rule (implementation-looking evidence is contamination that halts the run), and anti-cheat probes that catch fake or static behavior. We ported it as the user-invoked `black-box-check` (2026-08-08 mining round), trimming the JSON report schema and the workspace-isolation ritual to house prose-report style. We rejected folding just the anti-cheat probes into `audit-tests` — the probes are only trustworthy *because* the contract predates the run and the checker can't read the code's story about itself; splitting them from that identity leaves decoration, not discipline. We renamed it because "behavior" already names a skill kind in this repo's domain language.

## Amendments

- **2026-08-22** — Renamed `validate-behavior` in [ADR-0057](0057-rename-table-and-discipline-skill-term.md)'s rename table. The reason above — "behavior" already named a skill kind — no longer holds: the same record renames that kind Discipline skill, so the product sense is the only one "behavior" carries in a skill name.
- **2026-08-26** — The retire check [ADR-0057](0057-rename-table-and-discipline-skill-term.md) scheduled ran and the skill is **kept for one more window, on the user's call**. The count was zero: no typed `/validate-behavior` or `/black-box-check` in `~/.claude/history.jsonl` since the file begins (2026-01-11), no `Skill` load under either name in any local transcript — never run on this machine in the eighteen days since the port. The rule said retire; the user overrode it. The reason they gave was the count's scope — it is one machine's, and the work machine, where a running-app check is likelier to be wanted, had the skill for one day before the count — which the rule did not carry at the time. It does now: the override generalised into `DOMAIN.md`'s **Retire-on-zero window** row, where a one-machine zero settles the window only if that machine holds the whole population the skill serves. This skill is the other case, so the next window decides on both machines' counts and a second zero retires it. The gap this record opened with is unchanged. The window's other counts are in [ADR-0051](0051-usage-evidence-mining-round.md)'s amendment of the same date.
