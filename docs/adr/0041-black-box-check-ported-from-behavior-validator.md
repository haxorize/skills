# black-box-check ported from openclaw's behavior-validator

> **Amended by [ADR-0063](0063-team-fit-test-replaces-retire-on-zero-window.md):** the second-zero retire check scheduled in the amendments below is cancelled. `validate-behavior` is kept on the Team-fit test — the moment is an engineer proving a change works in the running app — and a **Skill** carries no retire-on-zero window; the window binds Deprecation stubs alone.
> **Amended by [ADR-0057](0057-rename-table-and-discipline-skill-term.md):** `black-box-check` is renamed `validate-behavior` in that record's rename table; the reason given here for the original name no longer holds, since the same record renames the skill kind Discipline skill.

Nothing in the suite validated the *running* artifact — `audit-tests` audits the test suite, `review-changes` judges the diff, `feedback-loops` runs checks that already exist — so a UI that only prints success text could pass every gate. openclaw/agent-skills' behavior-validator fills that gap with a discipline that only holds as a whole skill: validation against a behavior contract written *before* testing, source-blindness as a hard rule (implementation-looking evidence is contamination that halts the run), and anti-cheat probes that catch fake or static behavior. We ported it as the user-invoked `black-box-check` (2026-08-08 mining round), trimming the JSON report schema and the workspace-isolation ritual to house prose-report style. We rejected folding just the anti-cheat probes into `audit-tests` — the probes are only trustworthy *because* the contract predates the run and the checker can't read the code's story about itself; splitting them from that identity leaves decoration, not discipline. We renamed it because "behavior" already names a skill kind in this repo's domain language.

## Amendments

- **2026-08-22** — Renamed `validate-behavior` in [ADR-0057](0057-rename-table-and-discipline-skill-term.md)'s rename table. The reason above — "behavior" already named a skill kind — no longer holds: the same record renames that kind Discipline skill, so the product sense is the only one "behavior" carries in a skill name.
- **2026-08-26** — The retire check [ADR-0057](0057-rename-table-and-discipline-skill-term.md) scheduled ran and the skill is **kept for one more window, on the user's call**. The count was zero: no typed `/validate-behavior` or `/black-box-check` in `~/.claude/history.jsonl` since the file begins (2026-01-11), no `Skill` load under either name in any local transcript — never run on this machine in the eighteen days since the port. The rule said retire; the user overrode it. The reason they gave was the count's scope — it is one machine's, and the work machine, where a running-app check is likelier to be wanted, had the skill for one day before the count — which the rule did not carry at the time. It does now: the override generalised into `DOMAIN.md`'s **Retire-on-zero window** row, where a one-machine zero settles the window only if that machine holds the whole population the skill serves. This skill is the other case, so the next window decides on both machines' counts and a second zero retires it. The gap this record opened with is unchanged. The window's other counts are in [ADR-0051](0051-usage-evidence-mining-round.md)'s amendment of the same date.
- **2026-08-26, the work-machine window** — The work machine's own count is in and is also zero: 0 typed `/validate-behavior` or `/black-box-check` and 0 `Skill` loads under either name, measured there as the parsed tool_use. The Keep above stands: that machine had one day with the skill before the count, so its zero opens the re-run window rather than closing it, and the retire still waits on a second zero from both. [ADR-0051](0051-usage-evidence-mining-round.md)'s work-machine bullet carries the measurement.
- **2026-08-27** — The second-zero retire check the two bullets above schedule is cancelled by [ADR-0063](0063-team-fit-test-replaces-retire-on-zero-window.md): a **Skill**'s population includes teammates whose sessions nobody measures, so no count on either machine is a verdict on it. `validate-behavior` is kept on the Team-fit test, with the moment named — an engineer proving a change works in the running app rather than in tests. The generalisation this record's 2026-08-26 bullet claimed for `DOMAIN.md`'s **Retire-on-zero window** row no longer describes that row: the window binds Deprecation stubs alone. The Keep stands; only its mechanism changed.
- **2026-08-27** — Two things this record never stated, surfaced by the 2026-08-27 review and settled by Nick. **The user-invoked axis is load-bearing for the discipline, not a classification convenience.** Source-blindness requires a checker sharing none of the writing session's context; model-invocation loads into that very session, so an auto-reached run is contaminated by construction and "is not this check at all". Any future proposal to flip this skill to model-invoked — including one made to let another skill declare it as a dependency — is answering a reachability problem with a change that destroys the check. It stays user-invoked for that reason, recorded here so the next reader does not re-derive it.

  **Reachability is the real open problem, and the axis was masking it.** Re-measured 2026-08-27 on this machine: **0** typed invocations under either name across 4254 `history.jsonl` entries, and **0** `Skill` loads in any local transcript — unchanged since the port. Under the Team-fit test that is evidence, not a verdict, and the Keep stands. The first fix is placement: `tdd` already detects the moment (a slice touched behavior no test ran) and buried the offer in its closing sentence, so that is now a named branch with the skill's cost stated — a contract written first by a non-checker, and a context-free session — so the reader can judge the ceremony rather than be sold it. **What is deliberately left open** is whether that ceremony is worth its cost for this team's moment; a skill whose preconditions exceed the moment they serve fails Team-fit on the moment being real *at that price*, and that is the question the next round should put, not another placement change.
