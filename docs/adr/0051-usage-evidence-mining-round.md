# Mine 28 new sources and 34 deltas against usage evidence; fold the trust fixes

> **Amended by [ADR-0057](0057-rename-table-and-discipline-skill-term.md):** the Discipline-skill rename and the rename table are recorded there, not in ADR-0054 (which went to the commit-bypass hook); the `grill-me` merge is recorded in [ADR-0058](0058-grill-me-absorbs-grill-and-record.md).
> **Amended by [ADR-0058](0058-grill-me-absorbs-grill-and-record.md):** the `grill-me` merge and the `grill-and-record` deprecation stub this record assigned to a later batch are decided there, with the stub's window and its format reference's new home.
> **Amended by [ADR-0063](0063-team-fit-test-replaces-retire-on-zero-window.md):** the retire check on `validate-behavior` — the one check still open after the 2026-08-26 window — is canceled; skills are judged by the Team-fit test, and the retire-on-zero window binds Deprecation stubs only. This reaches the body, not the banner alone: the Consequences paragraph's "three retire checks", the `validate-behavior` line and the `adoption-verdict` flip line under *Round decisions* all describe the superseded regime. (`adoption-verdict`'s flip check had already closed on 2026-08-26 under [ADR-0032](0032-adoption-verdict-standalone-behavior.md); ADR-0063 restates the axis rather than canceling that check.)

The 2026-08-21 round read 28 previously unseen sources, delta-fetched the 34 repos already in the ledger, and re-heard every prior veto. Novelty ranked the candidates in the [ADR-0046](0046-four-repo-fragment-mine.md) and [ADR-0050](0050-twelve-repo-mine-and-discoverable-code-port.md) rounds; this time usage evidence did. Two Claude Code Insights reports (personal machine, 113 sessions over 2026-07-15 to 08-14; work machine, 83 sessions over 2026-07-21 to 08-20) and the typed history of 171 sessions since 2026-07-10 set the lens, and a candidate landed only where that evidence showed a failure it addresses and the current skill text lacked the fix. Three decisions own their own records and are only named here: the `committing` extraction from `ship` ([ADR-0052](0052-committing-extracted-from-ship.md)), the global rules layer ([ADR-0053](0053-global-rules-layer.md)), and the Discipline-skill rename with the `grill-me` merge (ADR-0054, written when its batch lands). Everything else the round decided is recorded below.

## Why usage evidence beat novelty

Four rounds of mining had widened the suite to 42 skills, with dilution the named failure mode each time. The evidence showed a different one: the suite runs as a single loop, and its defects are trust defects. On the personal machine, 80-plus sessions ran the same path: `/from-ticket`, `/implement`, `/handoff`, a fresh session with `/review-changes` on the handoff file, the typed line "address all findings through the /receiving-review lens" (55 times, verbatim), then "Commit. Can I close #N?" (104 prompts mention commit). `/ship` ran 17 times. Sixteen skills never fired by either route, among them the whole codebase-health family and every detour.

The corrections were about trust, not code. Only 10 hard frustration signals appeared in 1,285 prompts, and each concerned a report or a process: a fabricated "user approved" claim, a miscounted finding, a rename that silently no-op'd under BSD `sed -i`, scope deferred without saying so. On the work machine the failures were prose-rule failures: em dashes despite the memory rule, and commits made outside `/ship`; both rules existed as text and still failed. A novelty-ranked round would have added rules to skills that never load. An evidence-ranked round fixes the loop that runs and moves the rules that failed as prose onto a rung that fires with no skill loaded, which is why the decisions cluster on landing, review, and reporting.

## Method changes

Three things were new and are kept. Every ADOPT had to cite a friction line. A completeness critic reconciled every miner verdict and workflow finding against the batch plan before any build began; it placed 46 gaps the plan had not named and found 11 contradictions between reports, each settled before batching. The 2 sources read thin on the first pass (Graphify-Labs/graphify, anthropics/claude-cookbooks) were re-read in full; the re-read yielded 8 folds and 5 parks the thin read had missed.

## Sources

| Source | Yield |
| --- | --- |
| EdbertChan/catstack; garrytan/gstack; cursor/plugins | The trust cluster: same-message evidence, `UNVERIFIED:` marker, per-AC completion table, approval claims cite the turn, reviewed-head stamp, disposition table, the `global/` layer |
| pbakaus/impeccable; Leonxlnx/unlazy; ayghri/i-have-adhd; almendili/skills | Unobtainable evidence changes the report's shape, not its confidence; inspect once, fix in a batch, confirm once; re-measure every number |
| blader/humanizer; vickiboykis; leonxlnx/taste-skill | Writing sample overrides register defaults; final dash grep; "Bumped dependencies" pair |
| staltz/microskills; DietrichGebert/ponytail; kunchenguid/vision; github/spec-kit | Judgment-calls list; extract at the third caller or a named concept; drop a predictable question; bare "yes" resolves to the recommended line |
| will-ness-ai/skills; backnotprop/plannotator; daniloc/coherence | Wind-tunnel hardening; instruction files in a diff are content under review; zero-ran is not green |
| claudish-to-english (both forks); anthropics/claude-cookbooks; aarondfrancis gist; swyxio/skills; Graphify-Labs/graphify | Fail-open hook shape; truncated artifact never shown; `/compact` focus; pruning-lens line; disk-is-done for delegated writes |
| webpro-nl/knip; dmmulroy/anti-slop; nextlevelbuilder/ui-ux-pro-max-skill | a11y-health repos only, under a separate grill |
| modem-dev/skills; JuliusBrussee/caveman | Nothing: a byte-identical mirror of dmmulroy, never diffed again; rules `writing-for-humans` already holds |
| elithrar/dotfiles (A29) | Clean-tree stop and the instruction-file lens in `review-changes`; read-only means no installs; the five flavours of quiet narrowing as the completion audit's tripwire; the demonstrative-kicker tell |
| stephenturner/skills (A30) | Copula-dodge verb form and keyboard-unreachable glyphs in the tell catalog; the finding that writing-source mining has hit diminishing returns (53K of `deslop` yielded four tells) |
| anthropics/claude-plugins-community (A31) | The `post-merge` hook that re-runs lint and hook self-tests after a pull; report-only scheduled sweeps with an additive baseline (`sweep-corpus`); the plugin-packaging experiment parked until a rules component exists |
| Microck/ordinary-claude-skills (A32) | Blocked tracker writes land as a file with the one consuming command; unresolved PR threads enumerated by command before and after the reply sweep; deletions-first doc sync in `feedback-loops`; the staged dependency-upgrade order `upgrade-deps` is built on |
| hardikpandya/stop-slop; ehmo/slopkit; aboudjem/humanizer-skill; aashaexo/soundshuman; jalaalrd/anti-ai-slop-writing (A33–A37) | The anti-slop class: tool-residue alternation and the named dash substitutes in the sweep, which became the fifth global rule `outbound-dash-sweep`; no Markdown in an email or Teams body; the rest already in the catalog under other names |
| wshobson/agents (A38) | `commit-bypass.sh` and the shared hook library; the `sed \<newline> -i` gap in `rename-safety`; security-first ordering in `upgrade-deps`; evidence-against in `diagnosing-bugs`; quoted text as an untouchable; the looping review orchestrators recorded as the counter-example one-pass-ledger-stop diverges from |
| warpdotdev/common-skills (A39) | The repo-local `## Review lenses` block; "Fixed in `<hash>`" only once the hash is on the remote; a FIXED row states the deficiency is gone; commit messages and PR bodies through a file; index-only conflict handling in `resolving-merge-conflicts` |

Of the 34 delta repos, 15 yielded fragments. The per-idea ledger, 110 adopt-or-adapt rows, lives in the memory file.

## Vetoes re-heard

Every prior veto was re-heard rather than sampled, because the user suspected a "more is worse" reflex had rejected ideas that now had a consumer. A veto flipped only when the evidence showed a failure the idea addresses and the current text lacked the fix; "the repo grew a consumer" counts, "it would be nice" does not. Where a rule existed and still failed, enforcement was the gap, and the veto stood.

| Outcome | Count | Examples |
| --- | --- | --- |
| Reopened | 10 (1 adopt, 9 adapt) | One-commit fast path and `Landing:` key; stable `F<n>` finding IDs; hooks directory; handoff landing zone; recommend-and-proceed outside grills |
| Parked with a named consumer | 16 | AC/test drift, a real prototype detour, `corpus-sweep` running `verify-docs` |
| Upheld | 82 | Batch "approve all", cross-model second opinions, persona packs |

## Decisions recorded here

- **`address-findings`** is a new user-invoked orchestrator, `requires: receiving-review`. It reads the newest review report, splits findings into mechanical (fix without asking) and ask (one batched question), and ends with a disposition row per finding: FIXED, DECLINED with reason, DEFERRED for the human to ratify, or ABANDON. One pass, a ledger, then stop. **`receiving-review`** correspondingly becomes a single fix pass; re-review is the user's call.
- **The one-commit path** ("commit, close, tick, push" in one move) lives in `committing`, not `/ship`, because `/ship` is user-invoked and the 100-plus ad-hoc "commit and push" prompts never reach it.
- **The `Landing:` key** in a repo's CLAUDE.md holds branch policy, whether a PR is required, whether push and ticket-close are pre-authorized, and the defect policy, defaulting to "fix, don't file". Outward acts (commit, push, tracker write, message, loop) always sit in the ask bin unless this key pre-authorizes them; compound-engineering's "reversibility decides" wording was rejected because the user's corrections sit at outward acts, and it would license pushes.
- **`handoff`** writes to `$TMPDIR/claude-handoffs/<repo>-<date>-<slug>.md`, and `review-changes` with no argument picks the newest for the current repo; the router names the bridge. A handoff carries the session's completion audit verbatim, with provenance tagged user's / inferred / my call; `review-changes` briefs every finder diff-only and hands that narrative to one falsification lens that reads it last.
- **`implement`'s close** becomes a claim with evidence: a per-AC table (DONE / PARTIAL / NOT DONE / CHANGED / UNVERIFIABLE, each row with an evidence line), a beat ledger, a parked ledger whose zero case is stated, and a judgment-calls list that includes silent defaults. The shape lives in a `completion-audit.md` sibling owned by `implement`, inherited by `handoff`, and consumed by `committing`. The verification loop is bounded to one inspection round, one batched fix, at most one confirm. The refactor beat gains one clause, extract at the third caller or a named concept, overriding the earlier veto because it is the only form checkable before extracting.
- **No em-dash hook.** Three reports asked for one; it was rejected because a hook sees file writes, and the em dashes that were caught were in chat output, which no hook can see. `writing-for-humans`' outbound register instead gains a mandatory last step: search the draft for `—` and rewrite. `write-skill` gains the general escalation row (prose, then hard prohibition, then hook or lint); here the top rung is unreachable.
- **A rename-safety hook** ships in `global/hooks/` as the round's one hook: a PreToolUse check on Bash that blocks in-place mass edits (`sed -i`, `perl -pi`, `xargs` feeding either) and names the path-shaped arguments it can see. Four shell-fragility sessions were the evidence.
- **`black-box-check` is renamed `validate-behavior`** now and kept one more window before a retire check. The critic argued the rename spends edits on a skill that may retire; it stands so the window measures the skill under a name that describes it.
- **`wizard` moves to user-invoked.** Its body writes a script, `.env`, and CI secrets into the repo, which is the consequential-publisher shape; it never fired autonomously and cost 391 description characters every turn.
- **Publisher load gates.** `to-feature`, `to-story`, `to-tasks`, `to-bug`, and `chart-course` check that `work-item-shape` and `writing-for-humans` loaded, and create a tracker item only after a confirming turn exists to cite. An ADO story auto-created mid-grill was the evidence.
- **New skills.** `onboard-repo` (user-invoked) interviews for tracker, landing, and loop commands, writes the `Issue tracker:` and `Landing:` blocks, and prints the hook snippet; it is a skill rather than a README section because it interviews and writes blocks a README can only describe. `deps-upgrade` (user-invoked; admitted as `upgrade-deps`, ADR-0057's 2026-08-22 amendment) wraps the upgrade flow that ran in about 8 sessions with no skill, tool-neutral: Context7 when that MCP is present, registry CLIs otherwise. `mine-skills` and `corpus-sweep` (admitted as `sweep-corpus`, same amendment) will sit under this repo's `.claude/skills/` because they act on this repo alone.
- **Smaller folds.** `grilling` drops any question whose answer is predictable or observable by running something. `adoption-verdict` stays model-invoked, and flips if the next window is still zero fires. `feedback-loops` treats zero-ran as not green. `capturing-learnings` always runs its offer and says "none this session" out loud.

## Parked, with the condition that unparks each

- **Adversarial verifier gate in `ship`**: unparks if the claims rule alone proves insufficient.
- **Two-machine sync check at session start**: unparks on the next "forgot to merge from my work machine" incident, or when a second hook makes the install snippet a one-line addition.
- The remaining parks each name a consumer in the batch plan and the memory file.

## Guards

`global/` holds only rules a skill depends on, each naming that skill and why a hook or lint cannot do its job. `committing` never owns the commit split. `address-findings` never re-runs the review. "No unasked commits" has one owner, the global rule. A rule found in both `ship` and `committing`, or in both `receiving-review` and `address-findings`, is a defect.

## Lineage

All six upstreams under [ADR-0034](0034-branch-mining-lineage-or-dormant-main.md) were diffed on main and branches. The ported skill files were unchanged in compound-engineering-plugin, oaustegard/claude-skills, obra/superpowers, openclaw/agent-skills, and dmmulroy/dotfiles. mattpocock/skills advanced 34 commits; one fragment was taken (the `---` separator between grill questions) and its 9 unmerged branches carried nothing portable. The sweep points advance to mattpocock `5b15a47`, compound-engineering `66ccf57`, oaustegard `66ec85b`, superpowers `b36e082`, openclaw `128a4ea`, and dmmulroy `a7beb72`; ADR-0034 carries the amendment once the round's last batch lands. jakubkrehel/skills' recorded sweep SHA did not resolve, so its main tip `6c43b20` is recorded instead. None of the 28 new sources becomes a lineage upstream.

## Consequences

The round lands as 7 batches, each reviewed through the `write-skill` pruning lens and committed separately, ADR before skill. Three retire checks are scheduled for the next window: the `grill-and-record` stub, `validate-behavior`, and `adoption-verdict`'s axis. Load counts for the gated skills are re-measured after the gates land. As before, reversal is the costly direction: the folds carry no markers, so this record and the batch plan are the only map back.

## Amendments

### 2026-08-21 — Batch 2 landed; eleven late sources mined

Batch 2 built `address-findings`, reshaped `receiving-review` to one fix pass (retiring the convergence guard — ADR-0017 carries the dated amendment), fixed `handoff`'s landing zone as the one place the handoff and report filenames are defined and added the model/effort stamp beside the head stamp it already carried, and gave `review-changes` its Batch 2 folds: the no-argument newest-handoff pickup, the clean-tree stop, stable `F<n>` IDs, the reviewed-head stamp with "N commits since", diff-only finder briefs with a falsification lens, the instruction-file and repo-declared lenses, the in-process path for small prose diffs, the coverage-ledger row for unreadable evidence, and the report written to the landing zone. One rejection recorded here: wshobson/agents' review orchestrators (`comprehensive-review`, `git-pr-workflows`) all loop fix → re-review → checkpoint with no disposition ledger and no deferral rule; the user's own corrections (ledger A07.9: stop looping, address all findings, no follow-ups) are the evidence against that shape, and one-pass-ledger-stop is the divergent choice on purpose. P1's novelty-rate stop condition stays parked: with one pass there is no looping reviewer to stop. The A29.3 lens budget re-hears the size-gating veto on a different axis — that veto rejected size-gated *risk planning*, and this gates only whether lenses fan out — but the re-hearing itself is the user's and is parked for Batch 5 with the other two user decisions from the late mine.

Eleven sources were added after Batch 1 and mined as a delta (ledger A29–A39): hardikpandya/stop-slop, ehmo/slopkit, aboudjem/humanizer-skill, aashaexo/soundshuman, jalaalrd/anti-ai-slop-writing, elithrar/dotfiles, stephenturner/skills, Microck/ordinary-claude-skills, anthropics/claude-plugins-community, wshobson/agents, warpdotdev/common-skills. Their folds are assigned to the batches that own the target skills and land as those batches do; the Sources table above gains their rows when Batch 6 closes the records. The rename-safety hook gained one measured fix from that mine: a backslash-newline continuation (`sed \<newline> -i`) passed the shape check; it is joined before matching now, with a self-test row.

### 2026-08-21 — Batch 3 landed

Batch 3 made "done" a claim with evidence and bounded the self-verification loop. `implement` now declares an edit boundary before the first edit (the narrowest directory per layer holding the slice's files there — the review's F15 showed a single containing directory is the repo root for any vertical slice; past it, Proceed / Split / Rethink is asked, never widened quietly — `diagnosing-bugs` declares the same boundary with its hypotheses and cites the rule), runs its refactor beat under one third-caller clause (third caller or a domain name; the caller named before an unrequested helper exists — the D4 contest settled by decision, A10.1/N19; `implement` owns it and `tdd`'s refactor step carries it, so the testable path reads it where it refactors), and fills the completion audit under the global evidence rule: evidence re-measured per row, out-of-repo evidence `UNVERIFIABLE` with the manual check named, every `DONE` checked against the quiet-narrowing tripwire's five flavours (smaller / safer / easier-to-test / already-existing-and-unexercised / merely-compatible — a tripped row is PARTIAL or CHANGED, never DONE), and the audit held to one inspection round, one batched fix, one confirm (N33, A14.3 — the implement-side answer to "stop looping"; Batch 2 bounded the receiving side). The audit form gained the completion line (`complete` / `complete with N unverified` / `incomplete`, counts taken from the table and the parked ledger, whose rows now say whether this ticket owns them; `committing` starts from the line and re-derives the word from the table, `handoff` carries it with the other four parts), and the judgment-calls list widened from unsure choices to the confident defaults a silent ticket left to the agent (M1): three of the round's corrections were confident defaults the old filter excluded by design. `feedback-loops` counts zero tests ran as a red, sweeps the class on a mechanical gate's second rejection of it (N21), and syncs docs by grepping for what the diff removed or renamed before reading for anything else (A32.3); `discoverable-code` ends a rename only when the old name searches to zero, the edit-tool counterpart of the Batch 1 rename-safety hook. Glossary rows `Beat ledger`, `Parked ledger` (renaming `Parked list`), `Judgment call`, `Edit boundary`, and `Quiet-narrowing tripwire` (alias: `handoff`'s success-substitution) landed. Smaller folds in the same batch: `feedback-loops`' description and loop intro take warnings into scope when the ask was to clear them (that ask sets the scope); `diagnosing-bugs` and `implement` route a found defect through the `Landing:` defect policy and a parked item through `to-bug`, `to-tasks`, or `to-story` on the user's ask; the README and `which-skill` blurbs and `phase-boundaries.md` follow the rename and the audit. Rejected here: the numeric file-count threshold on the edit boundary (A08.7's ">5 files") — a number the agent counts after the fact is machinery, and a directory is the boundary an agent can see before editing, one per layer so the stop still fires on a vertical slice; and a stop-hook completion backstop (A32.4) stays parked until the audit has run for weeks.

### 2026-08-22 — Batch 4a landed

Batch 4a was the non-naming half of Batch 4 (the rename table is B4b, grilled separately). It moved `wizard` to user-invoked; consolidated five things into single owners — tracker resolution into one nine-host sibling with a separate five-host `publishing.md` for the create-side procedure (Publish gate, label precheck, blocked write, transport safety), the naming-drift check into `work-item-shape` ([ADR-0048](0048-naming-drift-queue-trimmed-to-check-only.md) amended), the subagent rules into a six-host `subagent-brief.md`, the ADR gate's criteria into `adr` alone with the hosts pointing at the **ADR gate** by name, and the cold-reader pass into its two forms (`work-item-shape`'s item form, `writing-for-humans`' generic line); landed the publisher load gates and the Publish gate the "Publisher load gates" decision above names — with one correction to that line: `chart-course` gates `writing-for-humans` only, since its decision tickets follow chart-format, not `work-item-shape`; gave `adr-format.md` its **Deferred** section and a corrected omission rule; trimmed `diverging`'s description to the fixation triggers plus the `grilling` distinguisher; narrowed `adoption-verdict`'s dependency trigger to one candidate's major-version bump (the "update all packages" phrase waits for `deps-upgrade`); and shipped the second hook, `commit-bypass`, always-on ([ADR-0054](0054-commit-bypass-hook-always-on.md)), with `install.sh` printing one JSON object for both hooks and the post-merge hook running the four gates before the re-hoist ([ADR-0049](0049-opt-in-post-merge-hook-auto-runs-install.md) amended). The B4a review's falsification lens corrected three numbers in the build's completion audit (`diverging` 409 chars, not 392; 21 table rows, not 20; five dropped triggers, not two) and one false "verified NOT" (`diagnosing-bugs` does offer a subagent; it stays out of the brief group because the brief would be the evidence trail its own data-not-instructions line already governs). The two-machine sync check stays parked: the second hook did not make the snippet a one-line addition — it is still a pasted object — so the unpark condition is unmet. Deferred from the review into the batch plan: porting the tokenizer to `rename-safety.sh`, and a work-machine check of whether `az boards work-item create --description @file` reads the file before the publishers' inline `--description "$(cat …)"` lines are rewritten.

### 2026-08-22 — Batch 4b: the rename table

The `black-box-check` → `validate-behavior` line above was re-judged in B4b's table with every other skill name and stands. Of the two decisions this record assigned to "ADR-0054", the Discipline-skill rename is recorded in [ADR-0057](0057-rename-table-and-discipline-skill-term.md); the `grill-me` merge is decided when B4c lands, in its own record or a further amendment here. ADR-0054 went to the commit-bypass hook.
- **2026-08-22** — B4c landed the merge in its own record, [ADR-0058](0058-grill-me-absorbs-grill-and-record.md): `grill-me` records by default, `--plain` suppresses, `grill-and-record` is a one-window stub.

### 2026-08-22 — Batch 5: `onboard-repo` built, and why a skill beats a README section

Ledger row D13 conditioned `onboard-repo` on an answer to "why a skill rather than a README section". The answer, ratified at the B5 grill: a README section cannot interview, preview, or refuse to overwrite, and every publisher already bootstraps its own block on ask — the skill is the one place the tracker block, the `Landing:` key, `## Commands`, `## Convention skills`, `## Registry`, and the seed files (`DOMAIN.md`, `docs/solutions/`) land together in one sitting, each written only where nothing exists. It stays optional: every skill still degrades to asking when a block is missing. The same batch built `upgrade-deps` and the repo-local `mine-skills` / `sweep-corpus` pair, added the fifth global rule `outbound-dash-sweep.md` (the em-dash rule had failed as prose in memory, the register row, and the catalog; the failing case is a Teams draft typed with no skill loaded), and gave `explain` its cold-topic branch with the bare form kept as the re-pitch.

- **2026-08-22** — Review of B5 (F34): `onboard-repo` no longer seeds `docs/adr/`; `adr-format.md` creates that directory lazily at the first ADR, and a present-but-empty `docs/adr/` flips `grill-me` into recording mode on a repo with no decisions. The seeds are `DOMAIN.md` and `docs/solutions/`.

### 2026-08-22 — Batch 6: the records

Records only, no skill change. The Sources table gained the eleven late rows (A29–A39) the 2026-08-21 amendment promised; [ADR-0034](0034-branch-mining-lineage-or-dormant-main.md) carries the refreshed lineage SHAs; CLAUDE.md's ported-skill list marks `grill-and-record` as the stub. The linter gained the citation check the batch plan named: every skill a global rule's `Depends:` line names must mention `~/.claude/rules/` or the rule's filename stem, with a fixture and a self-test row — it found one real gap, `chart-course` named as a dependant of `large-write-chunking` and never citing it, closed in the same change. Scheduled for the next window, not taken here: the retire decisions on the `grill-and-record` stub and `validate-behavior` (and `adoption-verdict` if it is still at zero fires), and the load-count re-measure for `discoverable-code`, `work-item-shape`, and `tdd`+`implement`. Still parked: extracting a model-invoked doc-claim discipline that `verify-docs` and `sweep-corpus` would both declare.

- *2026-08-22, B10* — The park above closes: the discipline landed as `doc-claims`, declared by both ([ADR-0016](0016-behavior-skills-declared-deps-relaxed-atomicity.md), amendment of the same date), so the Parked row's 16 in the Reopened/Parked/Upheld table is 15 from here.

- *2026-08-22, after the B6 review* — Corrections to the paragraph above, which was written before B6 was reviewed: "records only" omits the one citation line the batch added to `chart-course`; the eleven late sources landed as seven rows; the plan has grown from seven batches to ten (B1–B10, B4 split into a/b/c), so the Lineage SHAs in [ADR-0034](0034-branch-mining-lineage-or-dormant-main.md) are earlier than the Lineage section above implies; and the citation check's first matcher accepted a bare `~/.claude/rules/` mention for any rule, which had let `committing` pass without naming `recommend-and-proceed` — the review of B6 (run after the batch was pushed, the lapse [ADR-0059](0059-review-receipt-hook.md) records) found both, and the matcher now accepts only the path `~/.claude/rules/<stem>.md` or the backticked stem.
- *2026-08-22, the hook* — The tree that lands after B6 also carries the round's hook-class escalation, which this ledger's per-batch entries never name: the third hook, `review-receipt`, under [ADR-0059](0059-review-receipt-hook.md), and with it the sixth line of the `Landing:` key defined under Round decisions above, `Review required:` — that definition's five contents are six from here on.
- *2026-08-23, the escalation row* — "`write-skill` gains the general escalation row" under No em-dash hook above never happened in `write-skill`: the row landed on 2026-08-23 in `writing-for-agents` (its Scaffolding section and `references/predictability.md`'s form-to-failure table), the skill that owns instruction-file conventions, where `write-skill` already routes. Its scope is the escalation of a rule that has already failed as prose — prose, then hard prohibition, then hook or lint — and "each step justified by an observed failure" (this record, ADR-0059) still governs that climb; creation of a new rule starts at the rung that can see the failure, which is [ADR-0053](0053-global-rules-layer.md)'s why-not-a-hook admission read forward, not a license to start at the top.
- *2026-08-26, the post-B8 window* — The three retire checks and the load re-measure scheduled above ran on 2026-08-26, on this machine's transcripts for every name but the three Domain skills (the work machine's paste covers only those; it received the skills on 2026-08-25 and holds no earlier typing under any other name). Each count was run at write time. Typed `/grill-and-record` in a user turn after the stub landed (`80e74bb`, 2026-08-22 18:38Z): **0** — `~/.claude/history.jsonl` holds 57 all-time with the last at 17:34Z that day, the parsed transcripts 25 with the same last — against 6 typed `/grill-me` since; the stub is retired, [ADR-0058](0058-grill-me-absorbs-grill-and-record.md) carries it. Typed `/validate-behavior` or `/black-box-check`: **0** since the history file begins (2026-01-11), and 0 `Skill` loads under either name — **kept one more window on the user's call**, because the work machine had the skill for one day before the count; [ADR-0041](0041-black-box-check-ported-from-behavior-validator.md) and [ADR-0057](0057-rename-table-and-discipline-skill-term.md) carry it, and a second zero on both machines retires it. `adoption-verdict` `Skill` loads: **1**, 2026-08-23 03:23Z in the a11y-health repo on "knip in a11y-health-ui; dmmulroy anti-slop oxlint plugin" — the fire B7 was expected to produce, reached by the model with nothing typed; the axis stays model-invoked and the flip line above closes. The load re-measure, against the axis audit's figures (typed from `history.jsonl`, loads from parsed `Skill` tool_use calls over every local transcript): `discoverable-code` 0 → 4 loads in 4 sessions, 3 of them since 2026-08-22; `tdd` 7 → 7 loads, 1 typed since; `implement` 101 typed all-time (the audit's 80 was since 2026-07-10), none since 2026-08-06; `work-item-shape` 0 → 0, which measures nothing here — no publisher has been typed on this machine since the load gates landed (`to-story` last 2026-08-05, `to-bug` 2026-08-06, `to-tasks` 2026-07-26, `to-feature` never), so that check waits for a work-machine window with publisher runs in it. Nothing changed on a delta. Two traps for the next counter, both over-counting: `grep 'Launching skill: X'` counts skill-body gate text, not loads — `implement`'s and `review-changes`' bodies each carry the phrase for `discoverable-code`, so it reports 265 against 4 real loads; and the bare string `"skill":"X"` is written by a `StructuredOutput` probe answering *which skill would you load* and by the workflow journal echoing that answer, so `work-item-shape` returns 6 against 0. The measure is the parsed tool_use, `{"skill":"X"` inside an assistant `Skill` call. Both shapes are safe for a zero and wrong for anything else. The Domain fire-count confirmation is in [ADR-0055](0055-first-domain-skill-phi-safe-code.md) and [ADR-0060](0060-third-domain-skill-accessible-ui.md).

- *2026-08-26, the work-machine window* — The `work-item-shape` re-measure the entry above left open closes, and it closes the other way. Measured on the work machine, where the publishers actually run, as the parsed tool_use and excluding the measuring session: **4** `Skill` loads (2 in `aeo-poc-demo`, 2 in `playbook`), against **112** typed invocations of the four publishers that gate on this skill — `to-story` 82, `to-tasks` 17, `to-feature` 12, `to-bug` 1. `chart-course` was typed twice more and is excluded: it gates `writing-for-humans` alone, and its body routes Decision-ticket bodies to chart-format rather than to this discipline, so the Publisher load gates line under Round decisions above is wrong on the `work-item-shape` half where it groups `chart-course` with the four. **The gated skill does fire**; the 0 above measures this machine's absence of publisher runs, not the gate, and the entry's "measures nothing here" was the right reading of it. What the count does *not* settle is the ratio: 4 loads against 112 is either mostly pre-gate typing — the load gates landed in this round, at `2321864` on 2026-08-22 in this repo, and on the work machine only when it received that tree, which is the later date — or a gate that misses. UNVERIFIABLE from this machine, since the per-invocation dates live on that one; the next work-machine session answers it by dating the typed runs against **that machine's own gate landing**, not this repo's commit date, and only a gate that misses is a defect. `validate-behavior` and `black-box-check` are also 0 typed and 0 loaded there, so the work machine contributes nothing to that window yet and nothing is retired on it.

- **2026-08-27** — The one check left open above — `validate-behavior`'s, awaiting a second zero from both machines — is canceled by [ADR-0063](0063-team-fit-test-replaces-retire-on-zero-window.md); `adoption-verdict`'s flip check had already closed on 2026-08-26 under [ADR-0032](0032-adoption-verdict-standalone-behavior.md), and is restated here rather than canceled. Each skill was judged on the Team-fit test in the 2026-08-27 grill instead. `validate-behavior` stays: the moment is an engineer proving a change works in the running app. `adoption-verdict` stays model-invoked: the question arrives in prose, from a teammate who may not know the suite. A42.S1's unpark condition ("three same-shape corrections in a week") was met between 08-21 and 08-26 by 4 records corrected after review (13f39ad, 93cde99, this record's post-B6 corrections, e1b4423). It is not a fold, because `round-log.md` is the corrections ledger the park asked for; it is logged as the first data point for M4's measure-first check. Three sites in this record's own body are superseded rather than rewritten, since amendments never rewrite: the Consequences paragraph's "Three retire checks are scheduled for the next window" — none is now scheduled, the stub's having closed on its own count and the other two being Team-fit calls; the *Round decisions* line for `adoption-verdict`, "flips if the next window is still zero fires"; and the `black-box-check` rename line, whose recorded rationale was "the window measures the skill under a name that describes it". The rename stands on its own merit under [ADR-0057](0057-rename-table-and-discipline-skill-term.md)'s stranger-at-autocomplete criterion; what is gone is the window it was argued from.
- **2026-08-29** — *No em-dash hook* above says "chat output, which no hook can see"; a Stop hook can. The rejection stands on the ground [ADR-0053](0053-global-rules-layer.md)'s 2026-08-29 amendment records — judgment, and one measured trial of a regex that caught nothing the rule had missed — not on reach.
