# Opt-in post-merge hook auto-runs install.sh

> **Amended by [ADR-0067](0067-commit-msg-hook-checks-shape-not-register.md) and [ADR-0076](0076-claude-md-admits-triggers-and-contracts-only.md):** the one opt-in now enables three git hooks, not one — `setup-hooks.sh` also wires a `commit-msg` hook that blocks a commit whose message breaks shape, and a `pre-commit` hook that blocks a commit whose staged paths fail their linter, where everything this record decided only warns after a pull.

## Context

`scripts/install.sh` (ADR-0007, ADR-0016) is user-initiated: after a `git pull` that adds, renames, or removes a skill, the maintainer must re-run it by hand or `~/.claude/skills/` drifts out of sync with `src/`. The failure is silent — a newly pulled skill simply isn't hoisted, and nothing signals the gap until the skill is reached for and missing.

A `post-merge` git hook removes the manual step: git runs it after every merge (including the merge half of a `git pull`), so install.sh re-runs automatically. Two facts constrain how this can be delivered:

- Git hooks live in `.git/hooks/`, which is **not** version-controlled — a committed hook body cannot install itself. The standard workaround is to commit the hook under a tracked directory and point `core.hooksPath` at it.
- Redirecting `core.hooksPath` to a committed directory means **any** hook in that directory — present now, or added by a future pulled commit — executes automatically on its git event, under the user's UID, with no further prompt. Because this repo is symlinked into `~/.claude/skills/`, that execution surface reaches the Claude Code config directory.

That last point is the load-bearing trade-off: convenience (no manual install) is bought with standing consent to auto-execute committed scripts on pull. It is inherent to every committed-hooks setup (Husky, pre-commit, et al.), not specific to this implementation.

ADR-0016 fixed the install *model* — per-skill symlinks, `install.sh` as the resolver, "no plugin migration." This decision is about *when* install runs, and does not move that premise: the symlink model, dependency resolution, and cherry-pick portability are all untouched. An amendment to 0016 would therefore be the wrong instrument; this is a new, additive decision.

## Decision

- **Add an opt-in `post-merge` hook.** `scripts/git-hooks/post-merge` re-runs `install.sh` after a merge. `scripts/setup-hooks.sh` is the one-time, per-clone opt-in that sets `core.hooksPath` to the committed hooks directory. Neither is active until a user runs the setup script.
- **The symlink/per-skill install model of ADR-0007 and ADR-0016 is unchanged.** The hook only automates *invocation* of the existing `install.sh`; it adds no plugin model, no new install mechanism, no change to dependency resolution.
- **Accept the committed-hooks trust trade-off.** For a personal, single-maintainer skills repo, the supply-chain surface is the maintainer's own commits; the convenience is worth the standing-consent cost. The acceptance is made *informed*, not silent: the README opt-in states plainly that enabling the hook auto-runs committed scripts on every pull, and to enable it only on a repo whose commits you trust.
- **The hook never blocks the pull and never fails silently.** A `post-merge` non-zero exit cannot abort a merge (git ignores it), so a failed sync must announce itself with an actionable message rather than scroll past as buried noise.

## Considered Options

- **Amend ADR-0016** — rejected: 0016's premise (the install model) has not moved; the hook is additive automation over it. Amendment is for a standing decision whose premise shifted, which is not this.
- **No ADR (treat as mere tooling)** — rejected: the hook changes *when* install runs and introduces an auto-execution surface; the ADR-gate discipline records exactly this class of change, and the commit-order rule wants the rationale committed before the tooling.
- **Confirmation gate in `setup-hooks.sh`** (prompt before enabling) — rejected as heavier than a personal repo needs; the opt-in *is* the gate, and the README makes its consequence explicit.
- **Opt-in post-merge hook + informed acceptance + new ADR** (chosen).

## Consequences

- New tracked directory `scripts/git-hooks/`. This is the first committed-hook artifact in the repo; future hooks would live here and inherit the same auto-execution property.
- `install.sh` now runs from two contexts (manual and hook-driven); its idempotency and re-run safety, already relied on by ADR-0016, become load-bearing for the automated path too. Pre-existing hardening gaps in `install.sh` (unquoted `requires:` expansion, `prune_stale` prefix matching) gain a lower-friction trigger and are worth tightening, tracked separately. — corrected: see Amendments 2026-09-01
- The hook fires on *any* merge, not only `git pull` — `git merge <branch>` triggers it too. Documented in the hook comment so the behavior isn't surprising.
- `git pull --rebase` does **not** fire `post-merge`; the manual `bash scripts/install.sh` remains the always-available fallback, stated in the README caveat.

## Amendments

- **2026-08-21** — [ADR-0053](0053-global-rules-layer.md) gives `install.sh` two more jobs, linking `global/rules/` into `~/.claude/rules/` and printing the hook snippet; the post-merge hook re-runs all three, and the trust trade-off above covers them unchanged.
- **2026-08-22** — The hook's scope widens past "invocation of `install.sh`": before the re-hoist it runs four gates (`scripts/lint-skills.sh`, `scripts/lint-selftest.sh`, and both `global/hooks/*-selftest.sh`, which invoke the PreToolUse hooks with synthetic payloads), warning rather than aborting, and prints one line naming any pulled change under `global/hooks/`. That line exists because the `settings.json` snippet points at the live checkout, so a pulled hook edit changes the running PreToolUse check with no re-consent — a trade-off this record's trust paragraph now covers explicitly: the README's opt-in names every script the hook runs, not only `install.sh`.
- **2026-08-22, later** — A fifth gate: `global/hooks/review-receipt-selftest.sh` joined the list with the third hook ([ADR-0059](0059-review-receipt-hook.md)); "both `global/hooks/*-selftest.sh`" above now reads as all three, and the README's trust paragraph names them as "the hook self-tests".
- **2026-08-23** — The gate list is no longer hand-kept: the hook derives the selftest roster from `global/hooks/*.sh` headers carrying an `# Install note:` line, the same derivation `install.sh` uses, and warns when a hook on that roster has no `<hook>-selftest.sh` beside it. "Joined the list" above is the last time a selftest was added by hand.
- **2026-08-28** — Briefly false, then true again. [ADR-0067](0067-commit-msg-hook-checks-shape-not-register.md) added a `commit-msg` git hook whose self-test was hardcoded into the gate list by hand, because the derivation above walks `global/hooks/` only and the new hook lives in `scripts/git-hooks/`. That is now derived too: the roster takes every `scripts/git-hooks/*-selftest.sh` alongside every `global/hooks/*.sh` carrying an `# Install note:` line, so "no longer hand-kept" holds for both hook directories. Only `scripts/lint-skills.sh` stays hardcoded, being the subject of a self-test rather than one. `scripts/security-selftest.sh` is deliberately outside the pull-time roster — it grades a scanner run by hand before reading an external directory, and the scheduled corpus sweep is where it runs.
- **2026-08-28, later** — The gate loop now reads **three** statuses rather than two. It discards each gate's stdout and stderr, so the status is the only channel a gate has, and `scripts/lint-selftest.sh` gained a case that is neither pass nor fail: a run that graded everything it could reach and left its two read-error branches unexercised, which happens under root or on a filesystem that ignores modes. That exits **2**, and the loop warns that the gate *graded partial* rather than that it failed. Any other non-zero is still a failure warning, 0 is still clean, and the hook still warns rather than aborts. Nothing about the roster or the trust trade-off moves.
- **2026-08-29** — Two things the 2026-08-29 review found in the gate loop. First, it could not reach its own three-state dispatch: the gate call was a bare command under this hook's `set -e`, so the first failing gate ended the hook before the WARN, before every later gate, and before the re-hoist — every "warns rather than aborts" claim above was true of the code as read and false of the code as run, from the day the loop landed. The call is now `|| gate_status=$?`, and `scripts/git-hooks/post-merge-selftest.sh` (a fake repo of gates with known exit codes, run through the hook) proves the loop runs every gate and draws each status's line; it is on this hook's own roster. Second, "only `scripts/lint-skills.sh` is hardcoded" stopped being true when the 2026-08-27 round added `scripts/lint-adrs.sh` and two more selftests under `scripts/` that no roster reached. The `scripts/` roster is now derived like the other two — every `scripts/*-selftest.sh`, which `lint-skills.sh` demands for every `scripts/*.sh` — and only the two linters are named by hand, because they are the subjects of selftests rather than selftests. `security-selftest.sh` joins the roster with that derivation: the exclusion written into the hook was about `security.sh` being run by hand, but its selftest's last row grades every shipped skill body, which is exactly what a merge brings in.
- **2026-08-30** — A third git hook, and the second that blocks. [ADR-0076](0076-claude-md-admits-triggers-and-contracts-only.md) adds `scripts/git-hooks/pre-commit`, which runs `scripts/lint-skills.sh` when a staged path is under `src/`, `global/`, `.claude/skills/`, or `scripts/`, or is `README.md`, `DOMAIN.md`, or `CLAUDE.md`, and `scripts/lint-adrs.sh` when one is under `docs/adr/`, and exits 1 when either is red — so the one opt-in this record made now also makes `git commit` run committed scripts, and refuse. The trust trade-off above widens by that much: `git pull` was the only event that auto-ran the tree's scripts; `git commit` is the second, and the README's trust paragraph names it. Its selftest, `pre-commit-selftest.sh`, joins the pull-time roster through the `scripts/git-hooks/*-selftest.sh` derivation of 2026-08-28. And the consent's object is wider than the hooks directory: what a hook invokes is other repo scripts — `scripts/lint-*.sh` on the mapped paths, and every staged gate's `*-selftest.sh` through the hook's selftest map — so the trust now covers every repo script a hook runs, on every commit touching a mapped path, not only the hooks in `scripts/git-hooks/`. `setup-hooks.sh`'s TRUST NOTE says exactly that at the moment of opt-in. The banner above now names this record beside ADR-0067.
- **2026-09-01** — Both "tracked separately" gaps are closed, on the S+M review's fix pass. The prefix matching was the finding that `prune_owned` (the function `prune_stale` became) asserts nothing about its owning prefix, so an empty second argument degrades the arm to `/*` and matches every dangling absolute symlink under the target; it now refuses to prune with no prefix. The `requires:` expansion was the finding that `link_skill` follows a dependency name verbatim, so a `..` or a slash in one sends `ln -s` outside `TARGET_DIR` — the `-d` test cannot tell that apart from a real skill — and a name that is not a plain directory entry is now warned and skipped. Neither was reachable from this repo's own frontmatter; both were reachable from the lower-friction trigger this record named. Nothing about the roster or the trust trade-off moves.
- **2026-09-05** — The `pre-commit` map has widened past what the 2026-08-30 entry above restates: the `lint-skills.sh` arm now takes all of `docs/`, and `docs/adr/` runs both linters. The hook as shipped also mapped `docs/lineage.md` by name — `git show <pre>:scripts/git-hooks/pre-commit` lines 11 and 116, and `README.md:212` — which the entry above never carried and no record ever did; the `docs/` arm subsumes it, and this line is where that name enters the record and leaves it. The entry above is left as written, per the additive rule; [ADR-0076](0076-claude-md-admits-triggers-and-contracts-only.md)'s 2026-09-05 amendment is the operative map and the record that owns it.
