# A `global/` layer of rules that fire with no skill loaded

> **Amended by [ADR-0077](0077-tightening-round-one-house-style.md):** the rules layer gains a 12,000-byte directory budget and the "before it knows to look" admission test; two rules move most of their mechanics into skills and both survive residually.

> **Amended by [ADR-0075](0075-transcript-sourced-mining-round.md):** the admission rule's "states why a hook or lint cannot do its job" is decided by a count over a stated window of the artifacts the rule governs — the count-based control on `global/README.md`'s key — and § Why rules and not hooks reads narrower: a hook can see most of the surfaces, at a precision that makes it useless.

Two rules failed in the measured window while existing as text: "no em dashes in outbound prose" (a memory entry) and "commits go through `/ship`" (the ship skill's premise). Both failed the same way. A skill's rule binds only while the skill is loaded, and a memory entry binds only while it is recalled; the failures happened in turns where neither held. The work machine, where most of them happened, cannot pull this repo's memory file at all. The fixes the round adopted for the trust cluster, the evidence rule, the recommend-and-proceed bins, the no-unasked-commits rule, and large-write chunking, all share this property: they govern chat output and ad-hoc turns, which is exactly where no skill is in force.

## Decision

The repo gains a `global/` directory. `global/rules/*.md` holds rules that a user's `~/.claude/rules/` picks up on every turn; `scripts/install.sh` symlinks them there additively, pruning only links that point into this repo's own `global/`, and never touches `~/.claude/CLAUDE.md`. `global/hooks/` holds hook scripts; `install.sh` prints the `settings.json` snippet that wires a hook and never edits `settings.json`, because a settings file merged by a script is a settings file the user no longer knows the contents of.

Four rules ship: `evidence`, `recommend-and-proceed`, `no-unasked-commits`, `large-write-chunking`. One hook ships: `rename-safety`, a PreToolUse check on Bash that blocks in-place mass edits — `sed -i`, `perl -pi`, and `xargs` feeding either — and names the path-shaped arguments it can see. It is opt-in by directory, fails open, and carries its own expect/reject selftest, because a fail-open gate whose regex drifts is indistinguishable from a clean run.

## Why rules and not hooks

A hook sees tool calls and file writes. None of the four rules can be checked there: a hook cannot tell an asked commit from an unasked one, cannot sort a question into the fact, judgment, or preference bin, cannot see the chat message where an unverified count was stated, and sees a truncated write only after it has truncated. The em-dash hook three reports asked for was rejected for the same reason; the em dashes that were caught were in chat output. Where a hook *can* see the failure, the rename case, the hook is the right rung, and it is the only one this round ships.

## Admission rule

`global/` holds only rules a skill depends on. Each rule file names the depending skills and states why a hook or lint cannot do its job — amended: see Amendments 2026-08-31; `global/README.md` states the rule and lint checks the `Depends:` line resolves to an existing skill. A named skill cites the rule from the batch that edits that skill onward — the `Depends:` line is the dependency, the citation is the later batch's deliverable, and lint grows a citation check once the last of those batches lands. Without this gate the directory becomes a second CLAUDE.md, the place every preference lands because it is global, and the context cost of a rule loaded on every turn is paid by every project the user opens.

## Considered Options

- **Add the rules to `~/.claude/CLAUDE.md` by hand.** Rejected: it is not version-controlled, does not reach the work machine, and the memory-file failure is the evidence that a personal file does not bind.
- **Have `install.sh` edit `settings.json` and `CLAUDE.md`.** Rejected: an installer that rewrites the user's harness config is the trust failure the round is fixing, moved one level up. Printing the snippet keeps the edit a human act.
- **A hook per rule, as the reports suggested.** Rejected per the section above; the one hook that can see its failure ships.
- **Rules inside the skills that consume them.** Rejected: that is the arrangement that failed. The consumers are the skills named in each `Depends:` line; the rule lives above them so it holds when they do not load.

## Consequences

`install.sh` now runs three jobs (skills, rules, hook snippet) and the opt-in post-merge hook from [ADR-0049](0049-opt-in-post-merge-hook-auto-runs-install.md) re-runs all three on pull; the rules share the trust trade-off that record accepted. Lint gains a `Depends:` check and the 200-line and no-ADR-number checks extend to `global/rules/`. The layer is the host later batches point at: `committing` cites the no-unasked-commits rule, `receiving-review` and `address-findings` cite the evidence rule, `grilling` and `implement` cite the bins. A rule that loses its last depending skill leaves the directory.

## Amendments

- **2026-08-22 (see [ADR-0054](0054-commit-bypass-hook-always-on.md))** — A second hook ships: `commit-bypass`, always on rather than opt-in by directory, with `committing` as its depending skill. "The only one this round ships" above was true of Batch 1; the rename case is no longer the only failure a hook can see. The admission rule stands — the hook's header names its dependant, and `global/README.md` carries the roster.
- **2026-08-22** — A fifth rule, `outbound-dash-sweep`, shipped in B5 of the 2026-08-21 round: the em-dash rule that failed as prose in memory, the register row, and the catalog; the failing case is a Teams draft typed with no skill loaded. It carries the same admission (`writing-for-humans` and `ask-for-me` depend on it) and the same why-not-a-hook answer, with the file case added: an outbound draft written to disk has no name a hook can distinguish from code.
- **2026-08-22** — The citation check landed in B6 of the 2026-08-21 round, ahead of the "last batch" trigger above — not because every dependant already carried its citation: it found two that did not (`chart-course` for `large-write-chunking`, and `committing` for `recommend-and-proceed` once the B6 review had the bare-directory matcher tightened), both closed in the same change; the accepted forms are the path `~/.claude/rules/<stem>.md` and the backticked stem. The same day a third hook, `review-receipt`, shipped under [ADR-0059](0059-review-receipt-hook.md): the `Landing:` key grew a `Review required:` line, and `hooks/` now covers a precondition on disk as well as a command-line shape.
- **2026-08-23** — `rename-safety` no longer matches a regex over de-quoted text; it tokenizes the command line through the scanner all three hooks share ([ADR-0054](0054-commit-bypass-hook-always-on.md) Amendments 2026-08-23). "`sed -i`, `perl -pi`, and `xargs` feeding either" above is now the in-place flag found as an argument of the program that runs — `ruby -i` included — however the flag reaches it; "a fail-open gate whose regex drifts" reads as a rule that drifts, and the selftest is still what tells drift from a clean run. `global/hooks/` also holds two files that are not hooks, `hook-lib.sh`/`hook-lib.py` and `selftest-lib.sh`; a hook is the file whose header carries an `# Install note:` line, which is how `install.sh` tells them apart.
- **2026-08-29** — *Why rules and not hooks* above rests in part on "cannot see the chat message where an unverified count was stated", and that premise is no longer whole: a Stop hook reads the final message, and a PostToolUse hook reads the files a claim lands in. The 2026-08-27 round's batch 9 tried the mechanism the premise ruled out — a claims regex over 75 handoff and review files in the landing zone and 112 commit messages in this repo's log — and it matched prose about verification and never a claim the rule had missed (the figure is cited inline in `global/rules/evidence.md`'s "Why not a hook" line since this date). The rules layer therefore stands on a narrower ground than this record wrote: not that no hook can see the surface, but that whether evidence supports a claim is a judgment no shape a check can match, and the one trial of a shape found nothing. The decision is unchanged; its reason is. [ADR-0051](0051-usage-evidence-mining-round.md)'s "no em-dash hook" rests on the same sentence and carries a pointer here.
- **2026-08-31** — The tightening round's C9 batch moves every `Why not a hook or lint:` line out of the rule files and into this record, per [ADR-0077](0077-tightening-round-one-house-style.md) § The always-loaded rules: the line is admission evidence for a maintainer, not a rule the loaded session obeys, and the five totaled 2,214 bytes paid on every turn in every project. § Admission rule's "Each rule file names the depending skills and states why a hook or lint cannot do its job" now reads: each rule file carries the `Depends:` line, and its `Why not a hook or lint:` control — the count `global/README.md`'s key defines — is recorded here, one entry per rule. [ADR-0075](0075-transcript-sourced-mining-round.md)'s batch-4 amendment is where four of the five counts were first recorded and re-run under the key; this record is now the one place they are read from, and a correction to a count lands here. The five counts, verbatim from the lines as cut — each bullet quotes its line whole, mechanism clause and corpus path included, with this batch's own caveats appended in italics after the quoted text:
  - `evidence.md` — a Stop hook can read the final message and a write hook the files a claim lands in — a commit message, a review report, a handoff — but whether the evidence supports the claim is a judgment, not a shape a check can match; a claims regex run on 2026-08-29 over 75 handoff and review files in the landing zone and 112 commit messages in this repo's log matched prose about verification, never a claim the rule had missed. *Recorded as non-conforming to the key's contract*: it states a single run date, not a ≥30-day window, and no fire count — the 2026-08-30 re-run that brought the other four under the key (ADR-0075's fix pass) never reached this pre-existing line, and re-deriving the judgment-graded match set is not a command a batch can re-run.
  - `large-write-chunking.md` — a hook sees the write after it has already been truncated, and cannot see the chat message that was cut mid-section — over the 30 days to 2026-08-30, the transcripts under `~/.claude/projects/` hold 430 `Write` calls and no truncated one (429 end in a newline; the other is an 11-character placeholder), so a size threshold has no failure to key on and refuses whole documents. The zero-failure count is also the strongest evidence the file-write half had no observed failure — one input to the same batch's decision to move that half into `handoff` § Where to write it.
  - `no-unasked-commits.md` — a hook sees the `git commit` call, and cannot tell an asked commit from an unasked one — over the 30 days to 2026-08-30, the transcripts under `~/.claude/projects/` hold 117 real `git commit` calls, 34 with no ask in a user turn, a `/ship`, or an `AskUserQuestion` answer, and one of the 34 the failure (2026-08-22: "Hold up. Did we ship these changes without reviewing them first?"), so a transcript-grepping hook fires 34 times to catch one and reads a handoff-carried ask as the failure.
  - `outbound-dash-sweep.md` — the draft is chat output, or a file with no distinguishing name — a Write hook would have to fire on every file, code included, to catch it — and a Teams paste never passes through a tool; over the 30 days to 2026-08-30, the transcripts under `~/.claude/projects/` hold 3,327 assistant text blocks carrying an em dash against 4 draft-shaped ones, so a Stop hook keyed on the glyph fires 3,327 times for 4 drafts. *Caveat carried from the vet*: 4 is the draft population, not a failure count — the line never stated how many of the 4 carried a dash.
  - `recommend-and-proceed.md` — which bin a question falls into is a judgment the harness cannot see — over the 30 days to 2026-08-30, the transcripts under `~/.claude/projects/` hold 84 `AskUserQuestion` calls carrying 140 questions and 83 assistant turns closing on a question mark, none of the 140 a fact a command answers, so a hook keyed on the ask has 167 events to adjudicate and no failure among them. *Caveat carried from the vet*: the zero is asserted over the 140 questions while the fire count is 167 events (84 calls + 83 turns) — the two populations differ.
