---
name: grill-and-record
description: Deprecated — `grill-me` records by default now. This name is retired after one window.
disable-model-invocation: true
---

# Grill and Record (deprecated)

This name is retired. Type `/grill-me` — it records to `DOMAIN.md` and the ADR log by default, and `--plain` turns that off.
