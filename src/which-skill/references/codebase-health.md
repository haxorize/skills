# Codebase health: the family boundaries

Open this only when an ask is upkeep-shaped rather than feature work and two of the eight routes both fit.

- **`/upgrade-deps`** — **(Dependencies)** upgrade a project's dependencies in the safe order (security first, each major its own step with the changelog read and the suite run, then the rest), with a supply-chain check on every package before the lockfile moves. npm, pip/uv, NuGet.
- **`/review-architecture`** — read-only architecture review of the whole codebase; surfaces deepening opportunities. Picking one frames a design, offers a grill (`/grill-me`), then hands you to `/to-story` to file the result. Declares `adr`, so a rejected candidate's load-bearing reason gets recorded where the next review would re-propose it.
- **`/sweep-domain`** — sweep the codebase to refresh `DOMAIN.md` when the vocabulary has drifted.
- **`/backfill-adrs`** — sweep recent git history for architectural decisions that were made but never recorded.
- **`/verify-docs`** — check a document's claims against what it answers to — the code and tests, the running product, or the sources a derived doc was distilled from — verdict per claim, an evaluation ledger's rows included, where one past its expiry is STALE. The prose-drift sibling of `/sweep-domain` (vocabulary) and `/backfill-adrs` (decisions); run it pre-publish, post-refactor, or as a periodic sweep — `feedback-loops` still auto-fixes docs the current change touched.
- **`/audit-tests`** — sweep an existing test suite asking "can these checks fail?", grading each load-bearing assertion (CONFIRMED / PLAUSIBLE / CANNOT FAIL / BLIND) and naming the suite's blind spots. The test-suite member of this family; `tdd`'s mutation check guards new tests at writing time.
- **`/delete-dead-code`** — a deliberate whole-repo dead-code sweep: find what nothing calls, tier it SAFE / CAUTION / DANGER, and remove it one test-verified deletion at a time. The source-tree member of this family — it removes what the others only judge; `implement` parks pre-existing dead code, `review-architecture` never deletes, `/simplify` works the diff.
- **`/validate-behavior`** — validate the *running* app, CLI, API, or generated artifact against a behavior contract written before testing: source-blind (reading the implementation contaminates the checker; a non-checker may derive the contract from legacy code), with anti-cheat probes that catch UI which only displays success. The runtime member of this family — `audit-tests` judges the suite, `verify-docs` the prose, `delete-dead-code` prunes the source tree, this the product itself. `verify-docs` also reaches a running product, to settle whether a sentence is true; the split is what is on trial — there the document, here the product, against a contract fixed before the run. That contract is one contract for one running target, written source-blind; a *rebuild* contract — pre-rewrite, everything that must survive, derived from the source on purpose — is `/rebuild-contract`'s.
